# Static website checkpoint — 7 September 2026

> **Status: historical, superseded.** Kept as the record of the 7 September
> 2026 checkpoint; do not act on its "Next acceptance" paragraph. On 8 September
> 2026 the owner selected Docker on the existing VPS for this website (root
> `AGENTS.md`, `DEPLOY-VPS.md`). On 21 September 2026 the GitHub Actions
> Cloudflare publication workflow was retired (commit `fe3cb72`); the local
> Cloudflare commands stay until a verified cutover. The `/tmp` evidence files
> cited below no longer exist (checked 23 September 2026). Current instructions:
> [deployment.md](deployment.md) and [DEPLOY-VPS.md](../DEPLOY-VPS.md).

Lumo UI: pnpm verify passed. Local Wrangler served all 16 locale pages and
19 referenced Next assets, with unchanged exported HTML, slash/root redirects,
security headers and real 404s in en/fa.
Evidence: /tmp/lumo-verify-local.log and /tmp/telarsa-static-runtime-results.json.

The website is not publicly deployed by this checkpoint. No visual component,
CSS, package dependency or lockfile was changed. The optional Caddy root locale
negotiation differs from the chosen static /en/ redirect; explicit locale routes
are retained. See deployment.md for commands, CLI authentication and the owner’s
separate apex/www Custom Domain bindings.

Next acceptance: deploy the verified export, record the Worker deployment ID,
bind both hostnames and repeat locale/asset/redirect checks through public DNS.
The owner waived remote CI and requested matching main/develop branches; this
checkpoint uses [skip ci]. No package tag is needed to deploy the website.
