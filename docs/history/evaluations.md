# The August 2026 evaluation arc

Kept because the conclusions changed the codebase; the reports themselves were removed once their findings were fixed.

**Trajectory (anchored scale, shadcn/ui = Mantine = 8, Ark = 7.5):** self-assessments had ratcheted to 9.6; the first independent blind reviews landed at ≈7.0; a fix pass reached 8.9 on the old surface; 19k lines of new components diluted it back to ≈7.5; the repair passes that followed were re-evaluated at 7.8, then 7.9; after the 15 August complexity pass (React Aria surface, experiments, docs, comments, single site registration, dead string groups) a fresh blind evaluator scored 7.3 and the maintainer's own anchored score is 7.8 — the blind reviewer weighed breadth/maturity at 6 (0.0.0, nine calendar days of history, no AT run) and complexity at "high, partly justified".

**What the reviews proved and what was done:**
- The mutation campaign asserted 99 modules against 111 and used a circular oracle → rewritten around `vitest related`, an `unobserved` status, and a registry-derived count; the honest baseline was 44/63/4, driven to 111/111 with styling floors and per-module behavioral operators.
- `chartMirror` was absent from four chart families → mirrored through each family's real seam, pinned on served SVG geometry.
- The served-bytes gate never saw a popup interior; `defaultOpen` cannot fix that (portals do not SSR) → the popup-interiors tier grades 18 families live. On first contact it found Base UI's hardcoded English `Dismiss` and the aria-hidden-label naming failure.
- 47% of generated prop descriptions were a false "inherited" filler → split, ratcheted, and documented to zero; cva variant keys turned out to be real doc sites.
- The React Aria compatibility surface (165 carriers, 127 discards, `slot`, `excludeFromTabOrder`) kept minting accepted-and-inert props — cancel buttons wired to a slot no engine reads, `hrefLang` served on a `<button>` → removed; `DialogClose` added; `tabIndex` made real.
- Table gained Home/End/PageUp/PageDown; Cascader and TreeSelect moved onto the shared popover; DataGrid renders its validation reason.

**Standing limits:** no browser or assistive-technology evidence exists; the mutation floor is one operator per module; distribution is private by decision.
- The site kept two registries for one catalog and `LumoStrings` carried four React-Aria-era groups nobody read → `demos.tsx` retired (examples files are the registration), the groups deleted, `no-latin-aria` keyed on script (decision §21).
- **15 Aug 2026 — adversarial pass at `709f792`: blind 7.8/10, post-fix 7.9/10; fix range `709f792..8b3e770`.** PhoneInput field/value semantics, required Dialog/Drawer popup names, named-role gate coverage, inert Dialog idrefs, and stale docs were closed; the parked registry endpoint remains an explicit owner decision.
- 15 Aug (evening), first pass scored on `docs/rubric.md` at `b7526b0`, blind: **7.3** (A 8.0 · B 6.7 · C 7.8 · D 7.9 · E 6.8 · F 7.5 · G 6.9 · H 6.0 · I 3.5). Confirmed: ListBox PageUp/PageDown a no-op near the ends (fixed); `ToggleButtonGroup` name optional and `role=group` outside the gate (name required now; `radiogroup` graded); wrapped-Dialog lift gap (dev warning); vacuous chart/controls assertions (guarded); rule 14 blind to a control's `aria-label` and firing on hidden controls (fixed); mutation report unpublished (CI artifact); CONTRIBUTING/i18n doc drift (fixed). Goal #2's clean-pass counter stays at 0 — one A-dimension contract defect was confirmed.
- 15 Aug (night), second rubric-scored blind pass at `0ca83cc`: **7.2** (A 7.7 · B 7.0 · C 7.8 · D 7.5 · E 6.7 · F 7.5 · G 6.9 · H 6.0 · I 3.25). Confirmed: Select's open listbox pointed `aria-labelledby` at its role=combobox trigger, whose accessible name is its VALUE — the committed `select.aria.yml` showed an unnamed listbox (now labelled by the same label as the trigger); `ComboBox.label` optional with no other naming path (required now); one `<title>` for all 306 main routes (per-page metadata now); Toast `locale` docblock said inert while code used it; menubar/navigation-menu/date-picker/select/tabs/calendar/bubble copy still named React Aria as the engine (served in both locales — fixed). Also applied: description/errorMessage wiring on ComboBox, MultiSelect, TagsInput. Clean-pass counter for goal #2 stays at 0 (one A-dimension defect confirmed).
- 16 Aug, third rubric-scored blind pass at `57c58f4`: **7.3** (A 7.8 · B 7.2 · C 7.7 · D 7.5 · E 7.0 · F 7.5 · G 7.0 · H 6.2 · I 3.25). Confirmed in the library: `NumberField` served Latin, en-US-grouped digits under `fa-IR` (`value="1,234"`) — the engine formats in the runtime locale and nothing told it; it now takes the provider's locale (`locale` prop overrides), proved by SSR test. Also: the served-bytes gate was blind to digits inside attributes (exactly where that value lives) — `no-latin-digits` now grades `value`/`aria-valuetext`/`placeholder`/`alt`/`title` (Latin-by-nature and machine-value input types skipped; ColorInput's code field and MaskInput's masked value declare themselves islands); `MenuItem href` bypassed the router-link seam (fixed); the hover-card popup is `role=dialog` and is now name-checked in the evidence job; false claims corrected ("SWR-shaped", `link.tsx` docblock naming Pagination, NumberField comment). Clean-pass counter: 0 (one A-dimension defect confirmed).
- 16 Aug, fourth rubric-scored blind pass at `9f06bb8`: **7.5** (A 7.9 · B 7.3 · C 8.3 · D 7.75 · E 6.8 · F 7.75 · G 7.25 · H 5.8 · I 3.1). First pass to probe first-byte output of 16 product-depth families and all 31 blocks with realistic values. Confirmed in the library: `PowerSearch.resultCount` (number) served Latin digits; Gantt's zoom/split announced raw values; `AttachmentName` did not island Latin file names while `FileUploadItem` did; the gate did not read digits in `aria-label`/`aria-description`; the blocks grader used 2 of 14 rules; `dir` was declared on ~40 interfaces via `GlobalDOMAttributes` while the docs said otherwise; packages claimed MIT with no LICENSE. All fixed (decision §24). Left open and recorded: the date family's split calendar behaviour (DateField keeps the value's calendar; Calendar converts). Clean-pass counter: 0.

