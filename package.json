{
  "name": "lumo-ui",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm --filter @lumo-ui/website dev",
    "build": "pnpm -r build",

    "verify": "pnpm run gate:types && pnpm run gate:no-css-modules",

    "gate:types": "tsc --noEmit && pnpm -r --if-present run typecheck",
    "gate:no-css-modules": "node scripts/no-css-modules.mjs",

    "typecheck": "pnpm run gate:types",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
