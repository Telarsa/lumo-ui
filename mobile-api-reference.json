{
  "version": 1,
  "generatedFrom": [
    "packages/mobile/lib/src/*.dart"
  ],
  "undocumented": 0,
  "widgets": {
    "LumoAccordion": {
      "file": "packages/mobile/lib/src/disclosure.dart",
      "doc": "Several disclosures as one control — the web `DisclosureGroup`. `value` / `defaultValue` are the OPEN IDS (controlled / uncontrolled), `onChanged` reports the whole set; with `allowsMultiple` false (the default, as on the web) opening one closes the other.\n\nProp names: the web wrapper renames these to `expandedKeys` / `defaultExpandedKeys` / `onExpandedChange` over Base UI's own `value` / `defaultValue` / `onValueChange`; the mobile names are the engine's, which is what the rest of this package uses (`LumoTabs`, `LumoSegmentedControl`). `allowsMultipleExpanded` is `allowsMultiple` for the same reason.\n\nThe frame is the web's: a hairline above the first section and below every one of them (`border-y` + `divide-y`).",
      "props": [
        {
          "name": "items",
          "type": "List<LumoDisclosureItem>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "allowsMultiple",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether more than one section may be open at a time."
        },
        {
          "name": "defaultValue",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The open section ids at first build (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Disables every section."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<Set<String>>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "value",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The open section ids (controlled)."
        }
      ]
    },
    "LumoAlert": {
      "file": "packages/mobile/lib/src/alert.dart",
      "doc": "An inline status message — the web `Alert`, and NOT a toast: it sits in the layout where the thing it talks about is, stays until the app removes it, and does not float above the page (that is `showLumoToast`). `title` is REQUIRED here (optional on the web, where an alert may be body-only): an unnamed message on a phone is a coloured rectangle. `description` is the body, `actions` the controls under it.\n\n**The live-region rule.** `isLive` is FALSE by default, and that default is the contract, not an oversight: an alert that is already in the tree at the first frame would be announced at load, out of context, ahead of the screen the reader asked for — the same argument the web file makes for defaulting `live=\"off\"`. Set `isLive: true` ONLY when the alert APPEARS in response to something that just happened (a submit that failed, a code that was accepted) — i.e. when the widget is inserted, or its text replaced, by a state change the reader caused. Flutter's `liveRegion` is a per-NODE flag announcing that node's own label, so it is set on the title node and on the description node — one announcement each, in reading order — not on a wrapper (a wrapper has no label of its own to announce). `SemanticsRole.alert`/`.status` are NOT used: the framework's role check forbids a node from carrying the role and `liveRegion` at once, and the flag is the half TalkBack and VoiceOver act on today (`toast.dart` made the same call).\n\nSemantics: the title is a HEADER (a divergence, stated: the web renders it as a `<p>` so a transient message stays out of the document outline — mobile has no such outline, and `header` is how the heading rotor finds the message's summary line), the description its own node after it, the icon and the tint decoration (`ExcludeSemantics`), the ✕ a named button at the inline END. Nothing here truncates: the title and the description wrap, and the actions are a `Wrap`, so a narrow screen grows the alert instead of eating its words.\n\n**The ✕'s hit area.** The web draws AND hits the dismiss at `h-control-sm w-control-sm` — 29 logical px (`LumoControl.sm`), fine for a pointer and half a finger on a phone. The DRAWN box stays 29, matching the web exactly, so nothing about the resting or pressed look moves; the 44×44 minimum is a transparent tap surface CENTRED on it that OVERHANGS into the alert's own 16px padding. That is why the ✕ is a `PositionedDirectional` child of a `Stack` over the frame rather than a cell in the content row: growing the target inflates neither the alert's height nor the text column's width (the row keeps a same-width empty slot where the ✕ used to sit, so wrapping is unchanged). The 44 box carries the name, the button role and the tap, so explore-by-touch gets the whole target; the drawn button inside is `ExcludeSemantics`'d and keeps the theme's press feedback.\n\nWeb props not carried: `live` as a three-value `\"off\" | \"polite\" | \"assertive\"` (Flutter's semantics has ONE live flag — a polite/assertive parameter would reach nothing, so it is a `bool`), and `className`. `actions` is the addition the web deliberately does not have (there, an action is CONTENT inside `children`; Dart has no `children` slot of arbitrary markup here, so the slot is explicit).",
      "props": [
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The summary line. REQUIRED, and announced as the alert's heading.",
          "announced": true
        },
        {
          "name": "actions",
          "type": "List<Widget>?",
          "required": false,
          "default": null,
          "description": "Controls under the body, usually `LumoButton`s — slots, not labels plus handlers."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The body of the message.",
          "announced": true
        },
        {
          "name": "dismissLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced name of the ✕, e.g. «بستن». REQUIRED whenever `onDismiss` is set.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading glyph. DECORATIVE — the title says what the icon says."
        },
        {
          "name": "isLive",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Announce this message on arrival. See the docblock: true only for an alert that APPEARS in response to an action."
        },
        {
          "name": "onDismiss",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Called when the reader dismisses the alert. Removing it is the caller's."
        },
        {
          "name": "tone",
          "type": "LumoAlertTone",
          "required": false,
          "default": "LumoAlertTone.accent",
          "description": "The tone default is `accent`, as on the web."
        },
        {
          "name": "variant",
          "type": "LumoAlertVariant",
          "required": false,
          "default": "LumoAlertVariant.subtle",
          "description": "The visual variant."
        }
      ]
    },
    "LumoAlertDialogTrigger": {
      "file": "packages/mobile/lib/src/alert_dialog.dart",
      "doc": "The declarative shape, as `LumoDialogTrigger` is for `showLumoDialog`: a trigger whose press asks the question. `onConfirm` runs only on the confirming verb, `onCancel` on the declining one (and on a system back — there is no third answer); `onOpenChange` fires true on open and false when the dialog closes, whichever way.",
      "props": [
        {
          "name": "cancelLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The declining verb. Required.",
          "announced": true
        },
        {
          "name": "confirmLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The confirming verb — «حذف», not «بله». Required.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The consequence, in the reader's language. Required.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the dialog — the question. Required.",
          "announced": true
        },
        {
          "name": "trigger",
          "type": "Widget Function(VoidCallback? ask)",
          "required": true,
          "default": null,
          "description": "Built with the press that asks the question; `null` when disabled."
        },
        {
          "name": "isDestructive",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Renders the confirm button in the destructive variant."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onCancel",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Called when the reader backs out. The dialog closes either way."
        },
        {
          "name": "onConfirm",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Called when the reader confirms."
        },
        {
          "name": "onOpenChange",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called when the surface opens or closes, with the new state."
        }
      ]
    },
    "LumoAppBar": {
      "file": "packages/mobile/lib/src/app_bar.dart",
      "doc": "The screen header: a leading slot (a back button, or the caller's own widget), the title, and actions at the inline end.\n\n`title` is REQUIRED and is announced as a `header` — the node a reader jumps to when it asks for the headings of a screen. `onBack` brings `backLabel` with it (asserted): a chevron is a shape, not a name, and «Back» is the English default this library exists to prevent.\n\nThe back chevron is DIRECTIONAL. It is `Icons.chevron_left`, whose `IconData.matchTextDirection` is true, so the `Icon` widget mirrors the glyph itself under an RTL `Directionality` — back points LEFT in English and RIGHT in Persian, because \"back\" is the way the reading order came from. (The Khroos app hand-rolled this as `AppHeader` and had to hard-code `chevron-right`, since its icon set is a name table with no direction in it. That hard-coding is the defect this widget removes.)\n\nImplements `PreferredSizeWidget`, so it drops straight into `Scaffold(appBar:)` — where the Scaffold adds the status-bar inset and this widget's own `SafeArea` consumes it — and it works equally as the first child of a `Column`, which is how Khroos used its hand-rolled twin.\n\nNot Material's `AppBar`: that widget builds a `BackButton` whose tooltip and announced name come from `MaterialLocalizations.backButtonTooltip` («Back»), English no parameter of ours reaches — the same defect as the Material route helpers `gate:flutter-contract` fails the build on.",
      "props": [
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The screen's name. Required, and announced as a header.",
          "announced": true
        },
        {
          "name": "actions",
          "type": "List<Widget>?",
          "required": false,
          "default": null,
          "description": "Controls at the inline END — already-named widgets (`LumoIconButton`)."
        },
        {
          "name": "backLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced name of the back chevron. REQUIRED when [onBack] is set.",
          "announced": true
        },
        {
          "name": "centerTitle",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Centre the title. Defaults to centring only when there is a back button — the shape Khroos's `AppHeader` settled on, and iOS's own."
        },
        {
          "name": "leading",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "The caller's own leading widget, when the slot is not a back button."
        },
        {
          "name": "onBack",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Pressing the back chevron. Requires [backLabel]."
        },
        {
          "name": "subtitle",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A second line under the title — a place, a count, a state."
        }
      ]
    },
    "LumoAspectRatio": {
      "file": "packages/mobile/lib/src/layout.dart",
      "doc": "A box that keeps a fixed width-to-height ratio while its width flexes — the web `AspectRatio` (`aspect-ratio.tsx`). A ratio is a DIMENSION, not a direction: there is nothing here to mirror, which is exactly why it belongs in a library whose other primitives all do.\n\nThe usual child is media, which should be `BoxFit.cover` and decorative (`ExcludeSemantics`) unless it carries meaning of its own.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "ratio",
          "type": "double",
          "required": true,
          "default": null,
          "description": "Width divided by height: `16 / 9`, `1`, `4 / 3`."
        }
      ]
    },
    "LumoAttachment": {
      "file": "packages/mobile/lib/src/file_upload.dart",
      "doc": "One file in a list — the web `Attachment` / `FileUploadItem` as DATA, not a widget tree, because a Flutter consumer holds a `List` in state and rebuilds from it.\n\n`sizeLabel` is PRE-FORMATTED: the web's `formatFileSize(bytes, locale)` needs `Intl.NumberFormat`'s `style: \"unit\"`, which Dart's `intl` does not carry — so the app formats («۱٫۲ مگابایت») and this library never renders a raw number. `statusLabel` is REQUIRED for every state except `done`: the state must be in WORDS, since a spinner and a red border say nothing to a reader (WCAG 1.4.1). `progress` is the fraction 0..1 (null while uploading = indeterminate) and `progressLabel` is that fraction as the app formatted it («۴۵٪»).",
      "props": [
        {
          "name": "name",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The file's own name, exactly as the picker reported it."
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Why it failed, in words. Shown and announced when `status` is `failed`.",
          "announced": true
        },
        {
          "name": "progress",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "Fraction complete, 0..1. `null` while `uploading` = indeterminate."
        },
        {
          "name": "progressLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "`progress` as the app formatted it, e.g. «۴۵٪». Announced with the name.",
          "announced": true
        },
        {
          "name": "sizeLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The size, ALREADY formatted, e.g. «۱٫۲ مگابایت».",
          "announced": true
        },
        {
          "name": "status",
          "type": "LumoAttachmentStatus",
          "required": false,
          "default": "LumoAttachmentStatus.done",
          "description": "Where this file is in the upload lifecycle."
        },
        {
          "name": "statusLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What `status` MEANS. REQUIRED unless `status` is `done`.",
          "announced": true
        }
      ]
    },
    "LumoAttachmentTile": {
      "file": "packages/mobile/lib/src/file_upload.dart",
      "doc": "One file's row — the web `FileUploadItem` / `Attachment` (`row` variant). Usable on its own: an attachment on a sent message is not inside a picker.\n\nSemantics: the row is ONE named node — name, size, state, value, error, in that order — with every visible copy excluded, plus the ✕ as its own node underneath (`explicitChildNodes`). The progress bar is DECORATION here (`ExcludeSemantics`): the row's own name already carries the state and the value, and a second announcing node per row makes a five-file list unlistenable. `onRemove` and `removeLabel` are ONE decision (asserted) — an ✕ has no name of its own.",
      "props": [
        {
          "name": "file",
          "type": "LumoAttachment",
          "required": true,
          "default": null,
          "description": "The attachment this tile shows."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onRemove",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Drop this file."
        },
        {
          "name": "removeLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced name of the ✕. REQUIRED when `onRemove` is set.",
          "announced": true
        }
      ]
    },
    "LumoAvatar": {
      "file": "packages/mobile/lib/src/avatar.dart",
      "doc": "A person or organisation, as a picture or as initials — the web `Avatar`. `label` is REQUIRED: the name, announced (`Semantics(image: true)`), and the source of the initials shown when there is no `image` (or behind it while it loads). A `status` needs a `statusLabel` (asserted): the dot sits at the inline-END bottom corner (`PositionedDirectional`, so bottom-LEFT in Persian) and its meaning is announced after the name — person, then state, as one node.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name of who or what this is. Announced; the initials come from it.",
          "announced": true
        },
        {
          "name": "image",
          "type": "ImageProvider?",
          "required": false,
          "default": null,
          "description": "An image to show instead of the initials."
        },
        {
          "name": "shape",
          "type": "LumoAvatarShape",
          "required": false,
          "default": "LumoAvatarShape.circle",
          "description": "The shape variant."
        },
        {
          "name": "size",
          "type": "LumoAvatarSize",
          "required": false,
          "default": "LumoAvatarSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "status",
          "type": "LumoAvatarStatus?",
          "required": false,
          "default": null,
          "description": "A presence or state marker. It needs a `statusLabel`: a coloured dot is not a word."
        },
        {
          "name": "statusLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What the dot MEANS, e.g. «آنلاین». Required with `status`.",
          "announced": true
        }
      ]
    },
    "LumoBadge": {
      "file": "packages/mobile/lib/src/badge.dart",
      "doc": "A status marker — the web `Badge`: `label` REQUIRED and a String (the web's `children` is `LumoNode`, so a bare count is a type error there; here the String is the type-level guard — a count is passed already formatted with `formatNumber`). `tone` × `variant`, two sizes; a plain text node for the reader (no live region: one would re-announce every badge at load, and no button — a badge is not pressable). `LumoBadge.dot` is the count form: a round `solid` pill for «۳» unread, still a formatted String.\n\n**`size` is the mobile addition** (the web `Badge` is `text-xs px-2 py-0.5` and nothing else): `md` IS that web scale, `sm` is the denser step for a badge riding inside a list row or a tab label, where the web's only badge slot is beside body copy. `variant.outline` is the second addition — a quiet marker on a photo or an already-coloured surface, which the web has no slot for. `tone` and the `solid`/`subtle` treatments are the web's, verbatim.\n\n**Cramped behaviour.** Given a bounded width the badge SHEDS ITS ICON before it touches the words — `segmented_control.dart`'s `_fit()`, the house pattern — and only ellipsizes the label when even the bare label does not fit. It ellipsizes rather than wrapping because a badge is a fixed-height pill (the web's `whitespace-nowrap`) and a second line would break the row it rides in; nothing is LOST by that truncation, because the announced name is the `Semantics` label above the drawing and always carries the full string. Given an unbounded width — a badge in a `Row` with no `Flexible`, the ordinary case — it keeps the icon and grows, exactly as `w-fit` does. Before this, a long label made the inner `Row` overflow: measured «گزارش عملکرد سه‌ماههٔ چهارم شرکت» + an icon at 320 dp = \"A RenderFlex overflowed by 82 pixels\".",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading icon (decorative; the label names the badge)."
        },
        {
          "name": "size",
          "type": "LumoBadgeSize",
          "required": false,
          "default": "LumoBadgeSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "tone",
          "type": "LumoBadgeTone",
          "required": false,
          "default": "LumoBadgeTone.neutral",
          "description": "The semantic tone the colour carries."
        },
        {
          "name": "variant",
          "type": "LumoBadgeVariant",
          "required": false,
          "default": "LumoBadgeVariant.subtle",
          "description": "The visual variant."
        }
      ]
    },
    "LumoBarChart": {
      "file": "packages/mobile/lib/src/chart.dart",
      "doc": "A bar chart — the web's `barY` mark, as widgets.\n\nVertical is the Khroos `_MiniBars` shape (a daily series under a card header); horizontal is its `_BarList` (a ranked list with the figure beside the name). Both were hand-rolled in the app because the library had no chart, and both are proof of what a real screen needed.\n\nLumoBarChart( label: 'بازدید روزانه', summary: 'بیشترین بازدید در روز پنجشنبه', emptyLabel: 'هنوز داده‌ای برای این بازه ثبت نشده است.', data: [LumoChartPoint(label: 'شنبه', value: 120, valueLabel: '۱۲۰')], )\n\nDirection is the trap this widget is tested for: the bars live in a `Row`, so under fa-IR the FIRST category is drawn at the RIGHT and the axis runs right-to-left, with no flag and no physical offset anywhere in the file.\n\nSTACKED: a point with a `partValue` draws its whole translucent and the part solid inside it — one hue at two strengths, never a second colour. That is `_MiniBars` (daily views with the leads share filled in) and it is the one shape the app needed that a single-value bar chart could not express.\n\nCramped: a vertical chart sheds its category labels before it truncates one (`segmented_control.dart`'s `_fit()` house rule) — a day name measured wider than its bar's share of the row is not ellipsized to «ش…», it is dropped, and the names stay in the semantics tree where they were never at risk.",
      "props": [
        {
          "name": "data",
          "type": "List<LumoChartPoint>",
          "required": true,
          "default": null,
          "description": "The plotted points, in reading order."
        },
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What a reader is told when there is nothing to plot. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The chart's announced name. REQUIRED — an unnamed chart announces nothing.",
          "announced": true
        },
        {
          "name": "maxValue",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "The top of the scale. Defaults to the largest `value` in `data`."
        },
        {
          "name": "orientation",
          "type": "LumoBarChartOrientation",
          "required": false,
          "default": "LumoBarChartOrientation.vertical",
          "description": "Which axis the control runs along."
        },
        {
          "name": "partTone",
          "type": "LumoChartTone?",
          "required": false,
          "default": null,
          "description": "The tone of the SOLID part inside a bar that has a `partValue`. Defaults to the bar's own tone, so the stack is one hue at two strengths — never a second colour, which is how a stacked bar quietly becomes a rainbow."
        },
        {
          "name": "plotHeight",
          "type": "double",
          "required": false,
          "default": "120",
          "description": "Height of the bar area (vertical only), in logical pixels."
        },
        {
          "name": "showValueLabels",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Draw each point's `valueLabel` above its bar (vertical) — horizontal always shows it, because a ranked row has room for it."
        },
        {
          "name": "summary",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "One sentence about what the chart shows, announced as the chart node's value right after its name («بیشترین بازدید در روز پنجشنبه»)."
        },
        {
          "name": "tone",
          "type": "LumoChartTone",
          "required": false,
          "default": "LumoChartTone.accent",
          "description": "The tone every bar takes, unless a point names its own."
        }
      ]
    },
    "LumoBreadcrumbs": {
      "file": "packages/mobile/lib/src/breadcrumbs.dart",
      "doc": "A breadcrumb trail — the web `Breadcrumbs`/`Breadcrumb`/`BreadcrumbEllipsis` as one widget. `label` is REQUIRED for the same reason it is on the web: React Aria defaulted the trail's name to the English \"Breadcrumbs\", and an unnamed trail is anonymous, which is worse because no gate can see it.\n\n**The separator flips.** The web leans on `›` (U+203A) carrying the Unicode `Bidi_Mirrored` property; Flutter's counterpart is an IconData with `matchTextDirection`, which `Icons.chevron_right` has — so the chevron points at the reading END and becomes a left chevron under fa-IR with no `dir` flag and no `if (rtl)`. The Khroos app hand-rolled the trail with a hard-coded `chevron-left`, which is the same glyph pointing the WRONG way the moment the app is read in English.\n\n**The last crumb is the current page**: announced as text, never as a link (no `link` flag, no tap action, even when the caller gave it an `onTap`), and drawn in `fg` at `w500` — the web's `data-current` treatment. Flutter's semantics have no counterpart to `aria-current`, so the word itself is the only honest way to say it: pass `currentLabel` («صفحهٔ فعلی») and it is appended to the last crumb's announced name. Optional, not required, because unlike a nameless control an unannounced current crumb still reads correctly — it is simply the last thing in the trail.\n\n**Collapsing**: with `maxVisible` set and more crumbs than that, the middle ones fold behind a named button — `overflowLabel` is REQUIRED with `maxVisible` (the constructor asserts it), because a «…» has no name of its own. The web's `BreadcrumbEllipsis` is deliberately INERT punctuation (it keeps that file server-only); here it is a real button that expands the trail in place, since a phone has no room to show the trail another way and a menu route for three words is worse than the words.\n\n**The touch band.** The web's trail is a 20 px-high row of text, and mobile measured the same: a link crumb was a 57x20 target and the «…» button 14.3x14 — the smallest control in the library. Every TAPPABLE part of the trail therefore lays out `LumoControl.lg` (44) on the block axis, and the «…» 44 on both. The text still paints at `text-sm` in the same place; the band is transparent space around it, so the trail is 44 tall wherever it has a link. A deliberate deviation from the web, for the reason mobile always deviates: a finger is not a mouse pointer.",
      "props": [
        {
          "name": "items",
          "type": "List<LumoCrumb>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the trail, e.g. «مسیر صفحه». Required.",
          "announced": true
        },
        {
          "name": "currentLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Appended to the last crumb's announced name, e.g. «صفحهٔ فعلی».",
          "announced": true
        },
        {
          "name": "maxVisible",
          "type": "int?",
          "required": false,
          "default": null,
          "description": "How many crumbs stay visible before the middle ones fold away."
        },
        {
          "name": "overflowLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced name of the button the folded crumbs hide behind. REQUIRED with `maxVisible`.",
          "announced": true
        }
      ]
    },
    "LumoButton": {
      "file": "packages/mobile/lib/src/button.dart",
      "doc": "The primary action — the web button's contract on Material's button: four variants, three sizes on the shared control scale, disabled announced, pressed takes the \"hover\" fill, focus ring for keyboard/switch access. The child is a Widget (Flutter has no `LumoNode`; a raw number in a `Text` is the app's — Dart cannot forbid it by type, `formatNumber` is the rule).\n\n**`sm` (29) and `md` (36) sit BELOW the 44 px touch floor, deliberately.** The web says the same out loud — `button.variants.ts` annotates only `lg` with \"meets the 44px touch-target floor\" — and the reason it is safe here is that a text button is WIDE: measured in a 360 dp column, `md` is 360×36 and `sm` is 360×29, so the finger has the whole line to land on and only the short axis is tight. That reasoning fails the moment the button is SQUARE, which is why `LumoIconButton` below grows its hit area and this one does not. Reach for `lg` when a text button is narrow and stands alone.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onPressed",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Called when the control is pressed. Null disables it."
        },
        {
          "name": "size",
          "type": "LumoButtonSize",
          "required": false,
          "default": "LumoButtonSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "variant",
          "type": "LumoButtonVariant",
          "required": false,
          "default": "LumoButtonVariant.solid",
          "description": "The visual variant."
        }
      ]
    },
    "LumoButtonGroup": {
      "file": "packages/mobile/lib/src/button_group.dart",
      "doc": "A row of related buttons joined into one control — `packages/ui/src/button-group.tsx`.\n\nLumoButtonGroup(label: 'عملیات سند', children: [ LumoButton(variant: LumoButtonVariant.outline, …, child: Text('رونوشت')), LumoIconButton(label: 'حذف', variant: LumoButtonVariant.outline, …), ])\n\n`label` is REQUIRED for the web's reason: an unnamed `role=\"group\"` is announced as bare \"group\". The children keep their own names, roles and states — the group is a named container with explicit child nodes above them, never a name in front of an unnamed button.\n\nTHE WHOLE TRAP IS THE CORNERS. A `LumoButton` draws its own `LumoRadius.md` on all four; joining them means squaring the corners at each SEAM and keeping the two at the group's outer ends. The web squares them with logical utilities (`rounded-s-none` / `rounded-e-none`) and its docblock records why: upstream's `rounded-l-none` rounds the WRONG corner in Persian. Here the same fact is a `BorderRadiusDirectional.horizontal(start:)` / `(end:)` on each slot's clip — which resolves to the right/left pair under fa-IR and the left/right pair under en-US, and this file never learns which. A VERTICAL group stacks along the block axis, so its ends are `BorderRadius.vertical(top:)` / `(bottom:)`: physical on purpose, because the block axis does not mirror — the same exception the web file writes down.\n\nNeighbouring `outline` children meet border to border, and that meeting IS the group's inner rule. `solid` children have no border of their own, so put a `LumoButtonGroupSeparator` between them — exactly what the web's `ButtonGroupSeparator` is for.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The children, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the group. Required.",
          "announced": true
        },
        {
          "name": "orientation",
          "type": "LumoButtonGroupOrientation",
          "required": false,
          "default": "LumoButtonGroupOrientation.horizontal",
          "description": "Which axis the control runs along."
        }
      ]
    },
    "LumoButtonGroupSeparator": {
      "file": "packages/mobile/lib/src/button_group.dart",
      "doc": "A hairline between two SOLID buttons, which have no border of their own — the web's `ButtonGroupSeparator`. `borderControl`, not `border`: it reads as part of a control's boundary (WCAG 1.4.11). Decoration: excluded from semantics.",
      "props": [
        {
          "name": "orientation",
          "type": "LumoButtonGroupOrientation",
          "required": false,
          "default": "LumoButtonGroupOrientation.horizontal",
          "description": "The enclosing group's axis; the rule is drawn across it."
        }
      ]
    },
    "LumoButtonGroupText": {
      "file": "packages/mobile/lib/src/button_group.dart",
      "doc": "Non-interactive copy inside a group — a unit, a prefix, a count. The web's `ButtonGroupText`, styled as a `Button`'s peer so the seam is invisible. The child carries its own words; there is nothing for this widget to name.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        }
      ]
    },
    "LumoCalendar": {
      "file": "packages/mobile/lib/src/calendar.dart",
      "doc": "A month grid that selects ONE day — `packages/ui/src/calendar.tsx`.\n\nAnnounced strings, all REQUIRED: `label` (the grid's name), `previousMonthLabel` / `nextMonthLabel` (the chevrons are icons) and `todayLabel` (the Today action, and the hint on today's cell). The web has no `previousMonthLabel`/`nextMonthLabel` because react-day-picker composes its nav names from `LumoStrings`; Flutter has no such table, so they are parameters again — which is the stricter of the two, not the weaker.\n\n`value` is a Gregorian `DateTime` of which only the local calendar day is read; `onChanged` receives local midnight of the chosen day. The grid is CONTROLLED (`value` + `onChanged`), as the web's `Calendar` is; the uncontrolled shape lives in `LumoDatePicker`, which owns the value.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the grid. Required.",
          "announced": true
        },
        {
          "name": "nextMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the next-month control.",
          "announced": true
        },
        {
          "name": "previousMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names of the month-paging chevrons. Required — they are icons.",
          "announced": true
        },
        {
          "name": "todayLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the Today action, and the hint on today's cell. Required.",
          "announced": true
        },
        {
          "name": "focusedMonth",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The visible month, when the caller drives it (any day inside it). Leave `null` and the grid pages itself."
        },
        {
          "name": "isDateMarked",
          "type": "bool Function(DateTime date)?",
          "required": false,
          "default": null,
          "description": "Marks days that carry something — the Khroos booking calendar's dot on a day that already has appointments. REQUIRES `markedLabel`. Not a web prop: the web draws such things with a `modifiers` class name, which a screen reader cannot hear; here the marker is announced."
        },
        {
          "name": "isDateUnavailable",
          "type": "bool Function(DateTime date)?",
          "required": false,
          "default": null,
          "description": "Marks individual days unselectable — holidays, booked days. The web's `isDateUnavailable`, receiving local midnight of the day in question."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "markedLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced on every marked day. Required as soon as `isDateMarked` is given.",
          "announced": true
        },
        {
          "name": "maxDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "Latest selectable DAY (itself selectable)."
        },
        {
          "name": "minDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "Earliest selectable DAY (itself selectable). Bounds SELECTION and paging."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<DateTime>?",
          "required": false,
          "default": null,
          "description": "Called with local midnight of the chosen day."
        },
        {
          "name": "onMonthChanged",
          "type": "ValueChanged<DateTime>?",
          "required": false,
          "default": null,
          "description": "Fires with day 1 of the month the grid moved to."
        },
        {
          "name": "selectMonthLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Name of the caption button that opens the month/year list — the web's `captionLayout: \"dropdown\"`. `null` (the default) leaves the caption a plain heading, the web's `\"label\"` layout.",
          "announced": true
        },
        {
          "name": "showTodayAction",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Whether the Today action is shown under the grid. `todayLabel` still names today's cell when it is not."
        },
        {
          "name": "today",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The day marked as today; defaults to the device clock. Injectable so a screen (or a test) is deterministic — the web REQUIRES it for SSR."
        },
        {
          "name": "value",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The selected day; only its local year/month/day are read."
        }
      ]
    },
    "LumoCalendarSheet": {
      "file": "packages/mobile/lib/src/date_field.dart",
      "doc": "The picker's body: the sheet's title and ✕, and `LumoCalendar` — month caption, previous/next chevrons with previous at the inline START, the 7-column grid whose first column is Saturday under a Jalali locale, day cells named by the FULL date, today hinted with `todayLabel`, the selected day marked, out-of-range days disabled, and the Today action.\n\nThe grid itself is NOT rewritten here: `LumoCalendar` (calendar.dart) is the one month grid in this package, and this widget is its sheet chrome. Public so an app can host it in its own route; `LumoDateField` shows it in a bottom sheet.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the close affordance. An icon is not a name.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "nextMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the next-month control.",
          "announced": true
        },
        {
          "name": "onSelected",
          "type": "ValueChanged<DateTime>",
          "required": true,
          "default": null,
          "description": "Called with local midnight of the chosen day."
        },
        {
          "name": "previousMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the previous-month control.",
          "announced": true
        },
        {
          "name": "today",
          "type": "DateTime",
          "required": true,
          "default": null,
          "description": "Which day counts as today, so a test can pin it."
        },
        {
          "name": "todayLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced marker for today, e.g. «امروز».",
          "announced": true
        },
        {
          "name": "maxDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The latest date that can be chosen."
        },
        {
          "name": "minDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The earliest date that can be chosen."
        },
        {
          "name": "value",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The current value. Supply it with `onChanged` for a controlled widget; omit both and the widget owns its own."
        }
      ]
    },
    "LumoCard": {
      "file": "packages/mobile/lib/src/card.dart",
      "doc": "A surface with a header, a body and a footer — the web `Card` with its parts. Static unless `onTap` is given; then the whole card is ONE button named by `label` (REQUIRED with `onTap` — the constructor asserts it: a tappable surface with no name announces as \"button\" and nothing else). The content stays reachable as child nodes under that name, so the reader hears the name once, \"button\", then the content. `padding` defaults to 16 all round; `LumoCardHeader`/`LumoCardFooter` only add block-axis rhythm inside it. Elevation is a shadow cast straight down (block axis) — nothing to mirror.\n\nThe press fill cross-fades over 80ms — a mobile addition (the web `Card` is not pressable and has no transition), so it obeys the platform's «reduce motion» the way the rest of the library does: the duration collapses to `Duration.zero` under `MediaQuery.disableAnimationsOf`, the spelling `disclosure.dart` uses.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "label",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced name of the card when it is a button. Required with `onTap`.",
          "announced": true
        },
        {
          "name": "onTap",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Makes the whole card a button."
        },
        {
          "name": "padding",
          "type": "EdgeInsetsGeometry",
          "required": false,
          "default": "const EdgeInsets.all(16)",
          "description": "Padding inside the surface. Directional: start and end, never left and right."
        },
        {
          "name": "variant",
          "type": "LumoCardVariant",
          "required": false,
          "default": "LumoCardVariant.outline",
          "description": "The visual variant."
        }
      ]
    },
    "LumoCardFooter": {
      "file": "packages/mobile/lib/src/card.dart",
      "doc": "Actions, usually buttons, at the inline END (`MainAxisAlignment.end` resolves against direction), above a hairline — `border-bs`, the block-start rule.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The children, in reading order."
        }
      ]
    },
    "LumoCardHeader": {
      "file": "packages/mobile/lib/src/card.dart",
      "doc": "Title (REQUIRED, a header for the reader), description, and an `action` opposite the title at the inline END — a `Row`, so it lands on the left in Persian with no positional value. `pbe-0` on the web: the header adds only bottom rhythm inside the card's own padding.",
      "props": [
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The visible title.",
          "announced": true
        },
        {
          "name": "action",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "The control that acts on the whole card, level with the title."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        }
      ]
    },
    "LumoCarousel": {
      "file": "packages/mobile/lib/src/carousel.dart",
      "doc": "A slide carousel — the web `Carousel` family (`CarouselContent`, `CarouselItem`, `CarouselPrevious`, `CarouselNext`) as ONE widget on Flutter's `PageView`. No engine is rented: embla exists on the web because the DOM has no pager; Flutter ships one.\n\n**The inline axis.** A horizontal `PageView` reads `Directionality` through `getAxisDirectionFromAxisReverseAndDirectionality`, so under RTL its axis is `AxisDirection.left`: page 0 is drawn at the RIGHT, page 1 to its LEFT, and «next» moves the deck leftward — the same fix the web makes by deriving embla's `direction` from the locale. That is why there is no `reverse:` here and no direction flag: passing `reverse` would UNDO the mirroring, which is exactly the bug this library exists to prevent. `carousel_test.dart` pins it with real positions under fa-IR and en-US.\n\n**Required names.** `label` names the region; `previousLabel`/`nextLabel` name the chevrons (a chevron is not a name); `slideLabel(index, count)` names every slide AND its dot — a function, because Persian word order is not English with the words swapped, and because the caller owns the digits: `(i, n) => 'اسلاید ${formatNumber(i + 1, locale)} از ${formatNumber(n, locale)}'`. `index` is ZERO-based.\n\n**Auto-play** is off by default and, when on, stops for good on the first interaction (a drag, a chevron, a dot) and never starts at all under `MediaQuery.disableAnimationsOf` — a slide that moves on its own is WCAG 2.2.2, and \"reduce motion\" is the platform saying no. The dot that widens when its slide arrives is animated too, and collapses to `Duration.zero` under the same flag: it was the one animation here that ignored it.\n\n**The chevrons and the dots are real buttons.** Both wore `Semantics(button: true)` over a `GestureDetector(excludeFromSemantics: true)` and neither passed `onTap` to the node — so the semantics tree carried a button with NO `SemanticsAction.tap`: a reader could find the «بعدی» chevron and could not press it, and a semantics walk looking for tap targets found the carousel empty. `onTap` now sits on the node, as `breadcrumbs.dart` already did it. Their hit areas are `LumoControl.lg` (44) bands around glyphs that paint 29 (the chevron circle) and 6x6 (a dot); the dot cell narrows below 44 only when the slides are too many to fit the row.",
      "props": [
        {
          "name": "items",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The slides, in reading order: the FIRST is at the reading start."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the whole carousel, e.g. «پیشنهادهای ویژه». REQUIRED.",
          "announced": true
        },
        {
          "name": "nextLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the «next» chevron. REQUIRED.",
          "announced": true
        },
        {
          "name": "previousLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the «previous» chevron. REQUIRED.",
          "announced": true
        },
        {
          "name": "slideLabel",
          "type": "String Function(int index, int count)",
          "required": true,
          "default": null,
          "description": "Names slide `index` of `count` — and its dot. REQUIRED; `index` is 0-based.",
          "announced": true
        },
        {
          "name": "autoPlay",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Advance on a timer. Off by default; ignored when animations are disabled."
        },
        {
          "name": "height",
          "type": "double",
          "required": false,
          "default": "200",
          "description": "The deck's block-axis extent. Flutter's pager needs a bounded one; the web gets it from the slides' own content."
        },
        {
          "name": "interval",
          "type": "Duration",
          "required": false,
          "default": "const Duration(seconds: 5)",
          "description": "Time on each slide while auto-playing."
        },
        {
          "name": "onIndexChanged",
          "type": "ValueChanged<int>?",
          "required": false,
          "default": null,
          "description": "Called with the 0-based index whenever the visible slide changes."
        },
        {
          "name": "showDots",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Show the dot row under the deck."
        }
      ]
    },
    "LumoChartPoint": {
      "file": "packages/mobile/lib/src/chart.dart",
      "doc": "",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The point's name — announced and (where there is room) drawn. REQUIRED.",
          "announced": true
        },
        {
          "name": "value",
          "type": "double",
          "required": true,
          "default": null,
          "description": "The magnitude. Geometry only: it is never shown and never announced."
        },
        {
          "name": "valueLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The figure as the app formatted it. REQUIRED — a bare number announces nothing, and `formatNumber` is the app's call, not the chart's.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Decoration beside the name on a horizontal bar (the funnel's stage icons). Excluded from semantics — an icon is not a name."
        },
        {
          "name": "partValue",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "The part of `value` drawn SOLID inside the bar, with the rest of the bar left translucent — the stacked shape the Khroos analytics screen hand-rolled as `_MiniBars` (daily views, with the leads share filled in). Geometry only, exactly like `value`: the words live in `valueLabel`."
        },
        {
          "name": "shareLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The same figure expressed as a share of the whole, ALREADY FORMATTED («۳۲٪»). Shown in a `LumoDonutChart`'s centre while this slice is selected, where the legend beside it keeps showing `valueLabel` — the Khroos `_MDonut` behaviour (count in the legend, share in the middle). Falls back to `valueLabel`.",
          "announced": true
        },
        {
          "name": "tone",
          "type": "LumoChartTone?",
          "required": false,
          "default": null,
          "description": "Overrides the tone this point would take from `lumoChartSeriesTones`."
        }
      ]
    },
    "LumoChartSeries": {
      "file": "packages/mobile/lib/src/chart.dart",
      "doc": "",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The series' name, announced as the group node above its points. REQUIRED.",
          "announced": true
        },
        {
          "name": "points",
          "type": "List<LumoChartPoint>",
          "required": true,
          "default": null,
          "description": "The points of this series, in x order."
        },
        {
          "name": "tone",
          "type": "LumoChartTone?",
          "required": false,
          "default": null,
          "description": "The semantic tone the colour carries."
        }
      ]
    },
    "LumoCheckbox": {
      "file": "packages/mobile/lib/src/checkbox.dart",
      "doc": "A checkbox — the web `Checkbox` on Flutter's semantics: named by its visible `label` or an explicit `accessibilityLabel` (a select-all cell legitimately has none), the constructor asserts one is present. The box sits at the inline START (right in Persian, by `Directionality`), the row is centre-aligned as on the web (Persian line boxes are taller). Controlled (`isSelected`) and uncontrolled (`defaultSelected`) like the web; `isIndeterminate` is the one field a switch does not have and is announced as the MIXED state. `errorMessage` is for a STANDALONE checkbox — a rule about the answer belongs on the group.\n\n**The hit area is at least [LumoControl.lg] (44) square.** The web's `checkboxVariants` is `w-fit` and unheighted; measured in a 360 dp column a box labelled «ای» was 56.5×36 around a drawn 20×20 indicator, which a pointer hits and a thumb misses. The floor is a MINIMUM, so a long label still sizes to its words and nothing drawn moves — the box stays at the inline start with the label beside it and only transparent row grows. Same rule, same numbers, as `radio_group.dart`.",
      "props": [
        {
          "name": "accessibilityLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The announced name, for when it must differ from the visible text.",
          "announced": true
        },
        {
          "name": "defaultSelected",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "The starting state for an uncontrolled checkbox."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error for this checkbox alone; supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isIndeterminate",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the box shows the mixed state — some children checked, not all."
        },
        {
          "name": "isInvalid",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Overrides the invalid state derived from `errorMessage` (or the group's)."
        },
        {
          "name": "isReadOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "isSelected",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Controlled selection; `null` leaves the state to the widget (`defaultSelected`)."
        },
        {
          "name": "label",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        }
      ]
    },
    "LumoCheckboxGroup": {
      "file": "packages/mobile/lib/src/checkbox.dart",
      "doc": "A group of checkboxes with one shared `label` (REQUIRED — the checkboxes name the options, not the question), one description and one error. Each child keeps its own `isSelected`/`onChanged`: the web's `value: string[]` exists for form submission, which Flutter has no counterpart of, so the group does not re-declare state it could not deliver. `isDisabled` and the invalid state (from `errorMessage`, or `isInvalid`) reach every child.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The children, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Supplying one marks the group invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isInvalid",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Overrides the invalid state derived from `errorMessage`."
        }
      ]
    },
    "LumoChip": {
      "file": "packages/mobile/lib/src/chip.dart",
      "doc": "A chip — a filter, a keyword, a selected value — the web `Tag`: `label` REQUIRED (a chip IS its text; the web's `children`), optionally selectable (`isSelected` + `onChanged` — a toggle button announced with its selected state; the web keeps selection for `ListBox`, mobile filter chips are the idiom) and optionally removable: `onRemove` + `removeLabel` are ONE decision — the constructor asserts `removeLabel` whenever `onRemove` is set (the web makes it a discriminated union), because an ✕ has no name of its own and a convention has already failed on this project. The ✕ sits at the inline END (left in Persian) — `EdgeInsetsDirectional`, so the trimmed cap follows it.\n\n**The touch band.** The chip PAINTS the web's box exactly — 24/28 tall by size, the ✕ glyph the web's `size-5` — but a tappable chip LAYS OUT `LumoControl.lg` (44) tall, the painted body centred in transparent space, and the ✕'s hit surface is a 44x44 band over the chip's inline end. Measured before: the ✕ node was 20x20 and a selectable chip 28 tall. Flutter hit-tests nothing outside a widget's own box (`RenderBox.hitTest` rejects a position its `size` does not contain), so unlike the web's `after:-inset-2.5` — which grows the same target to 40x40 with a pseudo-element — the band has to be real layout. Nothing painted moves; a Wrap of chips gets taller rows. The band wins the ambiguous pixel: on a chip that is BOTH selectable and removable the ✕ owns the last 44 px, as the web's overhanging pseudo-element already does. A static chip (no `onChanged`, no `onRemove`) is not a target and keeps its 24/28 box.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading icon (drawn at the inline start; decorative)."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isSelected",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether this one is selected."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Makes the chip a toggle: called with the next selected state."
        },
        {
          "name": "onRemove",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Called when the remove control is activated."
        },
        {
          "name": "removeLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced name of the remove control. REQUIRED when `onRemove` is set.",
          "announced": true
        },
        {
          "name": "size",
          "type": "LumoChipSize",
          "required": false,
          "default": "LumoChipSize.md",
          "description": "The size step, from the shared control scale."
        }
      ]
    },
    "LumoColorInput": {
      "file": "packages/mobile/lib/src/color_input.dart",
      "doc": "A colour field: a trigger showing the current colour and its NAME, opening a sheet of swatches and an optional hex box — the web `ColorInput` (`color-input.tsx`) in the shape a phone can hold.\n\nThe web puts a text field and a native `<input type=\"color\">` side by side and hangs the swatches under them. A phone has no native colour control and no room beside a field, so the picker lives in a **`showLumoSheet` route** (never Material's `showModalBottomSheet`, whose route names itself «Dialog» and its barrier «Dismiss» in English) — the same decision `LumoMultiSelect` and `LumoSelect` made.\n\n**The trigger is never a coloured square alone.** It shows the swatch AND the colour's name (or its hex), because a square is not a value: a reader who cannot see it, and a reader looking at a screen in sunlight, both need the words. The node is announced as `label` with the colour as its VALUE.\n\n**Hex is an LTR island.** `#1E90FF` is a code, and a code reads left-to-right in Persian too — the same treatment `LumoMaskInput` and `LumoPhoneInput` give their contents (the web writes it `dir=\"ltr\"` + `data-lumo-latn` on the same control). The box itself stays where the form put it.\n\nRequired announced strings: `label` (the field, and the sheet's route), `pickerLabel` (the swatch group inside the sheet — a second, different name, so nothing is announced twice). `hexLabel` and `invalidColorMessage` are ONE decision — the constructor asserts them together, because a hex box that cannot say \"that is not a colour\" is a box that silently discards what was typed. `closeLabel` names the sheet's ✕ and its scrim.\n\nWeb props not carried: `format: \"hex\" | \"hex8\"` (alpha is carried when the colour has it, so the two are not a choice the caller has to make), and `name` (nothing is posted by the platform).",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the sheet's ✕ and its scrim. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced (and, unless `showLabel` is false, displayed) name. REQUIRED.",
          "announced": true
        },
        {
          "name": "pickerLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the sheet and the swatch group inside it. REQUIRED.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "Color?",
          "required": false,
          "default": null,
          "description": "The colour, uncontrolled."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the trigger and carried in its `hint`; marks it invalid.",
          "announced": true
        },
        {
          "name": "hexLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Names the hex box. Its presence is what puts the box in the sheet.",
          "announced": true
        },
        {
          "name": "invalidColorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced when the typed text is not a colour. Required with [hexLabel]."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<Color>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "showLabel",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Whether the label is painted. It is announced either way — hiding a name is not dropping it."
        },
        {
          "name": "swatches",
          "type": "List<LumoColorSwatch>",
          "required": false,
          "default": "const []",
          "description": "The presets. May be empty when the hex box is the only way in."
        },
        {
          "name": "value",
          "type": "Color?",
          "required": false,
          "default": null,
          "description": "The colour, controlled."
        }
      ]
    },
    "LumoColorPicker": {
      "file": "packages/mobile/lib/src/color_input.dart",
      "doc": "A grid of preset colours, one of which is chosen — the web `ColorPicker` (`color-picker.tsx`), which is a `role=\"radiogroup\"` of `<input type=\"radio\">`s wearing colours, and so is this.\n\n**There is no hue wheel.** A continuous 2-D gradient is chosen by dragging a point, and a drag is a gesture a switch user, a keyboard user and a screen reader user cannot perform — the web file reaches for the platform's own `<input type=\"color\">` for exactly that reason, which a phone does not have. A finite set of NAMED swatches is a control every one of those readers can operate, and the caller who needs an arbitrary colour types it as hex ([LumoColorInput]). Taken from the same trade `forui` and Material's own pickers make on a phone: discrete, named, tappable.\n\nSelection is announced as state, never by colour alone (WCAG 1.4.1): the chosen swatch draws a tick, in black or white by the swatch's own luminance, plus a ring — and the semantics node says `checked`.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the group. REQUIRED — the swatches name the COLOURS, not the question.",
          "announced": true
        },
        {
          "name": "swatches",
          "type": "List<LumoColorSwatch>",
          "required": true,
          "default": null,
          "description": "The colours offered, in reading order."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<Color>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "value",
          "type": "Color?",
          "required": false,
          "default": null,
          "description": "The chosen colour, controlled."
        }
      ]
    },
    "LumoColorSwatch": {
      "file": "packages/mobile/lib/src/color_input.dart",
      "doc": "",
      "props": [
        {
          "name": "color",
          "type": "Color",
          "required": true,
          "default": null,
          "description": "The colour this swatch stands for."
        },
        {
          "name": "name",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The colour's own name («آبی نفتی», «قرمز خروسی»). Falls back to the hex."
        }
      ]
    },
    "LumoCombobox": {
      "file": "packages/mobile/lib/src/combobox.dart",
      "doc": "A text field that filters a list — the web `ComboBox` (Base UI's Combobox), with `autocomplete.tsx`'s Persian folding as the built-in filter. ONE widget for both web components: `Autocomplete` is the same control minus the trigger button, and a phone has no room for two shapes of the same field.\n\n**The list is INLINE, under the field — not `showLumoPopover`.** A popover is a `PopupRoute`: pushing it takes focus off the `TextField`, which ends the typing that is the whole point of a combobox, and on a phone the keyboard already covers the space an anchored surface would take. So the suggestions render in the field's own subtree while it has focus, the keyboard pushes both, and nothing is announced as a route. Consequence, stated because it is a real divergence from the web: the list takes layout space and moves what sits below it.\n\nAnnounced strings, all REQUIRED: `label` (the field's name), `suggestionsLabel` (the list's name — the web's, and Base UI names nothing), `emptyLabel` (what a reader is told when nothing matches) and `clearLabel` (an ✕ is not a name).\n\nSemantics: the field is a **`textField`**, not `SemanticsRole.comboBox` — checked against Flutter 3.35.2, where `comboBox` is one of the roles whose debug validator is still `_unimplemented` and THROWS («Missing checks for role SemanticsRole.comboBox») the moment a node carries it. The list is a named `SemanticsRole.list` and the option matching `value` is announced `selected`, which is what a reader needs from either shape.\n\nThe ✕ and the option rows are `LumoControl.lg` of HIT AREA around drawings that keep the web's scale (`IconButton size=\"sm\"`; `px-2 py-1.5` on a row). A stated mobile deviation: the web's 29-px ✕ and ~32-px row are mouse sizes. `isRequired` is the web's own prop (`ComboBoxProps.isRequired`) and reaches the reader as `SemanticsFlag.isRequired`; `errorMessage` also puts `SemanticsValidationResult.invalid` on the field, the state the web spells `aria-invalid`. There is no `isInvalid`: the web `ComboBox` has none either.\n\n`onSearch` hands the query to the caller AND turns the built-in filter off: a caller that fetches its own matches owns `options` outright, and filtering an already-filtered list a second time only hides rows.",
      "props": [
        {
          "name": "clearLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the ✕ that empties the field. REQUIRED.",
          "announced": true
        },
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What the list shows when nothing matches. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. REQUIRED — an unnamed field is a defect.",
          "announced": true
        },
        {
          "name": "options",
          "type": "List<LumoComboboxOption>",
          "required": true,
          "default": null,
          "description": "The options to choose from, in reading order."
        },
        {
          "name": "suggestionsLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the suggestion list. REQUIRED — the web's `suggestionsLabel`.",
          "announced": true
        },
        {
          "name": "allowsCustomValue",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Accept text that matches no option: submitting commits it as the value."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the field and announced. Supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "The web's `isRequired`: draws the « *» marker and sets the reader's `required` state."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String?>?",
          "required": false,
          "default": null,
          "description": "Called with the chosen id, with the typed text when `allowsCustomValue` accepts one, and with `null` when the field is cleared."
        },
        {
          "name": "onSearch",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the query on every keystroke. Providing it turns the built-in filter OFF — the caller's `options` are taken as already matched."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Placeholder text shown while the field is empty. Never a substitute for the label.",
          "announced": true
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The selected option id, or a raw string when `allowsCustomValue`. Controlled when non-null; leaving it null lets the widget keep its own choice, the way `LumoSearchField` and `LumoCheckbox` do."
        }
      ]
    },
    "LumoComboboxOption": {
      "file": "packages/mobile/lib/src/combobox.dart",
      "doc": "One suggestion of a `LumoCombobox`: `id` is what the field reports, `label` the announced and displayed text (the web's `ComboBoxItem` `id` + children).",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The stable key handed back through `onChanged`."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The announced and displayed text, and what the built-in filter matches.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoCommand": {
      "file": "packages/mobile/lib/src/command.dart",
      "doc": "The search-everything surface: a search box over grouped results, with the recent choices offered before anything is typed — the web `Command` (`command.tsx`) and the palette half of `power-search.tsx`.\n\n**On a phone it is a full-height sheet, not a floating palette.** The web centres a dialog at 20vh because a pointer and a physical keyboard are given; here the keyboard covers the bottom half of the screen the moment the field takes focus, so the surface takes the whole height and the list scrolls under a PINNED search box — [showLumoCommand] opens exactly that. The height is fixed rather than fitted to the results, because a sheet that shrinks with every keystroke moves the row a thumb was about to hit.\n\n**`emptyLabel` is announced, not just drawn.** The web relies on Base UI's `Autocomplete.Empty`, which mounts a `role=\"status\"`; here it is a live region carrying its own words, so \"nothing matched\" reaches a reader who is still in the search field. Nothing else in this file is a live region: the results are a list the reader browses.\n\n**Matching is Persian-aware.** The query and every candidate go through `lumoFoldForSearch` (`combobox.dart`, imported rather than reimplemented — the web imports `foldPersian` from `autocomplete.tsx` for the same reason), so «کيف» finds «کیف», tashkeel and ZWNJ are ignored, and «۱۲» finds «12». Labels, descriptions and `keywords` are all searched.\n\n**The rows are `LumoItem`s.** A row in a palette and a row in a settings list are the same object to a reader; two implementations is how they stop being. Each is ONE button node named by its label, with its description as a child node.\n\nWeb props not carried: `filter`/`itemToString` (items are a declared shape here, and `keywords` covers what a custom `itemToString` was for), `CommandShortcut` (a phone has no modifier keys to show), `href` rows (navigation is `onSelected` plus the app's router), and the separate `CommandDialog`/`Command`/`CommandInput`/`CommandList` parts — a phone palette is configured, not composed out of six components at every call site.\n\nIt must be given a BOUNDED height (it fills what it is given, and the list scrolls inside). [showLumoCommand] does that.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the ✕ and, through the route, its scrim. REQUIRED.",
          "announced": true
        },
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What the reader is TOLD when the query matches nothing. REQUIRED.",
          "announced": true
        },
        {
          "name": "groups",
          "type": "List<LumoCommandGroup>",
          "required": true,
          "default": null,
          "description": "The results, grouped. Groups whose items all filter out are not drawn."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the surface, and the ROUTE when it is opened as a sheet. REQUIRED.",
          "announced": true
        },
        {
          "name": "searchLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the search box (and stands in as its placeholder unless `placeholder` is given). REQUIRED.",
          "announced": true
        },
        {
          "name": "onQueryChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called as the query changes — for a palette whose results come from a server. The widget still filters what it was given."
        },
        {
          "name": "onSelected",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the chosen item's id."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the empty search box. Defaults to [searchLabel].",
          "announced": true
        },
        {
          "name": "recent",
          "type": "List<LumoCommandItem>",
          "required": false,
          "default": "const []",
          "description": "What the reader chose last time — offered while the query is empty."
        },
        {
          "name": "recentLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Names the recent group. REQUIRED when [recent] is not empty (asserted).",
          "announced": true
        }
      ]
    },
    "LumoCommandGroup": {
      "file": "packages/mobile/lib/src/command.dart",
      "doc": "",
      "props": [
        {
          "name": "items",
          "type": "List<LumoCommandItem>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The group's visible title, announced once as a header. REQUIRED.",
          "announced": true
        }
      ]
    },
    "LumoCommandItem": {
      "file": "packages/mobile/lib/src/command.dart",
      "doc": "",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A second line — where the row leads, what it will do.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading glyph. DECORATIVE: the label says what the icon says."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "keywords",
          "type": "List<String>",
          "required": false,
          "default": "const []",
          "description": "Extra words the query may match that are not shown — an English synonym for a Persian command, an old name a reader still types."
        }
      ]
    },
    "LumoContextMenu": {
      "file": "packages/mobile/lib/src/context_menu.dart",
      "doc": "A LONG-PRESS menu on a surface, anchored at the point the finger landed — `packages/ui/src/context-menu.tsx`, whose gesture is a right-click.\n\nLumoContextMenu( label: 'گزینه‌های سند', items: const [LumoMenuItem(label: 'رونوشت'), LumoMenuSeparator(), LumoMenuItem(label: 'حذف', isDestructive: true)], child: LumoCard(child: …), )\n\n`label` is REQUIRED and for a sharper reason than elsewhere: no trigger BUTTON names a long-press menu, so the name has nowhere else to come from — the web file says the same about its `aria-label`.\n\nEntries are `menu.dart`'s own — `LumoMenuItem`, `LumoMenuCheckboxItem`, `LumoMenuSeparator`, `LumoMenuSection` — and the menu is rendered by `LumoMenuTrigger` itself, not by a copy of it: the web fuses `MenuPopover` and `Menu` for the same reason, and a second menu body would be a second place for the checked state and the destructive tone to drift.\n\nHOW IT ANCHORS AT THE PRESS POINT. `showLumoPopover` positions against a `RenderBox`, so the press point is given one: a zero-size anchor placed at the recorded offset for the life of the menu. `Offset` and `Rect` have no logical form — a pointer lands at a physical point, not at a reading edge — but everything downstream is logical: the surface hangs from the anchor at `bottomStart`, which is the point's right in Persian and its left in English, and flips above when the room below is short.\n\nA context menu is a SHORTCUT surface: every action in it must also exist somewhere visible, the rule the web file states, and doubly so here — a long press is invisible. The gesture is left in the semantics tree (not excluded) so the surface carries a `longPress` action a screen reader can perform and announce.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The surface a long press opens the menu on."
        },
        {
          "name": "items",
          "type": "List<LumoMenuEntry>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the menu. Required.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onOpenChange",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Fires true when the menu opens and false when it closes, any way."
        }
      ]
    },
    "LumoCrumb": {
      "file": "packages/mobile/lib/src/breadcrumbs.dart",
      "doc": "One crumb of a trail: `label` REQUIRED (a crumb IS its text — the web's `children`), `onTap` optional. A crumb with `onTap` is a LINK; a crumb without one is a step you cannot go back to, and the LAST crumb is never a link whatever it was given, because it is the page you are already on.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "onTap",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Where this crumb goes. Flutter has no router here, so this is the seam."
        }
      ]
    },
    "LumoDateField": {
      "file": "packages/mobile/lib/src/date_field.dart",
      "doc": "A date field with a calendar behind a button — the mobile counterpart of the web's `DatePicker` (`DateInput` + `Calendar`): a read-only field showing the value through `formatLumoDate` (Jalali under `fa-*` and any `-u-ca-persian` tag, Gregorian otherwise — `calendarOf`), and a calendar button at the inline END that opens the picker as a bottom sheet route. The grid is Lumo's own: Material's `showDatePicker` is Gregorian-only and its strings default to English. Announced strings, all REQUIRED: `label`, `openLabel` (the calendar button is an icon), `closeLabel` (the sheet's ✕ and scrim), `previousMonthLabel` / `nextMonthLabel` (icon chevrons, previous at the inline START), `todayLabel` (the Today action; also the hint on today's cell). The web's `openCalendarLabel` is `openLabel` here; the web has no `previousMonthLabel`/`nextMonthLabel` because react-day-picker composes them from `LumoStrings` — Flutter has no such table, so they are props again. `value` is a Gregorian `DateTime` (Flutter's date type; the web's `CalendarDate` has no Dart twin) of which only the local calendar day is read; `onChanged` receives local midnight of the chosen day. Segment typing (`DateInput`) is not carried: a mobile keyboard's date entry is the picker.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the sheet's close button and its scrim. Required.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. Required: an unnamed field is a defect.",
          "announced": true
        },
        {
          "name": "nextMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the next-month control.",
          "announced": true
        },
        {
          "name": "openLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the button that opens the calendar. Required — it is an icon.",
          "announced": true
        },
        {
          "name": "previousMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names of the month-paging chevrons. Required — they are icons.",
          "announced": true
        },
        {
          "name": "todayLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the Today action (selects today, when in range). Required.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the field and announced. Supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether user input is required before the form is submitted."
        },
        {
          "name": "maxDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "Latest selectable DAY (itself selectable). Days after it are disabled."
        },
        {
          "name": "minDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "Earliest selectable DAY (itself selectable). Days before it are disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<DateTime>?",
          "required": false,
          "default": null,
          "description": "Called with local midnight of the chosen day."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the field when there is no value.",
          "announced": true
        },
        {
          "name": "style",
          "type": "LumoDateStyle",
          "required": false,
          "default": "LumoDateStyle.medium",
          "description": "How the value is written in the field."
        },
        {
          "name": "today",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The day marked as today; defaults to the device clock. Injectable so a screen (or a test) is deterministic — the web REQUIRES it for SSR."
        },
        {
          "name": "value",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The selected day; only its local year/month/day are read."
        }
      ]
    },
    "LumoDatePicker": {
      "file": "packages/mobile/lib/src/date_picker.dart",
      "doc": "",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the sheet's ✕ and of its scrim. Required.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. Required.",
          "announced": true
        },
        {
          "name": "nextMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the next-month control.",
          "announced": true
        },
        {
          "name": "openLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the button that opens the calendar. Required — it is an icon.",
          "announced": true
        },
        {
          "name": "previousMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names of the month-paging chevrons. Required — they are icons.",
          "announced": true
        },
        {
          "name": "selectMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the caption button that opens the month/year list. Required — the caption is a control here, not a heading.",
          "announced": true
        },
        {
          "name": "todayLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the Today action, and the hint on today's cell. Required.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The initial day, when the value is uncontrolled."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the field and announced. Supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDateMarked",
          "type": "bool Function(DateTime date)?",
          "required": false,
          "default": null,
          "description": "Marks days that carry something, with an announced dot."
        },
        {
          "name": "isDateUnavailable",
          "type": "bool Function(DateTime date)?",
          "required": false,
          "default": null,
          "description": "Marks individual days unselectable — holidays, booked days."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether user input is required before the form is submitted."
        },
        {
          "name": "markedLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What the dot means. Required as soon as `isDateMarked` is given.",
          "announced": true
        },
        {
          "name": "maxDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "Latest selectable DAY (itself selectable). Also the last month in the list."
        },
        {
          "name": "minDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "Earliest selectable DAY (itself selectable). Also the first month in the list."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<DateTime>?",
          "required": false,
          "default": null,
          "description": "Called with local midnight of the chosen day."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the field when there is no value.",
          "announced": true
        },
        {
          "name": "presets",
          "type": "List<LumoDatePreset>",
          "required": false,
          "default": "const []",
          "description": "Shortcuts shown above the grid. Empty by default."
        },
        {
          "name": "style",
          "type": "LumoDateStyle",
          "required": false,
          "default": "LumoDateStyle.medium",
          "description": "How the value is written in the field."
        },
        {
          "name": "today",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The day marked as today; defaults to the device clock. Injectable so a screen (or a test) is deterministic."
        },
        {
          "name": "value",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The selected day, when controlled."
        }
      ]
    },
    "LumoDatePreset": {
      "file": "packages/mobile/lib/src/date_picker.dart",
      "doc": "",
      "props": [
        {
          "name": "date",
          "type": "DateTime",
          "required": true,
          "default": null,
          "description": "The day this preset selects; only its local calendar day is read."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed. Required.",
          "announced": true
        }
      ]
    },
    "LumoDateRange": {
      "file": "packages/mobile/lib/src/calendar.dart",
      "doc": "",
      "props": [
        {
          "name": "from",
          "type": "DateTime",
          "required": true,
          "default": null,
          "description": "The first day of the span."
        },
        {
          "name": "to",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The last day of the span; `null` while only the first end is picked."
        }
      ]
    },
    "LumoDateRangePicker": {
      "file": "packages/mobile/lib/src/date_range_picker.dart",
      "doc": "Two dates and a range grid behind one button — `packages/ui/src/date-range-picker.tsx`, which is `date-picker.tsx`'s composition twice. The two halves are separate boxes named by `startLabel` and `endLabel` (REQUIRED), so a reader hears «تاریخ شروع» before the value rather than one field holding two dates.\n\nThe separator is an EN DASH and it is `ExcludeSemantics`d, exactly as the web hides it: an arrow would claim which date comes first ON SCREEN, and that flips with the script while the meaning does not.\n\nThe sheet closes when the span is COMPLETE. A partial span (a start with no end) is a real state — it is announced, it is painted, and `onChanged` fires with it — so a reader who taps once and then ✕ keeps what they picked. There is no Done button, and therefore no seventh announced string; the shape is forui's `FCalendar` range and Material's `DateRangePickerDialog` minus its Save/Cancel chrome, which Material only needs because it is a full-screen route.\n\nNo validation engine produces «end before start» — the tap rule makes it unreachable (a tap before the open start MOVES the start), and `errorMessage` stays the caller's own sentence.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the sheet's ✕ and of its scrim. Required.",
          "announced": true
        },
        {
          "name": "endLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the end half, e.g. «تاریخ پایان». Required.",
          "announced": true
        },
        {
          "name": "inRangeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced on every day BETWEEN the ends. Required: the band is a colour.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name of the whole range. Required.",
          "announced": true
        },
        {
          "name": "nextMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the next-month control.",
          "announced": true
        },
        {
          "name": "openLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the button that opens the calendar. Required — it is an icon.",
          "announced": true
        },
        {
          "name": "previousMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names of the month-paging chevrons. Required — they are icons.",
          "announced": true
        },
        {
          "name": "selectMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the caption button that opens the month/year list. Required.",
          "announced": true
        },
        {
          "name": "startLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the start half, e.g. «تاریخ شروع». Required — see the header.",
          "announced": true
        },
        {
          "name": "todayLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The hint on today's cell. Required.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "LumoDateRange?",
          "required": false,
          "default": null,
          "description": "The initial span, when uncontrolled."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "endPlaceholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the end half when it is empty."
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the field and announced. Supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDateMarked",
          "type": "bool Function(DateTime date)?",
          "required": false,
          "default": null,
          "description": "Marks days that carry something, with an announced dot."
        },
        {
          "name": "isDateUnavailable",
          "type": "bool Function(DateTime date)?",
          "required": false,
          "default": null,
          "description": "Returns whether a day cannot be chosen — a closed day, a taken slot."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether user input is required before the form is submitted."
        },
        {
          "name": "markedLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What the dot means. Required as soon as `isDateMarked` is given.",
          "announced": true
        },
        {
          "name": "maxDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The latest date that can be chosen."
        },
        {
          "name": "minDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The earliest date that can be chosen."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<LumoDateRange>?",
          "required": false,
          "default": null,
          "description": "Fires after every tap in the grid, with `to` absent while only the first end is picked."
        },
        {
          "name": "startPlaceholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the start half when it is empty."
        },
        {
          "name": "style",
          "type": "LumoDateStyle",
          "required": false,
          "default": "LumoDateStyle.medium",
          "description": "How each end is written in its box."
        },
        {
          "name": "today",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The day marked as today; defaults to the device clock. Injectable for determinism."
        },
        {
          "name": "value",
          "type": "LumoDateRange?",
          "required": false,
          "default": null,
          "description": "The span, when controlled."
        }
      ]
    },
    "LumoDescription": {
      "file": "packages/mobile/lib/src/description_list.dart",
      "doc": "One term/value pair — the web `DescriptionGroup` + `DescriptionTerm` + `DescriptionDetail`.\n\nThe value is a **pre-formatted String** by default, never a number: Dart cannot refuse a raw `int` at the type level the way the web's `LumoNode` does, so the type asks for the string the app already ran through `formatNumber(n, locale)` / `formatLumoDate`. Use `LumoDescription.widget` when the value is drawn (a badge, a price with a currency chip) — it takes the widget AND the announced `value`, because nothing derives words from a widget and a reader cannot hear a badge.",
      "props": [
        {
          "name": "term",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name of the fact, e.g. «جمع پرداخت». Required."
        },
        {
          "name": "value",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The fact, as words. Required — announced in both forms."
        }
      ]
    },
    "LumoDescriptionList": {
      "file": "packages/mobile/lib/src/description_list.dart",
      "doc": "A spec sheet: name/value pairs — the web `DescriptionList`. `label` is REQUIRED and names the block; the web's bare `<dl>` has no name because a page heading above it does the job, and on a phone a card full of rows has no such heading to lean on.\n\nSemantics: each pair is ONE node (`MergeSemantics`) reading the term then the value, so a reader hears «جمع پرداخت، ۱٬۲۰۰٬۰۰۰ تومان» and not two unrelated fragments. No punctuation is invented between them — the join belongs to the platform's node separator, not to a colon this library would have to pick per language.\n\nThe Khroos app hand-rolled these rows across the provider profile and the business screens as `Row(mainAxisAlignment: spaceBetween)` with two `Text`s, which reads as two separate nodes and loses the pairing entirely.",
      "props": [
        {
          "name": "entries",
          "type": "List<LumoDescription>",
          "required": true,
          "default": null,
          "description": "The term/description pairs, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the block, e.g. «جزئیات صورتحساب». Required.",
          "announced": true
        },
        {
          "name": "orientation",
          "type": "LumoDescriptionListOrientation",
          "required": false,
          "default": "LumoDescriptionListOrientation.inline",
          "description": "Which axis the control runs along."
        },
        {
          "name": "size",
          "type": "LumoDescriptionListSize",
          "required": false,
          "default": "LumoDescriptionListSize.md",
          "description": "The size step, from the shared control scale."
        }
      ]
    },
    "LumoDialogTrigger": {
      "file": "packages/mobile/lib/src/dialog.dart",
      "doc": "The declarative shape: a trigger whose press opens the dialog. `onOpenChange` fires true on open and false when it closes (any way).",
      "props": [
        {
          "name": "actions",
          "type": "List<Widget> Function(BuildContext)",
          "required": true,
          "default": null,
          "description": "The actions, in reading order."
        },
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the close affordance. An icon is not a name.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "trigger",
          "type": "Widget Function(VoidCallback? open)",
          "required": true,
          "default": null,
          "description": "Built with the press that opens the dialog."
        },
        {
          "name": "body",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "The dialog's content."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onOpenChange",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called when the surface opens or closes, with the new state."
        }
      ]
    },
    "LumoDisclosure": {
      "file": "packages/mobile/lib/src/disclosure.dart",
      "doc": "A collapsible section — the web `Disclosure` + `DisclosureTrigger` + `DisclosurePanel` as ONE widget (Flutter has no compound-children idiom that buys anything here; the parts carried no props of their own that a caller sets). Controlled with `isOpen` + `onOpenChange`, uncontrolled with `defaultOpen`, exactly as the web has both.\n\nThe chevron rotates HALF A TURN on the block axis — a rotation, not a flip, so there is nothing to mirror; it sits at the inline END (`ms-auto` on the web, a `Row` here). The panel animates open with `AnimatedSize`, and the duration collapses to zero under `MediaQuery.disableAnimationsOf` (the platform's «reduce motion» — the web's `motion-reduce:transition-none`).\n\nA closed panel is NOT BUILT: it is out of the widget tree and out of the semantics tree, which is Base UI's default too (`keepMounted` is opt-in there; on mobile there is no find-in-page and no first-byte HTML to put it in, so the flag has no mobile counterpart and is not declared).\n\nSemantics: the trigger is ONE node — a `button`, `hasExpandedState` + `isExpanded` (Flutter's `expanded:`, the counterpart of `aria-expanded`), `enabled`, named by `title`. The visible title is inside `ExcludeSemantics` so the string is announced exactly ONCE; the chevron is decoration. The panel is a plain group, NOT a `SemanticsRole.region`: Flutter's region role requires a label of its own, and the only honest label is the title — which would put the same string in the tree twice, where the web can point `aria-labelledby` at the existing trigger node and pay nothing.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The section's name — visible and announced. Required.",
          "announced": true
        },
        {
          "name": "defaultOpen",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Open state at first build (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isOpen",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Open state (controlled)."
        },
        {
          "name": "onOpenChange",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called when the surface opens or closes, with the new state."
        }
      ]
    },
    "LumoDisclosureItem": {
      "file": "packages/mobile/lib/src/disclosure.dart",
      "doc": "One section of a `LumoAccordion`: `id` is the collection key the open set is written in (a key, never a rendered string), `title` is REQUIRED — the trigger's visible text AND its announced name, in one string, so seen and announced cannot drift.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The visible title.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoDonutChart": {
      "file": "packages/mobile/lib/src/chart.dart",
      "doc": "A donut chart — the web's pie, which `chart.tsx` does not port.\n\nThe ring is painted and EXCLUDED; the legend beside it is the series, one named node per slice, and is therefore not optional: it is the only form of this chart a reader who cannot see it receives. That is the Khroos `_MDonut` shape (a ring with a tappable legend and a total in the middle) with the accessibility half no longer left to the app.\n\nThe sweep starts at twelve o'clock and runs clockwise in BOTH directions. A ring has no inline axis to mirror — the same reason the web's `CHART_PIE_SWEEP` carries no direction — and the reading order a reader is actually offered is the legend's, which does mirror.\n\nAt most five slices; a sixth is refused rather than coloured by invention. Selection is optional and CONTROLLED (`selectedIndex` + `onSelected`, the web's `onSelectDatum`): tapping a legend row selects its slice, tapping the selected row clears it, and the centre shows that slice's own figure.",
      "props": [
        {
          "name": "data",
          "type": "List<LumoChartPoint>",
          "required": true,
          "default": null,
          "description": "The slices, in reading order — at most five."
        },
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What a reader is told when there is nothing to plot. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The chart's announced name. REQUIRED.",
          "announced": true
        },
        {
          "name": "centreLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The word under the figure in the middle («سرنخ»). Drawn only.",
          "announced": true
        },
        {
          "name": "centreValueLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The figure in the middle, ALREADY FORMATTED («۱۲۴»). Replaced by the selected slice's own `valueLabel` while a slice is selected.",
          "announced": true
        },
        {
          "name": "diameter",
          "type": "double",
          "required": false,
          "default": "120",
          "description": "Outside diameter of the ring, in logical pixels."
        },
        {
          "name": "onSelected",
          "type": "ValueChanged<int?>?",
          "required": false,
          "default": null,
          "description": "Called with the tapped slice, or `null` when the selected one is tapped again. Omitting it makes the legend static rather than a set of buttons."
        },
        {
          "name": "selectedIndex",
          "type": "int?",
          "required": false,
          "default": null,
          "description": "The selected slice (controlled), or `null` for none."
        },
        {
          "name": "summary",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "One sentence about what the chart shows; announced as the chart's value."
        },
        {
          "name": "thickness",
          "type": "double",
          "required": false,
          "default": "16",
          "description": "Thickness of the ring, in logical pixels."
        }
      ]
    },
    "LumoEmptyState": {
      "file": "packages/mobile/lib/src/empty_state.dart",
      "doc": "The \"there is nothing here yet\" panel — the web `EmptyState`: icon (in the 40px chip, decorative and excluded — the title already says what the picture says), `title` REQUIRED and a HEADER for the reader, `description`, and `actions` (widgets, usually `LumoButton`s — slots, not labels plus handlers). Centred: `TextAlign.center` is direction-neutral.",
      "props": [
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What is empty, in the reader's language. Required.",
          "announced": true
        },
        {
          "name": "actions",
          "type": "List<Widget>?",
          "required": false,
          "default": null,
          "description": "The actions, in reading order."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Why it is empty and what to do about it.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Decorative illustration or glyph."
        },
        {
          "name": "size",
          "type": "LumoEmptyStateSize",
          "required": false,
          "default": "LumoEmptyStateSize.md",
          "description": "The size step, from the shared control scale."
        }
      ]
    },
    "LumoFieldState": {
      "file": "packages/mobile/lib/src/form.dart",
      "doc": "What one field hands its control. Everything the control needs and nothing it does not: no form, no state object, no way to reach a sibling.",
      "props": [
        {
          "name": "errorMessage",
          "type": "String?",
          "required": true,
          "default": null,
          "description": "The message, or `null`. **Give this to the control's `errorMessage:`.** Every Lumo field control puts `errorMessage` into its own semantics `hint`, which is what binds the message to the field; drawing it in a `Text` beside the control instead leaves it announced by nothing.",
          "announced": true
        },
        {
          "name": "focusNode",
          "type": "FocusNode",
          "required": true,
          "default": null,
          "description": "**Give this to the control's `focusNode:`.** It is how a rejected submit reaches this field; a control that never receives it can be the first invalid field and still not take focus."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The field's name for a reader — pass it straight to the control's `label:` so the form and the control cannot disagree about it.",
          "announced": true
        },
        {
          "name": "name",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The field's key in [LumoFormState.values]."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<T>",
          "required": true,
          "default": null,
          "description": "Records the new value (and revalidates, once a submit has been rejected)."
        },
        {
          "name": "value",
          "type": "T",
          "required": true,
          "default": null,
          "description": "The current value. Supply it with `onChanged` for a controlled widget; omit both and the widget owns its own."
        }
      ]
    },
    "LumoFileUpload": {
      "file": "packages/mobile/lib/src/file_upload.dart",
      "doc": "A file picker and the list of what was chosen — the web `FileUpload` plus `FileUploadList`.\n\n**There is no drop zone, and no plugin.** A phone has nothing to drag from, so the web's four drag handlers, its clipboard path and its hidden `<input type=\"file\">` have no counterpart: what is left is a NAMED GROUP (`label`), one NAMED BUTTON (`browseLabel`) and the list. And this library takes no picker dependency — `image_picker`, `file_picker` and the platform channels are the APP's choice, not a component library's, so `onBrowse` is REQUIRED and the app opens whatever picker it owns, then hands the results back as `files`. The dashed frame is kept because it is what an upload slot looks like, not because anything can be dropped on it.\n\n`onRemove` takes the INDEX (a Flutter consumer holds a `List` and calls `removeAt`); `removeLabel` builds each ✕'s name from that file's own name, as on the web — a function, because Persian word order is not English with the words swapped. The two are one decision (asserted).",
      "props": [
        {
          "name": "browseLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Visible and announced text of the button that opens the app's picker. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the upload group AND its visible title, e.g. «تصویر کارت ملی». REQUIRED.",
          "announced": true
        },
        {
          "name": "onBrowse",
          "type": "VoidCallback",
          "required": true,
          "default": null,
          "description": "Open the picker. REQUIRED — the app owns it; this library ships no plugin."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The hint under the title — accepted formats, a size limit.",
          "announced": true
        },
        {
          "name": "files",
          "type": "List<LumoAttachment>",
          "required": false,
          "default": "const <LumoAttachment>[]",
          "description": "What has been chosen so far."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "maxFilesLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A PRE-FORMATTED count line, e.g. «حداکثر ۵ پرونده».",
          "announced": true
        },
        {
          "name": "onRemove",
          "type": "void Function(int index)?",
          "required": false,
          "default": null,
          "description": "Drop the file at this index."
        },
        {
          "name": "removeLabel",
          "type": "String Function(String name)?",
          "required": false,
          "default": null,
          "description": "Builds each ✕'s announced name from that file's name. REQUIRED with `onRemove`.",
          "announced": true
        }
      ]
    },
    "LumoFilterGroup": {
      "file": "packages/mobile/lib/src/filters.dart",
      "doc": "",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The group's name: a header in the sheet, announced there once.",
          "announced": true
        },
        {
          "name": "options",
          "type": "List<LumoFilterOption>",
          "required": true,
          "default": null,
          "description": "The options to choose from, in reading order."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Help text under the group's header in the sheet.",
          "announced": true
        },
        {
          "name": "isMultiple",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "`false` makes the group exclusive — a sort order, where choosing one option drops the last."
        }
      ]
    },
    "LumoFilterOption": {
      "file": "packages/mobile/lib/src/filters.dart",
      "doc": "",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "isPinned",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Keep this option on the BAR even while it is off — a quick filter. The Khroos results screen (`discovery_screens.dart`) puts «۴+ امتیاز», «نزدیک‌ترین» and «پاسخ سریع» in the row above the results and leaves them visible whether or not they are on, because a filter a reader cannot see is a filter a reader will not use. Unpinned options reach the bar only once they are chosen."
        }
      ]
    },
    "LumoFilters": {
      "file": "packages/mobile/lib/src/filters.dart",
      "doc": "The active filters, as a bar over a result list, plus the sheet that edits them — the web `Filters` (`filters.tsx`) in the shape a phone has room for.\n\n**What the web has that this does not, and why.** `filters.tsx` builds a clause model: field, operator, value — «قیمت» «بیشتر از» «۵۰۰٬۰۰۰», rows of three `Select`s and a text box. That is a desktop query builder: four controls per clause across a row 640 pixels wide, on a screen that is 360. So the model here is CHOICES rather than clauses — a group, its options, and which are on — which is what a phone filter sheet has always actually been, and precisely what the Khroos app hand-rolled (`inquiry_screens.dart`'s `FiltersBody`: two switches with helper text and a `Wrap` of selectable chips over a sort order). The operator axis is deliberately gone, not forgotten; an app that needs it wants a screen, not a sheet.\n\n**The bar's chips are TOGGLES, not removable tags.** A selectable `LumoChip` announces its own selected state, is one tap in both directions, and gives a thumb the whole chip instead of a 20-pixel ✕ — so the bar needs no `removeLabel` and never grows an unnamed glyph. Pinned options stay on the bar while off (see [LumoFilterOption.isPinned]); everything else appears there once chosen.\n\n**Announced strings, all REQUIRED**: `label` (names the bar as a region), `editLabel` (the control that opens the sheet), `closeLabel` (the sheet's ✕ and its scrim), `clearAllLabel`, `applyLabel` (the sheet's primary, which CLOSES — choices apply as they are made, so confirming is a dismissal, not a commit, exactly as in `LumoMultiSelect`), and `countLabel`, a FUNCTION of the already-formatted count, because «۳ فیلتر» is not English with the words swapped. The count goes through `formatNumber`; a bare number never reaches the screen, and the count is one node with its own words rather than a digit glued to a button.\n\n`extra` is the slot the app cannot do without: the Khroos filter sheet also holds a radius `LumoSlider` and switch rows with helper text — controls that are not a set of options and never will be. Whatever the builder returns is drawn under the groups, inside the same sheet.\n\nSelection is CONTROLLED (`values` + `onChanged`); the widget mirrors it optimistically so the sheet and the bar answer a tap before the caller's `setState` lands — the sheet is a ROUTE built above this widget and cannot see a `widget` field change.",
      "props": [
        {
          "name": "applyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the sheet's primary, which closes it. REQUIRED.",
          "announced": true
        },
        {
          "name": "clearAllLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the clear-everything control, on the bar and in the sheet. REQUIRED.",
          "announced": true
        },
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the sheet's ✕ and its scrim. REQUIRED.",
          "announced": true
        },
        {
          "name": "countLabel",
          "type": "String Function(String count)",
          "required": true,
          "default": null,
          "description": "Builds the count sentence from the ALREADY FORMATTED number of active filters («۳»). REQUIRED — a bare number announces nothing.",
          "announced": true
        },
        {
          "name": "editLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the control that opens the sheet. REQUIRED.",
          "announced": true
        },
        {
          "name": "groups",
          "type": "List<LumoFilterGroup>",
          "required": true,
          "default": null,
          "description": "The filter groups, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the bar as a region. REQUIRED — announced, not drawn.",
          "announced": true
        },
        {
          "name": "values",
          "type": "Map<String, List<String>>",
          "required": true,
          "default": null,
          "description": "The chosen option ids per group id. Controlled, as the web's `value`."
        },
        {
          "name": "extra",
          "type": "WidgetBuilder?",
          "required": false,
          "default": null,
          "description": "Extra controls for the sheet: a radius slider, a switch with helper text."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<Map<String, List<String>>>?",
          "required": false,
          "default": null,
          "description": "Called with the whole map after every change."
        }
      ]
    },
    "LumoForm": {
      "file": "packages/mobile/lib/src/form.dart",
      "doc": "A form: the fields, and the ONE thing a phone form must not get wrong — what happens when a submit is rejected.\n\nMirrors `packages/ui/src/form.tsx` + `form-state.tsx`, and carries across exactly the parts that are contract rather than plumbing. The web's `Field` mints ids and wires `aria-describedby`/`aria-invalid` into the first byte; **Flutter has no `aria-describedby`**, so the binding is different in mechanism and identical in effect: a field's error goes into that field's OWN semantics `hint`, which is what `text_field.dart` already does with its `errorMessage:` — so [LumoFormField] hands the message to the control and the control carries it. A message drawn beside a control, in a `Text` of its own, is announced by nothing when the reader is on the field; that is the defect the web file's whole wiring layer exists to prevent, and it is the same defect here.\n\n`errorSummaryLabel` is REQUIRED and is a FUNCTION of the already-formatted count, because «۳ فیلد را کامل کنید» is not English with the words swapped — the same shape as `LumoMultiSelect.countLabel`. The count goes through `formatNumber`, so it is «۳» under `fa-IR` and «3» under `en-US`; a bare number never reaches the screen.\n\nThe summary is a `LumoAlert(isLive: true)`: it is INSERTED by the rejected submit, which is precisely the case `alert.dart` says the live flag is for.\n\nWhat the web has and this does not: `validationBehavior: \"aria\" | \"native\"` (there is no browser constraint validation to switch off — no engine here authors a message in its own language, which is the whole reason that prop exists), async and Standard Schema validators (see [LumoFieldValidator]), and `name`/hidden inputs (nothing is posted by the platform; the app reads [LumoFormState.values]).",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The fields and everything between them, in reading order — which is also the order a rejected submit walks to find the first invalid field."
        },
        {
          "name": "errorSummaryLabel",
          "type": "String Function(String count)",
          "required": true,
          "default": null,
          "description": "Builds the announced summary from the ALREADY FORMATTED number of fields that failed («۳»). REQUIRED — a bare count announces nothing, and a default would be English.",
          "announced": true
        },
        {
          "name": "state",
          "type": "LumoFormState",
          "required": true,
          "default": null,
          "description": "The state this form renders. The app owns and disposes it."
        },
        {
          "name": "gap",
          "type": "LumoGap",
          "required": false,
          "default": "LumoGap.md",
          "description": "The step between the children, from the shared scale."
        }
      ]
    },
    "LumoFormField": {
      "file": "packages/mobile/lib/src/form.dart",
      "doc": "One field of a [LumoForm]: a name, a label, an initial value, the rules, and a builder that puts them on a control.\n\nA BUILDER rather than a fixed control, because the fields of this library are a dozen different widgets (`LumoTextField`, `LumoSelect`, `LumoDateField`, `LumoMultiSelect`, `LumoPhoneInput`…) and every one of them already takes `label`, `errorMessage` and — where it is a single focusable control — `focusNode`. The field owns the state; the control stays the control it already was:\n\nLumoFormField<String>( name: 'mobile', label: 'شمارهٔ همراه', initialValue: '', validators: [(v) => v.isEmpty ? 'شمارهٔ همراه را بنویسید.' : null], builder: (context, field) => LumoTextField( label: field.label, errorMessage: field.errorMessage,   // ← the binding focusNode: field.focusNode,         // ← the focus target onChanged: field.onChanged, ), )",
      "props": [
        {
          "name": "builder",
          "type": "Widget Function(BuildContext context, LumoFieldState<T> field)",
          "required": true,
          "default": null,
          "description": "Builds the control for this field from its current state."
        },
        {
          "name": "initialValue",
          "type": "T",
          "required": true,
          "default": null,
          "description": "The value the field starts at."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The field's name for a reader, handed on through [LumoFieldState.label]. REQUIRED: a field the form knows only by `name` cannot be named to anyone.",
          "announced": true
        },
        {
          "name": "name",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The field's key. Unique within one form."
        },
        {
          "name": "validators",
          "type": "List<LumoFieldValidator<T>>",
          "required": false,
          "default": "const []",
          "description": "The rules, in order. The FIRST message wins — put the \"is it there at all\" rule first, exactly as the web's `lumoValidators().all(…)` says to."
        }
      ]
    },
    "LumoGrid": {
      "file": "packages/mobile/lib/src/layout.dart",
      "doc": "A fixed-column grid — the web `Grid` (`stack.tsx`), with the auto-fill preset dropped: on a phone the column count is a design decision (two tiles, three tiles), not something to derive from a width that is always about 360.\n\nTracks run along the INLINE axis, so cell 1 is the reader's first cell in both scripts — the `Row` mirrors itself and no cell is placed by a physical offset. The last row is padded with empty cells so the columns stay put (a three-column grid with four tiles keeps its third column empty, it does not stretch the fourth tile across the row).\n\nIt lays out in ONE pass and scrolls with its parent, unlike `GridView`, which brings its own viewport — a grid of four tiles inside a settings screen is a layout, not a scrollable.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The children, in reading order."
        },
        {
          "name": "columns",
          "type": "int",
          "required": true,
          "default": null,
          "description": "How many cells fit on one line."
        },
        {
          "name": "align",
          "type": "LumoAlign",
          "required": false,
          "default": "LumoAlign.stretch",
          "description": "How the cells of one line line up against each other. `stretch` (the default) gives every cell of a row the height of the tallest — cards in a grid whose bottoms do not line up look broken — at the cost of an `IntrinsicHeight` pass per row."
        },
        {
          "name": "gap",
          "type": "LumoGap",
          "required": false,
          "default": "LumoGap.md",
          "description": "Space between the children."
        }
      ]
    },
    "LumoIconButton": {
      "file": "packages/mobile/lib/src/button.dart",
      "doc": "A button whose only content is an icon: `label` is REQUIRED — an icon is not a name. Announced through Semantics and shown as a tooltip.\n\n**The hit area is at least 48 square — Android's minimum, which is above iOS's 44 — while the DRAWN button keeps its size step.** An icon button is the one place the shared control scale is small on BOTH axes — measured at `sm` it was 29×29 and at `md` 36×36, so nothing rescues the miss the way a full-width text button's length does. The web's `size: \"icon\"` is `h-control-md w-control-md`, i.e. exactly the 36×36 square, and a pointer hits it fine; a thumb does not. So the painted box is unchanged (no visual drift — the fill, border and radius are still 29 or 36) and a transparent, semantics-free MARGIN around it forwards its taps to the same callback. Verified by test: a tap on the GLYPH resolves once, in the button (which keeps its pressed fill); a tap in the MARGIN resolves once, in the margin; a disabled button answers neither.\n\nThe node is DECLARED here and the drawn button is `ExcludeSemantics`'d — the same shape `toggle.dart`, `link.dart` and `item.dart` use. Merging the Material button's own node instead left its 36×36 node alive underneath the 44×44 one: harmless to a reader (it is `isMergedIntoParent` and never reaches the platform) but indistinguishable from a real undersized target to anything that walks the tree, `test/tap_target_floor_test.dart` included. One control, one node, one size.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onPressed",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Called when the control is pressed. Null disables it."
        },
        {
          "name": "size",
          "type": "LumoButtonSize",
          "required": false,
          "default": "LumoButtonSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "variant",
          "type": "LumoButtonVariant",
          "required": false,
          "default": "LumoButtonVariant.ghost",
          "description": "The visual variant."
        }
      ]
    },
    "LumoIconStack": {
      "file": "packages/mobile/lib/src/icon_tile.dart",
      "doc": "Overlapping avatars or icons with a «+۲» for the rest — the web `IconStack`.\n\nONE fact, ONE name: the whole stack is a single named image node and the members carry no names of their own, because \"سارا، رضا، مینا، و ۲ نفر دیگر\" read out one chip at a time is noise, not information. `label` is REQUIRED for that reason (the web requires it too); `overflowLabel(remaining)` is optional and, when given, is appended to that one name — it is not a second node.\n\nThe overlap is `-ms-2` on the web: a LOGICAL inline offset, so the deck leans the reader's way. Here it is `PositionedDirectional`, which mirrors the same way — under fa-IR the FIRST member is on the right and each next one tucks under it to the left. The «+N» goes through `formatNumber` with the scope's locale («+۲», never «+2»); the web takes the locale as a prop, the mobile scope already has it.",
      "props": [
        {
          "name": "items",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The members, in reading order. Each is clipped to the step's circle."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What the stack MEANS, e.g. «۵ عضو». REQUIRED — the members are silent beneath it.",
          "announced": true
        },
        {
          "name": "max",
          "type": "int",
          "required": false,
          "default": "4",
          "description": "How many to show before the rest collapse into a count."
        },
        {
          "name": "overflowLabel",
          "type": "String Function(int remaining)?",
          "required": false,
          "default": null,
          "description": "Says the remainder in words, e.g. `(n) => '$n نفر دیگر'`. Appended to `label`.",
          "announced": true
        },
        {
          "name": "size",
          "type": "LumoIconStackSize",
          "required": false,
          "default": "LumoIconStackSize.md",
          "description": "The size step, from the shared control scale."
        }
      ]
    },
    "LumoIconTile": {
      "file": "packages/mobile/lib/src/icon_tile.dart",
      "doc": "An icon in a tinted square — the thing at the top of a feature card, or beside a list row. The web `IconTile`.\n\n**DECORATIVE BY DEFAULT.** `accessibilityLabel` is null unless the tile is the SOLE carrier of its meaning, and when it is null the tile is removed from the semantics tree entirely (`ExcludeSemantics` — the web's `aria-hidden=\"true\"`, not a nameless node). A tile sitting beside its own label — the icon next to «احراز هویت» in a settings row, next to a card's heading — IS decorative: naming it makes a reader hear the same fact twice, and «icon» is not a fact at all. Name it only when there is no text.\n\nThe icon is a Widget, sized by the tile to HALF its edge (the web's `[&>svg]:size-1/2`) through `IconTheme`, and coloured by the tone pair; a widget that is not an `Icon` should read `IconTheme.of(context)` itself.",
      "props": [
        {
          "name": "icon",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The glyph. Sized and coloured by the tile."
        },
        {
          "name": "accessibilityLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A name, ONLY when the tile is the sole carrier of meaning. Null = decorative.",
          "announced": true
        },
        {
          "name": "shape",
          "type": "LumoIconTileShape",
          "required": false,
          "default": "LumoIconTileShape.rounded",
          "description": "The shape variant."
        },
        {
          "name": "size",
          "type": "LumoIconTileSize",
          "required": false,
          "default": "LumoIconTileSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "tone",
          "type": "LumoIconTileTone",
          "required": false,
          "default": "LumoIconTileTone.neutral",
          "description": "The semantic tone the colour carries."
        },
        {
          "name": "variant",
          "type": "LumoIconTileVariant",
          "required": false,
          "default": "LumoIconTileVariant.subtle",
          "description": "The visual variant."
        }
      ]
    },
    "LumoInfiniteList": {
      "file": "packages/mobile/lib/src/virtual_list.dart",
      "doc": "A [LumoVirtualList] that asks for another page when the reader nears the end — the web's `onEndReached`/`endReachedThreshold`/`asyncStatus` trio, which every real feed needs and no Flutter list ships.\n\n[loadingLabel] is REQUIRED and is both drawn beside the spinner and announced from a live region while [isLoadingMore] is true, because \"more is coming\" is the one thing a reader cannot see from a list that has stopped growing.\n\n[onEndReached] fires AT MOST ONCE per [itemCount]: the guard is the count itself, exactly as `virtual-list.tsx` does it with `requestedAtCount`. Any other guard (a bool, a timer) either fires twice for one page or stops firing when a page comes back empty.",
      "props": [
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Shown and announced when nothing has loaded. REQUIRED.",
          "announced": true
        },
        {
          "name": "itemBuilder",
          "type": "Widget Function(BuildContext context, int index)",
          "required": true,
          "default": null,
          "description": "Builds the row at `index`."
        },
        {
          "name": "itemCount",
          "type": "int",
          "required": true,
          "default": null,
          "description": "Rows loaded SO FAR. The \"loading more\" row is not one of them."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the list. REQUIRED.",
          "announced": true
        },
        {
          "name": "loadingLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced (live) and drawn while another page is on the way. REQUIRED.",
          "announced": true
        },
        {
          "name": "controller",
          "type": "ScrollController?",
          "required": false,
          "default": null,
          "description": "An optional controller: supply one to read or drive the value from outside, or omit it and the widget owns its own."
        },
        {
          "name": "endReachedThreshold",
          "type": "int",
          "required": false,
          "default": "3",
          "description": "How many rows before the end count as \"near the end\"."
        },
        {
          "name": "findItemIndex",
          "type": "int? Function(Object key)?",
          "required": false,
          "default": null,
          "description": "Finds where a key sits now, so the list can hold its place when rows are inserted above it."
        },
        {
          "name": "isLoadingMore",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "The caller's answer: a page is in flight. Drives the footer and the live announcement."
        },
        {
          "name": "itemKey",
          "type": "Object Function(int index)?",
          "required": false,
          "default": null,
          "description": "A stable key for the row at `index` — what `findItemIndex` looks up."
        },
        {
          "name": "keepAlive",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether rows scrolled out of view keep their state instead of being rebuilt."
        },
        {
          "name": "onEndReached",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Asked for another page. Fires once per [itemCount]."
        },
        {
          "name": "padding",
          "type": "EdgeInsetsGeometry?",
          "required": false,
          "default": null,
          "description": "Padding inside the surface. Directional: start and end, never left and right."
        },
        {
          "name": "physics",
          "type": "ScrollPhysics?",
          "required": false,
          "default": null,
          "description": "Scroll physics, for a caller that needs something other than the platform default."
        },
        {
          "name": "scrollDirection",
          "type": "Axis",
          "required": false,
          "default": "Axis.vertical",
          "description": "Which axis the list scrolls along."
        },
        {
          "name": "separatorBuilder",
          "type": "Widget Function(BuildContext context, int index)?",
          "required": false,
          "default": null,
          "description": "Builds the separator between two rows. Omit it for none."
        }
      ]
    },
    "LumoInputGroup": {
      "file": "packages/mobile/lib/src/input_group.dart",
      "doc": "A text field with ADDONS attached at the reading edges of the box — `packages/ui/src/input-group.tsx`.\n\nLumoInputGroup(label: 'مبلغ', isNumeric: true, trailing: Text('تومان')) LumoInputGroup(label: 'نشانی صفحه', trailing: LumoInputGroupButton(label: 'رونوشت نشانی', …))\n\nCOMPOSED, not compositional: `label` is REQUIRED and is the field's name, the same decision `text_field.dart` and the web's `InputGroup` take against the shadcn vendors' unlabelled input.\n\nThe web pins its adornments as absolute overlays inside the input's padding; a phone has less room, so here they are ATTACHED boxes sharing the field's border — the shape Material's `TextField` prefix and forui's `FTextField.prefixBuilder` draw, and what the «تومان» suffix of a Persian amount field actually wants.\n\nTHE CORNER TRAP IS THE SAME ONE AS `button_group.dart`. An addon at the reading START must round the two corners on the START side and leave the pair at the seam square — `BorderRadiusDirectional.horizontal(start:)`, which is the RIGHT pair under fa-IR and the LEFT pair under en-US. The divider between an addon and the input is a `BorderDirectional`, so it too lands on the seam and not on a named edge.\n\nSemantics: ONE text-field node named by `label`, with `description` and `errorMessage` as its hint; the visible copies of both are excluded so a reader hears each once (the rule `text_field.dart` records). An addon is rendered as given: a decorative glyph is the caller's to wrap in `ExcludeSemantics`, and a control must carry its own name — the same contract as `LumoTextField.suffix`.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. Required: an unnamed field is a defect.",
          "announced": true
        },
        {
          "name": "autofillHints",
          "type": "Iterable<String>?",
          "required": false,
          "default": null,
          "description": "Autofill hints for the platform's keyboard and password manager."
        },
        {
          "name": "autofocus",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control takes focus when it first appears."
        },
        {
          "name": "controller",
          "type": "TextEditingController?",
          "required": false,
          "default": null,
          "description": "An optional controller: supply one to read or drive the value from outside, or omit it and the widget owns its own."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Help text under the box; also the node's hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Supplying one marks the field invalid, as on the web.",
          "announced": true
        },
        {
          "name": "focusNode",
          "type": "FocusNode?",
          "required": false,
          "default": null,
          "description": "An optional focus node, for callers that manage focus themselves."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isInvalid",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Overrides the invalid state derived from [errorMessage]."
        },
        {
          "name": "isNumeric",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "The value is digits (an amount, a code): laid out left-to-right inside an RTL form and given a numeric keyboard. A data-type fact, not a direction flag — the same parameter `LumoTextField` carries, for the same reason."
        },
        {
          "name": "isReadOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether user input is required before the form is submitted."
        },
        {
          "name": "keyboardType",
          "type": "TextInputType?",
          "required": false,
          "default": null,
          "description": "Which keyboard the platform should offer."
        },
        {
          "name": "leading",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "The addon at the reading START — a unit, a prefix, a glyph."
        },
        {
          "name": "obscureText",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the text is masked, as for a password."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "onSubmitted",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called when the reader submits from the keyboard."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Placeholder text shown while the field is empty. Never a substitute for the label.",
          "announced": true
        },
        {
          "name": "showLabel",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "The label is always the field's NAME; `false` hides it visually and keeps it announced."
        },
        {
          "name": "size",
          "type": "LumoInputGroupSize",
          "required": false,
          "default": "LumoInputGroupSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "textInputAction",
          "type": "TextInputAction?",
          "required": false,
          "default": null,
          "description": "What the keyboard's action key does."
        },
        {
          "name": "trailing",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "The addon at the reading END — a unit, or a `LumoInputGroupButton`."
        }
      ]
    },
    "LumoInputGroupButton": {
      "file": "packages/mobile/lib/src/input_group.dart",
      "doc": "An icon-only control inside an addon slot — the web's `InputGroupButton`. `label` stays REQUIRED (an icon is not a name); ghost and small by default, because the group already draws the box.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onPressed",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Called when the control is pressed. Null disables it."
        },
        {
          "name": "variant",
          "type": "LumoButtonVariant",
          "required": false,
          "default": "LumoButtonVariant.ghost",
          "description": "The visual variant."
        }
      ]
    },
    "LumoItem": {
      "file": "packages/mobile/lib/src/item.dart",
      "doc": "The generic list row: leading media, title and description, trailing control — the web `Item` with `ItemMedia`/`ItemContent`/`ItemTitle`/ `ItemDescription`/`ItemActions` folded into ONE widget, because on a phone a row is not composed per call site a hundred times, it is configured.\n\n`title` is REQUIRED: it is the row's visible first line AND, when the row is tappable, its announced name. The leading slot sits at the inline START and the trailing slot at the inline END — a `Row`, which mirrors itself, so nothing here is positioned by a physical offset. With `onTap` and no `trailing`, the row draws the \"go\" chevron itself: `Icons.chevron_right` carries `matchTextDirection`, so the glyph points at the reading END (left under fa-IR) with no direction flag. The Khroos app hand-rolled this as `KRow` and hard-coded `chevron-left`, which points the wrong way the moment the app is read in English.\n\nSemantics: the row is ONE node named by `title` — a `button` when `onTap` is given, plain text otherwise. `explicitChildNodes` keeps a nested NAMED control (a `LumoIconButton` in `trailing`, a `LumoSwitch`) reachable as its own node UNDER the row, exactly as `LumoCard` does; the visible title is excluded so the name is heard ONCE, while the description stays a child node and is read after it. `leading` is decoration and is excluded — an icon is not a name.\n\n`isSelected` is `bool?` on purpose, mirroring `list-box.tsx`, which omits `aria-selected` entirely when nothing is selectable: `null` (the default) means \"this row is not part of a selection\" and announces no state; `true` and `false` both announce one. `LumoListBox` passes it; a standalone row leaves it alone.",
      "props": [
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The row's first line: shown, and announced as the row's name. Required.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The second line — the web `ItemDescription`. Announced after the name.",
          "announced": true
        },
        {
          "name": "hasDivider",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "A hairline at the block end — the web `ItemSeparator`, for a run of rows inside one card. Refused on `outlined`, which already has a frame."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isSelected",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "`null` = not part of a selection (no state announced). See the docblock."
        },
        {
          "name": "leading",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Decoration at the inline start (an icon, an avatar). Excluded from semantics."
        },
        {
          "name": "onTap",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Makes the whole row ONE button named by `title`."
        },
        {
          "name": "size",
          "type": "LumoItemSize",
          "required": false,
          "default": "LumoItemSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "trailing",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "The inline-end slot: a control (keeps its own node) or a glyph. With `onTap` and no `trailing`, the row draws a direction-matching chevron."
        },
        {
          "name": "variant",
          "type": "LumoItemVariant",
          "required": false,
          "default": "LumoItemVariant.plain",
          "description": "The visual variant."
        }
      ]
    },
    "LumoItemGroup": {
      "file": "packages/mobile/lib/src/item.dart",
      "doc": "A titled run of rows — the web `ItemGroup` plus a name. The web's group is deliberately role-less and nameless (\"a page that wants list semantics owns a `<ul>`\"); on a phone there is no surrounding page markup to own it, so `label` is REQUIRED and is DRAWN as a section header (the Settings-screen idiom the Khroos app hand-rolled as `_Lbl`). It is announced ONCE, as a header the reader lands on before the rows — not also as a container name.\n\n`hasDividers` puts a hairline between the rows (never after the last) and closes the gap, which is how a run of rows inside one card reads; without it the group keeps the web's `gap-2`.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The children, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The section's name — drawn as a header and announced. Required.",
          "announced": true
        },
        {
          "name": "hasDividers",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "A hairline between rows instead of a gap."
        }
      ]
    },
    "LumoKanban": {
      "file": "packages/mobile/lib/src/kanban.dart",
      "doc": "A board of columns whose cards move between them — the web `Kanban`.\n\n**The Khroos board is the reference.** `jobs_board_screen.dart` is a real four-column job board (در انتظار / در حال انجام / تکمیل‌شده / پرداخت‌شده) and it does NOT draw four columns: a phone is 390 px wide, so the app hand-rolled a column SWITCHER and showed one column at a time, plus a per-card \"advance to the next status\" button. Two things follow, and both are in this API:\n\n1. The board scrolls horizontally and a column is [columnWidth] wide, but never wider than the viewport less [peek] — so the next column always shows an edge. A board that fills the screen exactly looks like a list and nobody scrolls it; the peek is the affordance. 2. The Khroos advance button is a MOVE, and every card here has that move as a named semantic action — one per other column, named by [moveToColumnLabel]. Same reasoning as `LumoSortable`: a drag is not available to a screen-reader user, so a board that only drags is a board they cannot use. `kanban.tsx` reaches the same place through the keyboard; a phone has no keyboard, so the action IS the route.\n\n**The drag** is a LONG PRESS then a move (see `_Card`: a plain pan loses the gesture arena to the board's own horizontal scroller), and it hit-tests LIVE rects — nearest column by inline distance, then the card whose midpoint was crossed — rather than reasoning about index order. `kanban.tsx` records why in one line: rects need no `isRtl` branch, and dnd-kit's signed deltas do. Nothing in this file asks the direction a question; `PositionedDirectional`, `EdgeInsetsDirectional` and the `Row`'s own mirroring put the first column at the reading start on their own.\n\n**Not carried from the web:** moving a COLUMN (the web's `onColumnsChange` accepts a reordered column list; reordering columns by finger on a phone fights the board's own horizontal scroll), and the announcement strings `pickedUp`/`dropped`/`cancelled` — those narrate a keyboard pick-up-and-move model that does not exist here, where a move is one atomic named action.",
      "props": [
        {
          "name": "columns",
          "type": "List<LumoKanbanColumn>",
          "required": true,
          "default": null,
          "description": "The columns, in reading order: the first sits at the reading START — right under fa-IR."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the board, e.g. «وضعیت کارها». REQUIRED.",
          "announced": true
        },
        {
          "name": "moveToColumnLabel",
          "type": "String Function(String columnLabel)",
          "required": true,
          "default": null,
          "description": "Names the \"move this card to that column\" action for a destination column's name, e.g. `(c) => 'انتقال به «$c»'`. REQUIRED, and a FUNCTION rather than a two-part template, because Persian word order is not English with the words swapped.",
          "announced": true
        },
        {
          "name": "onCardMoved",
          "type": "void Function(String cardId, String toColumnId, int toIndex)",
          "required": true,
          "default": null,
          "description": "Called with the card, the column it lands in, and its index there. Both routes — the named action and the drag — come through here."
        },
        {
          "name": "columnWidth",
          "type": "double",
          "required": false,
          "default": "264",
          "description": "A column's width, capped at the viewport less [peek]."
        },
        {
          "name": "controller",
          "type": "ScrollController?",
          "required": false,
          "default": null,
          "description": "An optional controller: supply one to read or drive the value from outside, or omit it and the widget owns its own."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "peek",
          "type": "double",
          "required": false,
          "default": "32",
          "description": "How much of the next column stays visible. See the docblock."
        }
      ]
    },
    "LumoKanbanCard": {
      "file": "packages/mobile/lib/src/kanban.dart",
      "doc": "One card on a [LumoKanban].",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Stable identity across moves. Not announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The card's announced name, and — unless [child] is given — its drawn first line, e.g. «تعمیر پکیج — نشتی مبدل». REQUIRED.",
          "announced": true
        },
        {
          "name": "child",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Replaces the drawn `label`/`description` block — the Khroos job card's pills, its «هزینه و سود» panel and its advance button all live here. The name is still announced from the card's own node, so a `child` that repeats [label] is announced twice."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A second drawn line, e.g. «خانم موسوی · سعادت‌آباد». Read after the name.",
          "announced": true
        }
      ]
    },
    "LumoKanbanColumn": {
      "file": "packages/mobile/lib/src/kanban.dart",
      "doc": "One column of a [LumoKanban]: a name, and the cards in it. The count is NOT a parameter — it is `cards.length` through `formatNumber(n, locale)`, so it cannot drift from the cards and cannot arrive in Latin digits.",
      "props": [
        {
          "name": "cards",
          "type": "List<LumoKanbanCard>",
          "required": true,
          "default": null,
          "description": "The cards in this column, top to bottom."
        },
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Stable identity. Not announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The column's announced and drawn name, e.g. «در حال انجام». REQUIRED.",
          "announced": true
        },
        {
          "name": "emptyLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the column when it holds nothing, e.g. «کاری در «در انتظار» نیست». Optional; per column, because the sentence names the column.",
          "announced": true
        }
      ]
    },
    "LumoLineChart": {
      "file": "packages/mobile/lib/src/chart.dart",
      "doc": "A line chart — the web's `lineY`/`areaY` marks.\n\nThe polyline is the one mark widgets cannot draw, so it is painted; ON TOP of it sits a `Row` of one transparent, named slot per point, which is what a reader actually walks and what a touch-explore lands on. The `Row` mirrors itself and the painter is handed the same `TextDirection`, so the drawn line and the announced order agree in both directions: under RTL the first point is at the RIGHT.\n\nUp to five series (`lumoChartSeriesTones`); a sixth is refused at construction rather than given a colour the theme does not own.",
      "props": [
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What a reader is told when there is nothing to plot. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The chart's announced name. REQUIRED.",
          "announced": true
        },
        {
          "name": "series",
          "type": "List<LumoChartSeries>",
          "required": true,
          "default": null,
          "description": "The lines. At most five — see the assert."
        },
        {
          "name": "isArea",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Fill the area under each line (the web's `areaY`)."
        },
        {
          "name": "plotHeight",
          "type": "double",
          "required": false,
          "default": "140",
          "description": "Height of the plot area, in logical pixels."
        },
        {
          "name": "showCategoryLabels",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Draw the first series' point labels under the plot. They are announced either way; this is the drawn axis, and it is dropped when it will not fit."
        },
        {
          "name": "summary",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "One sentence about what the chart shows; announced as the chart's value."
        }
      ]
    },
    "LumoLink": {
      "file": "packages/mobile/lib/src/link.dart",
      "doc": "A navigational link — the web `Link`. Semantics: `link: true`, which is the role a reader needs to know that activating this moves them somewhere; it is NOT a button and must not be announced as one.\n\nFlutter has no router in this package (the web `Link` takes an `href` and can be handed the app's own link component), so **`onTap` is the seam**: the app navigates, the widget names and announces.\n\n**An external link SAYS it is external.** The web makes `newTab` + `newTabLabel` a typed pair — `target`/`rel` are removed from the prop type entirely — and renders the label as `sr-only` text APPENDED after the visible words, so it lands in the accessible name in reading order (WCAG 3.2.5). Here the same: `isExternal` requires `externalLabel` (the constructor asserts it) and the announced name is «label externalLabel», concatenated in reading order. There is no default, because a default would be English. No glyph is invented either: the web's warning is the WORDS, and an ↗ that nobody named would be the defect this library exists to prevent — pass `icon` yourself if the design wants one.\n\n`underline-offset-4` on the web is here a 3px underline offset for the same reason: Arabic-script tails (ی ج ح ع ژ) sit exactly where a default underline cuts through them.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The link's visible text and its announced name. Required.",
          "announced": true
        },
        {
          "name": "onTap",
          "type": "VoidCallback",
          "required": true,
          "default": null,
          "description": "What following the link does. Flutter has no router here — this is the seam."
        },
        {
          "name": "externalLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The announced warning appended to the name. REQUIRED when `isExternal`.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A glyph at the inline END, after the words. Decorative — excluded from semantics."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "A link that cannot be followed: still announced as a link (the web renders `<span role=\"link\" aria-disabled>`, because a nameless generic is worse), but not enabled and with no tap action."
        },
        {
          "name": "isExternal",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether following this link leaves the app."
        },
        {
          "name": "variant",
          "type": "LumoLinkVariant",
          "required": false,
          "default": "LumoLinkVariant.inline",
          "description": "The visual variant."
        }
      ]
    },
    "LumoListBox": {
      "file": "packages/mobile/lib/src/item.dart",
      "doc": "A standalone selectable list — the web `ListBox`: no popover, no trigger (that is `LumoSelect`). `label` is REQUIRED — an unnamed list announces as \"list\" and nothing else — and `emptyLabel` is REQUIRED too, because an empty list that says nothing is indistinguishable from a broken one and the web's empty text is a caller string as well (`asyncState.emptyText`; React Aria's `renderEmptyState` is gone from the web type).\n\nControlled (`value`) and uncontrolled (`defaultValue`) like the web's `selectedKeys`/`defaultSelectedKeys`; both are a `Set<String>` in single mode too, which is the web's shape — single mode simply never holds more than one key. `onChanged` is handed the whole next selection (the web's `onSelectionChange`).\n\nSemantics: a `list` named by `label`; every row a button carrying its `selected` state, so colour is never the sole carrier (WCAG 1.4.1) and the check glyph at the inline END is decoration on top of it. Rows are `LumoItem`s at `sm`: the web's option has its own denser metrics because it is a pointer target inside a scroll pane, whereas on a phone the row IS the touch target and must stay one.\n\nNot carried from the web: keyboard navigation and typeahead (a roving tab stop, `shouldFocusWrap`, `orientation`, Home/End/PageUp/PageDown, the Persian-folding typeahead) — Flutter's focus traversal owns arrow keys and there is no on-screen keyboard behind a list of rows; `onAction` (a row either selects or navigates, and a navigating row is a `LumoItem`); `asyncState` (loading/error/load-more belongs to the screen).",
      "props": [
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What is said when `items` is empty, e.g. «هیچ پرونده‌ای نیست». Required.",
          "announced": true
        },
        {
          "name": "items",
          "type": "List<LumoListBoxItem>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the list. Required.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The selected keys (uncontrolled)."
        },
        {
          "name": "disallowEmptySelection",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Refuse to end up with nothing selected — the web prop of the same name."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<Set<String>>?",
          "required": false,
          "default": null,
          "description": "Called with the whole next selection."
        },
        {
          "name": "selectionMode",
          "type": "LumoListBoxSelectionMode",
          "required": false,
          "default": "LumoListBoxSelectionMode.single",
          "description": "Whether one item may be selected, or many."
        },
        {
          "name": "size",
          "type": "LumoItemSize",
          "required": false,
          "default": "LumoItemSize.sm",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "value",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The selected keys (controlled); `null` leaves the state to the widget."
        }
      ]
    },
    "LumoListBoxItem": {
      "file": "packages/mobile/lib/src/item.dart",
      "doc": "One option of a `LumoListBox`: `id` is the key the list reports, `title` REQUIRED (the option's visible text and its announced name — the web reads an option's name out of its own contents, which cannot be empty here by construction).",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The visible title.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "leading",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "What sits at the reading start of the row."
        },
        {
          "name": "trailing",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "What sits at the reading end of the row."
        }
      ]
    },
    "LumoMaskInput": {
      "file": "packages/mobile/lib/src/mask_input.dart",
      "doc": "A field whose text is forced into a fixed shape as it is typed — a card number, a national ID, a postal code, a plate. The web `MaskInput` (`mask-input.tsx`), whose engine is maska; here the engine is [lumoMaskValue], forty lines that owe nothing to a package.\n\n**`label` REQUIRED**; `mask` uses maska's tokens (`#` digit, `@` letter, `*` either, everything else literal), and `onChanged` reports BOTH forms — the [LumoMaskValue.raw] the app stores and the [LumoMaskValue.masked] the reader sees — because a caller that gets only one of them writes the other conversion itself, differently.\n\n**Persian digits fill the mask.** «۱۲۳۴۵۶۷۸» typed into `'####-####'` yields `1234-5678` on screen and `'12345678'` as `raw`. The shown value is ASCII and an LTR ISLAND, matching the web's `dir=\"ltr\"` + `data-lumo-latn` on the same control: a masked value is a code, and a code reads left-to-right in every script — the box itself stays exactly where the form put it. This is a data-type fact, not a direction flag; it is `LumoTextField.isNumeric`, the same one `LumoPhoneInput` uses.\n\n`maskPlaceholder`, when given, is the character standing in for an unfilled slot: the field shows the whole skeleton (`'____-____'`) while it is empty, so the shape is visible before the first keystroke without a word in any language.\n\n**The caret goes to the end after every edit.** Editing in the middle of a masked value moves the caret; the alternative is a caret-mapping pass that is wrong in a different way in every mask. Stated rather than discovered.\n\nWeb props not carried: `name` (nothing is posted by the platform), and maska's own options beyond the token set (`eager`, `reversed`, custom token tables) — none of them reaches anything in this implementation, and a prop that reaches nothing is not declared.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced (and, unless `showLabel` is false, displayed) name. REQUIRED.",
          "announced": true
        },
        {
          "name": "mask",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The pattern: `'####-####'`, `'@@-###'`, `'###-##-####'`."
        },
        {
          "name": "autofocus",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control takes focus when it first appears."
        },
        {
          "name": "defaultValue",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Uncontrolled initial text, on the same terms."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error the app decided on. Wins over [incompleteMessage].",
          "announced": true
        },
        {
          "name": "focusNode",
          "type": "FocusNode?",
          "required": false,
          "default": null,
          "description": "An optional focus node, for callers that manage focus themselves."
        },
        {
          "name": "incompleteMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced while the field holds something that does not fill the mask. Optional: a field that is allowed to stay half-typed says nothing."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether user input is required before the form is submitted."
        },
        {
          "name": "maskPlaceholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The character drawn in an unfilled slot. `null` shows no skeleton."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<LumoMaskValue>?",
          "required": false,
          "default": null,
          "description": "Called after every edit with the raw value, the masked value and whether the mask is full."
        },
        {
          "name": "prefix",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A glyph at the inline start (decorative — the label names the field)."
        },
        {
          "name": "showLabel",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Whether the label is painted. It is announced either way — hiding a name is not dropping it."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Controlled text — raw or masked, it is re-masked either way."
        }
      ]
    },
    "LumoMaskValue": {
      "file": "packages/mobile/lib/src/mask_input.dart",
      "doc": "",
      "props": [
        {
          "name": "isComplete",
          "type": "bool",
          "required": true,
          "default": null,
          "description": "Whether every slot in the mask is filled."
        },
        {
          "name": "masked",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The value AS SHOWN, mask characters included: `'1234-5678'`."
        },
        {
          "name": "raw",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The value WITHOUT the mask's own characters, in ASCII: `'12345678'`. This is what a server is sent and what a database holds; the separators are presentation, and Persian digits are a numbering system, not data."
        }
      ]
    },
    "LumoMenuCheckboxItem": {
      "file": "packages/mobile/lib/src/menu.dart",
      "doc": "A toggle inside a menu — the web's `menuitemcheckbox`: one control, checked state announced, NOT a checkbox nested in an item (two controls). CONTROLLED (`isSelected` + `onChanged`): a menu of toggles is a view of state that lives elsewhere. Ticking keeps the menu open (the web's `closeOnClick` default: toggling three columns should not reopen the menu three times).",
      "props": [
        {
          "name": "isSelected",
          "type": "bool",
          "required": true,
          "default": null,
          "description": "Whether this one is selected."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        }
      ]
    },
    "LumoMenuEntry": {
      "file": "packages/mobile/lib/src/menu.dart",
      "doc": "One entry of a menu: an item, a checkbox item, a separator or a titled section. A sealed hierarchy — the menu's builder switches exhaustively.",
      "props": []
    },
    "LumoMenuItem": {
      "file": "packages/mobile/lib/src/menu.dart",
      "doc": "One action. `label` REQUIRED — the visible text IS the announced name (a bare icon is not one); `icon` is decoration in the leading slot; `isDestructive` colours the label `critical` (a colour is a hint, the word carries the meaning — put it in the label). Selecting closes the menu, then calls `onSelected` (so an action that opens a dialog opens it above nothing).",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading icon. Decorative — it is not announced, so it never carries meaning on its own."
        },
        {
          "name": "isDestructive",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether this item destroys something, and should be coloured and announced as such."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onSelected",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Called when the item is chosen."
        }
      ]
    },
    "LumoMenuSection": {
      "file": "packages/mobile/lib/src/menu.dart",
      "doc": "A titled group. `label` REQUIRED — the web's `Menu.GroupLabel` names the group; here it is a header node the reader lands on before the group's items.",
      "props": [
        {
          "name": "items",
          "type": "List<LumoMenuEntry>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        }
      ]
    },
    "LumoMenuSeparator": {
      "file": "packages/mobile/lib/src/menu.dart",
      "doc": "A rule between items. Decoration: excluded from semantics.",
      "props": []
    },
    "LumoMenuTrigger": {
      "file": "packages/mobile/lib/src/menu.dart",
      "doc": "A menu of actions on Lumo's popover surface (`showLumoPopover`), anchored to the trigger at `bottomStart` — the web `MenuPopover`'s default — which mirrors under RTL. `label` REQUIRED: the menu's announced name (names the route; the web names the popup by its trigger). Items are buttons, a checkbox item carries the checked state, a section starts with a header, separators are silent. The list is re-read on every rebuild of this widget, so a checkbox item toggled by the caller's `setState` updates while open.\n\nNo `isOpen`/`defaultOpen` (routes are pushed, not rendered — as `LumoDialogTrigger`), no submenus (`SubmenuTrigger`: nested popovers on a phone are a sheet's job), no link items (`href`: no router seam here).",
      "props": [
        {
          "name": "items",
          "type": "List<LumoMenuEntry>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "trigger",
          "type": "Widget Function(VoidCallback? open)",
          "required": true,
          "default": null,
          "description": "Built with the press that opens the menu."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onOpenChange",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called when the surface opens or closes, with the new state."
        },
        {
          "name": "placement",
          "type": "LumoPlacement",
          "required": false,
          "default": "LumoPlacement.bottomStart",
          "description": "Which side the surface prefers to open on."
        }
      ]
    },
    "LumoMessage": {
      "file": "packages/mobile/lib/src/message.dart",
      "doc": "One message in a transcript — the web `Message` + `Bubble` composed into a single widget, because the parts only ever appear together and every compositional seam on the web (`MessageBody`, `MessageHeader`, `MessageTime`) is a chance to put the bubble on the wrong side.\n\n* `text` is the usual case; `child` is the escape hatch for rich content (a photo, a quoted card). One of the two is REQUIRED (asserted). * `senderLabel` is REQUIRED for an incoming message: a reader hearing a transcript needs to know who is speaking, and «he said»/«she said» is not derivable from a bubble's colour. An outgoing message is the reader's own, so it needs no name. * `timeLabel` is a PRE-FORMATTED string — the app ran `formatLumoDate` / `formatNumber` and chose Jalali or Gregorian; this widget never sees a `DateTime` (the web's `MessageTime` takes a string for the same reason). * `status` needs a `statusLabel` (asserted): «در حال ارسال»، «ارسال شد»، «خوانده شد». * `avatar` is DECORATIVE here — `senderLabel` already names the speaker, so the avatar's own name would be announced twice; it is excluded. * `isSystem` is the centred notice («این گفت‌وگو رمزنگاری شده است») — it ignores `side` and takes no sender.\n\nGeometry: the row is a `Row` whose children are REVERSED for an outgoing message (the web's `flex-row-reverse` — flow order, not physical order), the body column aligns to `CrossAxisAlignment.start`/`.end` (which resolve against `Directionality`), and the tail is a `BorderRadiusDirectional` corner at the bubble's own block-end side. Every one of those mirrors with the locale; nothing here knows the words \"left\" or \"right\".\n\nSemantics: the whole message is ONE node (`MergeSemantics`) whose name is sender, then text, then time, then status — so a screen reader walking the transcript gets a conversation, not four fragments per line. That also means `child` must be CONTENT, not controls: a button inside would be absorbed into the message's node.",
      "props": [
        {
          "name": "side",
          "type": "LumoMessageSide",
          "required": true,
          "default": null,
          "description": "Whose message this is. REQUIRED, for the reason the web's `variant` is."
        },
        {
          "name": "avatar",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "The sender's picture. Decorative — `senderLabel` is the name."
        },
        {
          "name": "child",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Rich content instead of `text`. Content only — see the class docblock."
        },
        {
          "name": "isSystem",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "A centred notice rather than a bubble; `side` and `senderLabel` do not apply."
        },
        {
          "name": "senderLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Who said it. REQUIRED for an incoming message.",
          "announced": true
        },
        {
          "name": "status",
          "type": "LumoMessageStatus?",
          "required": false,
          "default": null,
          "description": "Delivery state of the message."
        },
        {
          "name": "statusLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What `status` MEANS, in words. REQUIRED when `status` is set.",
          "announced": true
        },
        {
          "name": "text",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The message text. Required unless `child` is given.",
          "announced": true
        },
        {
          "name": "timeLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The timestamp AS THE APP FORMATTED IT, e.g. «۱۴:۰۵». Never a `DateTime`.",
          "announced": true
        }
      ]
    },
    "LumoMessageGroup": {
      "file": "packages/mobile/lib/src/message.dart",
      "doc": "A day's worth of messages under a separator — the web `MessageGroup` (the `gap-4` column) plus the date chip mobile transcripts always have and the web leaves to the app. `dateLabel` is REQUIRED and PRE-FORMATTED: only the app knows whether «۲۶ مرداد» or «Today» is right, and `formatLumoDate` already picks the calendar from the locale.\n\nThe separator is a header node, so a reader can jump between days; the group itself adds no node of its own — an unnamed container would be one more thing to walk past, and every child is already its own message node.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The messages of that day."
        },
        {
          "name": "dateLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The day, as the app formatted it. Announced as a heading.",
          "announced": true
        }
      ]
    },
    "LumoMultiSelect": {
      "file": "packages/mobile/lib/src/multi_select.dart",
      "doc": "Several choices from a list — the web `MultiSelect`, in the shape a phone can actually hold. The web anchors a filtered popup under an input full of chips; a phone has no room beside a control and its keyboard covers the bottom half of the screen, so the options live in a **`showLumoSheet` route** (never Material's `showModalBottomSheet`, whose route names itself «Dialog»/«Dismiss» in English) and the chips stay under the trigger where the keyboard cannot hide them. The same decision the Khroos app reached by hand (`DropSelect`).\n\nAnnounced strings, all REQUIRED: `label` (the field's name), `closeLabel` (the sheet's ✕ and its scrim), `confirmLabel` (the footer's primary — it closes the sheet; selection is applied as it happens, so confirming is a dismissal, not a commit), `clearAllLabel`, `countLabel` (a FUNCTION of the already-formatted count, because «۳ مورد انتخاب شده» is not English with the words swapped) and `removeLabel` (a function of the chip's own text — the same shape as `LumoTagGroup.removeLabel`). `searchLabel` and `emptyLabel` are REQUIRED when `isSearchable` — the constructor asserts it.\n\nSelection is CONTROLLED (`values` + `onChanged`, the web's `value` + `onValueChange`); the widget mirrors it optimistically so the sheet and the chips answer a tap even before the caller's `setState` lands, and `didUpdateWidget` hands authority straight back to `values`.\n\nNumbers: the footer count goes through `formatNumber`, and the overflow chip past `maxChips` is «+۳» — digits only, no word in any language.\n\nThe sheet's FOOTER measures itself (the `_fit()` pattern of segmented_control.dart): count sentence and two buttons share one row while they fit, and the count moves to a line of its own when they do not. With real Persian labels at 320 dp the single row overflowed by 36 px, and a footer that sheds its arrangement is the house answer — words are never truncated to keep a Row.\n\n`isRequired` is the web's own prop (`MultiSelectProps.isRequired`); it and `errorMessage` reach the reader as STATE (`SemanticsFlag.isRequired`, `SemanticsValidationResult.invalid`), which is what the web spells `required` and `aria-invalid`. There is no `isInvalid`: the web has none.",
      "props": [
        {
          "name": "clearAllLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the footer's clear-everything button. REQUIRED.",
          "announced": true
        },
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the sheet's ✕ and of its scrim. REQUIRED.",
          "announced": true
        },
        {
          "name": "confirmLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the footer's primary button, which closes the sheet. REQUIRED.",
          "announced": true
        },
        {
          "name": "countLabel",
          "type": "String Function(String count)",
          "required": true,
          "default": null,
          "description": "Builds the footer sentence from the ALREADY FORMATTED count («۳»). REQUIRED — a bare number announces nothing.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. REQUIRED — an unnamed field is a defect.",
          "announced": true
        },
        {
          "name": "options",
          "type": "List<LumoMultiSelectOption>",
          "required": true,
          "default": null,
          "description": "The options to choose from, in reading order."
        },
        {
          "name": "removeLabel",
          "type": "String Function(String optionLabel)",
          "required": true,
          "default": null,
          "description": "Builds each chip's remove-button name from that chip's own text. REQUIRED — an ✕ is not a name.",
          "announced": true
        },
        {
          "name": "values",
          "type": "List<String>",
          "required": true,
          "default": null,
          "description": "The chosen option ids. Controlled, as the web's `value`."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "emptyLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What the reader is told when the query matches nothing. REQUIRED when `isSearchable`.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the field and announced. Supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "The web's `isRequired`: draws the « *» marker and sets the reader's `required` state."
        },
        {
          "name": "isSearchable",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Adds a search box above the options. `searchLabel` and `emptyLabel` become required."
        },
        {
          "name": "maxChips",
          "type": "int?",
          "required": false,
          "default": null,
          "description": "How many chips are drawn before the rest collapse into a «+۳» chip."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<List<String>>?",
          "required": false,
          "default": null,
          "description": "Called with the full id list after every change (the web's `onValueChange`)."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the trigger while nothing is chosen.",
          "announced": true
        },
        {
          "name": "searchLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Name of the search box (used as its placeholder too). REQUIRED when `isSearchable`.",
          "announced": true
        }
      ]
    },
    "LumoMultiSelectOption": {
      "file": "packages/mobile/lib/src/multi_select.dart",
      "doc": "One option of a `LumoMultiSelect`: `id` is what the field reports, `label` the announced and displayed text (the web's `{ value, label, disabled }`).",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The stable key handed back through `onChanged` — the web's `value`."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The announced and displayed text of the option.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoNavigationBar": {
      "file": "packages/mobile/lib/src/navigation_bar.dart",
      "doc": "",
      "props": [
        {
          "name": "items",
          "type": "List<LumoNavigationItem>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the navigation landmark. Required.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The first-frame destination's id (uncontrolled)."
        },
        {
          "name": "indicator",
          "type": "LumoNavigationBarIndicator",
          "required": false,
          "default": "LumoNavigationBarIndicator.pill",
          "description": "Which selected-state shape the bar draws."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The current destination's id (controlled)."
        }
      ]
    },
    "LumoNavigationDrawer": {
      "file": "packages/mobile/lib/src/navigation_drawer.dart",
      "doc": "The side drawer: a panel that enters from the reading START — the RIGHT under fa-IR, the LEFT under en-US — and mirrors entirely, including the corner it rounds (only its inline-END corners are round; the edge it is attached to is square).\n\n`label` REQUIRED (it names the route and is the panel's heading) and `closeLabel` REQUIRED (an ✕ is not a name). `sections` are groups of `LumoNavigationItem` — the same item the bottom bar takes, so one app declares its destinations once. `value` is the current destination's id.\n\nNEVER Material's `Drawer` / `Scaffold.drawer`: that pair names itself from `MaterialLocalizations` — `drawerLabel` («Navigation menu»), and the hamburger's tooltip `openAppDrawerTooltip` («Open navigation menu») — English no parameter of ours reaches, the same defect as the Material route helpers `gate:flutter-contract` fails the build on. `showLumoNavigationDrawer` is therefore the inline-axis sibling of `showLumoSheetRoute`: the same `showGeneralDialog` route, the same re-provided `LumoScope`, the same `closeLabel`-named scrim — only the travel axis differs, and it is the axis that mirrors, so the slide's sign is read from `Directionality`.\n\nSelecting a destination CLOSES the drawer and then reports it, the order `LumoMenuTrigger` uses: an action that opens a screen should open it above the app, not above the drawer.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the ✕. Required.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the drawer — it names the route and is drawn as the heading.",
          "announced": true
        },
        {
          "name": "sections",
          "type": "List<LumoNavigationSection>",
          "required": true,
          "default": null,
          "description": "The sections, in reading order."
        },
        {
          "name": "footer",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Below the destinations — a sign-out row, a version line."
        },
        {
          "name": "header",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Above the destinations — an account row, a logo."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "onClose",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "What the ✕ does. Defaults to popping the route the drawer was shown in."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The current destination's id."
        },
        {
          "name": "width",
          "type": "double",
          "required": false,
          "default": "300",
          "description": "Width in logical pixels."
        }
      ]
    },
    "LumoNavigationItem": {
      "file": "packages/mobile/lib/src/navigation_bar.dart",
      "doc": "One destination of a navigation surface — the bottom bar and the drawer share it, as the web's sidebar and navigation-menu share their item shape.\n\n`id` is what the surface reports; `label` is REQUIRED — it is the visible text AND the announced name (an icon is not a name, which is the whole reason `LumoNavigationBar` exists rather than a `Row` of icon buttons). `selectedIcon` is the filled twin drawn while the destination is current (Material's `NavigationDestination.selectedIcon`); `badge` is a PRE-FORMATTED String — the app ran `formatNumber(n, locale)`, because a raw `int` painted into a Persian bar would show Latin digits.",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "badge",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A count, already formatted for the reader's digits. Announced WITH the name."
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Decoration in the leading slot; the label carries the meaning."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "selectedIcon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Drawn instead of [icon] while this destination is the current one."
        }
      ]
    },
    "LumoNavigationSection": {
      "file": "packages/mobile/lib/src/navigation_drawer.dart",
      "doc": "A titled group of destinations inside a drawer — the web sidebar's `SidebarGroup`. `label` is the group's visible heading and is announced as a header; it is OPTIONAL and nullable (never defaulted), because a drawer of five destinations needs no heading at all — the same shape `LumoSeparator` takes for its optional name.",
      "props": [
        {
          "name": "items",
          "type": "List<LumoNavigationItem>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The group's heading, when the group has one.",
          "announced": true
        }
      ]
    },
    "LumoNumberField": {
      "file": "packages/mobile/lib/src/number_field.dart",
      "doc": "A number field — the counterpart of the web `NumberField`: a text field whose display is `formatNumber(value, locale)` (Persian digits and separators under `fa-*`) and whose input is parsed back to a `num` from ANY digits the reader types (Persian, Arabic-Indic, the locale's own — learned from `intl`, never tabled), plus a stepper pair stacked on the BLOCK axis at the inline END (up is more in both scripts; a horizontal `[-][+]` would encode a left-to-right number line), exactly as on the web. `label`, `incrementLabel`, `decrementLabel` REQUIRED — Base UI's three English leaks are three required parameters here. The digits are an LTR island: the field's own `textDirection` is `ltr` so a sign and a decimal keep their place, while `textAlign` keeps the number at the reading START. Never `TextInputType.number` alone: the keyboard is numeric, but the parser, not the platform, decides what a digit is. `onChanged` hands back `null` for an empty or unparsable field (the web's `NaN`). Semantics: ONE text-field node named by `label` (`MergeSemantics`) whose value is the formatted number, and two buttons named by the stepper labels — disabled at the bound.\n\n**The steppers' hit area.** The web paints the column `end-1 inset-y-1 w-6`, so each stepper is 24x14 — measured on mobile too, the smallest pair of buttons in the library. The chevrons still PAINT exactly there; the tappable cell is widened to `LumoControl.lg` (44) and given the control's FULL height, so each stepper is 44x18 instead of 24x14. It cannot be 44 TALL: two steppers stacked inside one 36 px control is the web's geometry, and 88 px of stacked target would be a different component. The extra 44−28 px of width eats into the input's `pe-8` gutter, which is empty — the number is at the reading START.",
      "props": [
        {
          "name": "decrementLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the decrement button. REQUIRED.",
          "announced": true
        },
        {
          "name": "incrementLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the increment button. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. REQUIRED.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "num?",
          "required": false,
          "default": null,
          "description": "Uncontrolled initial value."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error message for the field. Announced as the control's hint and marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isReadOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether user input is required before the form is submitted."
        },
        {
          "name": "max",
          "type": "num?",
          "required": false,
          "default": null,
          "description": "The highest value the control accepts."
        },
        {
          "name": "min",
          "type": "num?",
          "required": false,
          "default": null,
          "description": "The lowest value the control accepts."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<num?>?",
          "required": false,
          "default": null,
          "description": "Fires with the parsed number, or null when the field is empty/unparsable."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Placeholder text shown while the field is empty. Never a substitute for the label.",
          "announced": true
        },
        {
          "name": "step",
          "type": "num",
          "required": false,
          "default": "1",
          "description": "One stepper tick. Web `step`."
        },
        {
          "name": "value",
          "type": "num?",
          "required": false,
          "default": null,
          "description": "Controlled value. Null leaves the field uncontrolled; `double.nan` is a controlled EMPTY field (the web's NaN)."
        }
      ]
    },
    "LumoOtpField": {
      "file": "packages/mobile/lib/src/otp_field.dart",
      "doc": "A one-time code, entered into a row of boxes — the counterpart of the web `InputOtp`. ONE hidden `TextField` holds the value (one focus stop, one name, one paste, the platform's SMS autofill through `AutofillHints.oneTimeCode`); the boxes are painted decoration showing each digit through `formatNumber` (Persian digits under `fa-*`), the caret box wearing the focus ring. The row is wrapped in `Directionality(textDirection: TextDirection.ltr)` in BOTH locales ON PURPOSE: a code is a number and a number is a left-to-right run in every script — the one place a physical direction is written deliberately, as on the web. `onChanged`/`onCompleted` hand back ASCII whatever digits were typed (Persian, Arabic-Indic, the locale's own — learned from `intl`, never tabled). Semantics: the field is one text field named by `label` whose value is the entered digits (in the reader's numerals); each box is a read-only node named by the REQUIRED `cellLabel` so a reader walking the row hears «رقم ۱ از ۶، ۳» rather than a bare digit.\n\n**The touch band.** A box is `h-control-md w-control-md` — 36 square, the web's scale — and the field that receives the tap was exactly as tall, measured at 36 against a 44 px floor. The row now lays out `LumoControl.lg` (44) tall with the boxes painted, unchanged, in the middle of it; the one real field is stretched over the whole band, so the 4 px above and below each box are live.",
      "props": [
        {
          "name": "cellLabel",
          "type": "LumoOtpCellLabel",
          "required": true,
          "default": null,
          "description": "Names each cell for a reader walking the row. REQUIRED — a box is not a name.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. REQUIRED — one field, one name.",
          "announced": true
        },
        {
          "name": "autoFocus",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control takes focus when it first appears."
        },
        {
          "name": "defaultValue",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Uncontrolled initial value, ASCII."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown and announced when the code is rejected; sets the invalid border on every box.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "length",
          "type": "int",
          "required": false,
          "default": "6",
          "description": "How many boxes. Six is the Iranian SMS default."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Fires with the ASCII code on every change, complete or not."
        },
        {
          "name": "onCompleted",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Fires once the last box is filled, with the ASCII code — separate from `onChanged` so no caller writes `>=`."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Controlled value, ASCII. Non-digits are dropped, characters past `length` ignored."
        }
      ]
    },
    "LumoPhoneCountry": {
      "file": "packages/mobile/lib/src/phone_input.dart",
      "doc": "One entry of a phone plan — the web's `PhoneCountry`. `dial` carries NO plus. `names` is per locale tag because a country name is an announced string and Lumo never defaults one to English: an unnamed language falls back to the ISO `code`, never to another language — the web's rule, where the names live in `LumoStrings[\"phoneInput\"].countries`, which Flutter has no counterpart of, so they are data here.",
      "props": [
        {
          "name": "code",
          "type": "String",
          "required": true,
          "default": null,
          "description": "ISO 3166-1 alpha-2, uppercase."
        },
        {
          "name": "dial",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Dial code WITHOUT the plus, e.g. `98`."
        },
        {
          "name": "names",
          "type": "Map<String, String>",
          "required": false,
          "default": "const {}",
          "description": "The country's name per locale tag (`fa`, `fa-IR`, `en`, …)."
        },
        {
          "name": "nationalLength",
          "type": "int?",
          "required": false,
          "default": null,
          "description": "National number length, excluding the trunk prefix. The only validation claimed."
        }
      ]
    },
    "LumoPhoneInput": {
      "file": "packages/mobile/lib/src/phone_input.dart",
      "doc": "A phone number, entered the way Iranians actually type one: «۰۹۱۲…» with a trunk zero, in either numeral system, handed to the caller as E.164 (`+989121234567`) with the zero stripped — Persian digits on screen, ASCII on the wire. The web `PhoneInput`.\n\n**The number is ALWAYS an LTR island.** A phone number reads left-to-right in every script, so the dial prefix and the digits sit in their own `Directionality(ltr)` — the `<bdi dir=\"ltr\">` of the web component, and the same data-type rule as `LumoTextField(isNumeric:)`. It is not a direction flag: the ROW still mirrors, so the country selector takes the reading start (right under `fa-IR`, left under `en-US`).\n\nAnnounced strings, all REQUIRED: `label` (the field's name), `countryLabel` (names the country selector — a second control inside one field), `closeLabel` (the country sheet's ✕ and scrim) and `searchLabel` (names the sheet's search box). Country NAMES come from the `countries` data per locale and fall back to the ISO code, never to English.\n\n`keyboardType` is `phone`, never a numeric one that would reject the digits a Persian keyboard produces; the parser, not the platform, decides what a digit is.\n\nState: `errorMessage` is the ONLY validity input, exactly as on the web (`phone-input.tsx` has no `isInvalid` and derives `aria-invalid` from the message alone), and it reaches the reader as `SemanticsValidationResult .invalid` on the number field — the state, not only the sentence. The web has no `isRequired` here either, so neither does this.\n\nThe country selector is 132 logical px wide where the web's is `w-36` (144): a stated mobile deviation. At 320 dp the row must still leave the number field room for a dial prefix and ten digits, and 144 does not.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the country sheet's ✕ and its scrim. REQUIRED.",
          "announced": true
        },
        {
          "name": "countryLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the country selector. REQUIRED — it is a second control in one field.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. REQUIRED — an unnamed field is a defect.",
          "announced": true
        },
        {
          "name": "searchLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the country sheet's search box. REQUIRED.",
          "announced": true
        },
        {
          "name": "countries",
          "type": "List<LumoPhoneCountry>",
          "required": false,
          "default": "kLumoPhoneCountries",
          "description": "Overrides the shipped plan."
        },
        {
          "name": "defaultCountry",
          "type": "String",
          "required": false,
          "default": "'IR'",
          "description": "ISO code whose dial code is selected first."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the field and announced. Supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Fires with E.164, or `''` when the field is emptied."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Placeholder text shown while the field is empty. Never a substitute for the label.",
          "announced": true
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Controlled value, always E.164 (`+989121234567`). Null leaves the number to the widget."
        }
      ]
    },
    "LumoPopoverTrigger": {
      "file": "packages/mobile/lib/src/popover.dart",
      "doc": "The declarative shape: a trigger whose press opens the popover, anchored to the trigger itself. `label` REQUIRED (the popover's announced name); `closeLabel` REQUIRED when `showClose` (asserted). `onOpenChange` fires true on open and false when it closes (any way). No `isOpen`/`defaultOpen`: Flutter's overlays are routes you push, not state you render — the same divergence `LumoDialogTrigger` records.",
      "props": [
        {
          "name": "content",
          "type": "WidgetBuilder",
          "required": true,
          "default": null,
          "description": "Builds what the surface shows."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "trigger",
          "type": "Widget Function(VoidCallback? open)",
          "required": true,
          "default": null,
          "description": "Built with the press that opens the popover."
        },
        {
          "name": "closeLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced name of the close affordance. An icon is not a name.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onOpenChange",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called when the surface opens or closes, with the new state."
        },
        {
          "name": "padded",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Whether the surface adds its own padding. Turn it off for content that pads itself."
        },
        {
          "name": "placement",
          "type": "LumoPlacement",
          "required": false,
          "default": "LumoPlacement.bottomStart",
          "description": "Which side the surface prefers to open on."
        },
        {
          "name": "showClose",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether a close affordance is drawn."
        }
      ]
    },
    "LumoProgress": {
      "file": "packages/mobile/lib/src/progress.dart",
      "doc": "An operation in flight — the web `ProgressBar`. `label` REQUIRED (what is progressing, in the reader's language); `value` is the FRACTION 0..1, or `null` for indeterminate (a full-width pulse — a sliding bar would run backwards under RTL); `valueLabel` is the pre-formatted value the app built with `formatNumber` («۴۵٪») — the SAME string is shown (with `showValue`) and announced as the semantics value, so seen and announced cannot drift. The fill grows from the inline START (`AlignmentDirectional.centerStart`).\n\nNo `SemanticsRole.progressBar`: Flutter 3.35 declares the role but its debug validator is unimplemented and throws; the label + value node is what TalkBack/VoiceOver read either way.\n\nBOTH animations obey `MediaQuery.disableAnimationsOf`: the indeterminate pulse is stopped and parked opaque, and the determinate fill's 160ms growth (the web's `transition-[inline-size]`) collapses to `Duration.zero`, so the bar jumps to the new value instead of sweeping across the screen.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "showValue",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Render the label and the value above the track."
        },
        {
          "name": "size",
          "type": "LumoProgressSize",
          "required": false,
          "default": "LumoProgressSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "tone",
          "type": "LumoProgressTone",
          "required": false,
          "default": "LumoProgressTone.accent",
          "description": "The semantic tone the colour carries."
        },
        {
          "name": "value",
          "type": "double?",
          "required": false,
          "default": "0",
          "description": "0..1, or `null` = indeterminate."
        },
        {
          "name": "valueLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The value as the app formatted it, e.g. «۴۵٪». Announced; shown with `showValue`.",
          "announced": true
        }
      ]
    },
    "LumoPullToRefresh": {
      "file": "packages/mobile/lib/src/pull_to_refresh.dart",
      "doc": "Pull a list down past its start to reload it.\n\n**Why this is not `RefreshIndicator`.** The brief was: build on Material's widget only if its semantics can be named in Lumo's language. What the source says (`packages/flutter/lib/src/material/refresh_indicator.dart`, Flutter 3.35):\n\n- line 664: `semanticsLabel: widget.semanticsLabel ?? MaterialLocalizations.of(context).refreshIndicatorSemanticLabel` — so there IS a public parameter and passing it does reach the label. On that narrow question the answer is \"yes, it can be named\". - line 618: `assert(debugCheckHasMaterialLocalizations(context))` — but the widget still refuses to build without a Material locale table, for a string it may never use.\n\nTwo things decided it anyway, and neither is the label:\n\n1. **There is no way in.** The name lands on the `RefreshProgressIndicator` while it is on screen. Nothing names the refreshable region, and nothing offers a refresh ACTION — a pull is a gesture a screen-reader user cannot make, so on Material's widget that reader has no route to a refresh at all. Here [refreshLabel] is a `CustomSemanticsAction` on the region, which is the whole reason the string is required. 2. **The puck is Material's.** `RefreshProgressIndicator` is a white circle with an arrow, coloured from `ColorScheme`/`ProgressIndicatorTheme`, with Material's own elevation. Lumo answers a pull the way the Khroos app hand-rolled it: a glyph that rotates with the pull, a spinner while it runs, both on the scope's colours.\n\n**Why overscroll, not touch deltas.** The gesture is read from `ScrollNotification`s, exactly as `provider_screens.dart` reads them: a `GestureDetector` wrapped around a list competes with the list in the gesture arena and steals drags that were meant to scroll. Overscroll is the list's own report that it is already at its start and the finger is still pulling, which is precisely when a pull means \"refresh\". Both physics are handled: iOS's `BouncingScrollPhysics` reports a negative `pixels` on a `ScrollUpdateNotification`, Android's clamping physics reports an `OverscrollNotification` and never moves `pixels` at all.\n\n**Why the child's physics are replaced.** A list shorter than its viewport does not scroll, so it never overscrolls, so it can never be pulled — the same trap `RefreshIndicator` documents and leaves to the caller. `AlwaysScrollableScrollPhysics` is imposed through a `ScrollConfiguration` so a short list stays refreshable without the caller remembering. A child that names its own `physics` still wins; that is the caller's choice.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The scrollable. Anything that reports `ScrollNotification`s."
        },
        {
          "name": "onRefresh",
          "type": "Future<void> Function()",
          "required": true,
          "default": null,
          "description": "Runs the refresh. The indicator stays up until the future completes."
        },
        {
          "name": "refreshLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced while a refresh runs, e.g. «در حال به‌روزرسانی…», AND the name of the semantic action that starts one. REQUIRED: it is the only route a screen-reader user has, since they cannot perform the pull.",
          "announced": true
        },
        {
          "name": "maxDistance",
          "type": "double",
          "required": false,
          "default": "96",
          "description": "The furthest the indicator travels, however hard the pull."
        },
        {
          "name": "pullLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Drawn while the finger is pulling but has not yet gone far enough, e.g. «برای تازه‌کردن بکشید». Optional — with no string the glyph pulls alone.",
          "announced": true
        },
        {
          "name": "releaseLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Drawn once the pull has passed [triggerDistance], e.g. «رها کنید تا تازه شود».",
          "announced": true
        },
        {
          "name": "triggerDistance",
          "type": "double",
          "required": false,
          "default": "64",
          "description": "How far the list must be pulled before releasing refreshes."
        }
      ]
    },
    "LumoRadio": {
      "file": "packages/mobile/lib/src/radio_group.dart",
      "doc": "One option: `value` REQUIRED (what the group reports), `label` REQUIRED (each radio names ITSELF — inheriting the group's name would announce five options all called «روش پرداخت»). The circle sits at the inline START; the dot scales from the centre, direction-neutrally. Announced as a member of an exclusive group with its checked state. No `errorMessage` here — validation belongs to the group.\n\n**The hit area is at least [LumoControl.lg] (44) square.** The web's `radioVariants` is `w-fit` and unheighted, which under a pointer is right and under a thumb is not: measured in a 360 dp column an option labelled «ای» was 56.5×36 and the drawn circle inside it is 20×20. The floor is applied as a MINIMUM on the row, so a long option still sizes to its words and nothing drawn moves — the circle is still at the inline start and the label still beside it; only transparent row grows. The dot's scale stands still under `disableAnimations`.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "value",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The current value. Supply it with `onChanged` for a controlled widget; omit both and the widget owns its own."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoRadioGroup": {
      "file": "packages/mobile/lib/src/radio_group.dart",
      "doc": "A group of mutually exclusive options — the web `RadioGroup`: named by the visible `label` or an explicit `accessibilityLabel` (the constructor asserts one is present; the radios name the OPTIONS, not the question). Controlled (`value`) and uncontrolled (`defaultValue`) like the web; `onChanged` hands over the chosen option's `value`. `orientation` is purely visual, as on the web (a widening WAI-ARIA permits). Validation belongs to the group by construction: `errorMessage` marks every option invalid.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The children, in reading order."
        },
        {
          "name": "accessibilityLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The announced name, for when it must differ from the visible text.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The starting value for an uncontrolled control."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Supplying one marks the group invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isInvalid",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Overrides the invalid state derived from `errorMessage`."
        },
        {
          "name": "isReadOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "label",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "orientation",
          "type": "LumoRadioOrientation",
          "required": false,
          "default": "LumoRadioOrientation.vertical",
          "description": "Which axis the control runs along."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Controlled selection; `null` leaves the state to the widget (`defaultValue`)."
        }
      ]
    },
    "LumoRangeCalendar": {
      "file": "packages/mobile/lib/src/calendar.dart",
      "doc": "A month grid that selects a SPAN of days — `packages/ui/src/range-calendar.tsx`.\n\nEverything `LumoCalendar` says about Jalali applies. What is specific to a range is the band, and the band is the point: **a range drawn only as a background colour does not exist for a reader**. Every day inside the span is announced — `startLabel` on the first, `endLabel` on the last, `inRangeLabel` on the days between — and all three are REQUIRED for that reason. The band itself rounds on LOGICAL corners (`BorderRadiusDirectional`), so it opens toward the reader in both scripts, exactly as the web's `rounded-ss`/`rounded-es` do.\n\nThe tap rule is Material's `DateRangePickerDialog`'s, which is also react-day-picker's practical behaviour: a tap with no range in progress, or with a complete one, starts a new span; a tap on or after the open start closes it; a tap BEFORE the open start moves the start instead of producing an inverted range. `onChanged` fires on both — a span with no `to` is a real intermediate state, which is why `LumoDateRange.to` is nullable.\n\n`value` seeds the span and re-seeds it whenever the caller changes it, so the grid works controlled (inside `LumoDateRangePicker`) and uncontrolled (on its own) without a second parameter.",
      "props": [
        {
          "name": "endLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced on the last day of the span. Required.",
          "announced": true
        },
        {
          "name": "inRangeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced on every day BETWEEN the ends. Required: the band is a colour, and a colour is not an announcement.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the grid. Required.",
          "announced": true
        },
        {
          "name": "nextMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the next-month control.",
          "announced": true
        },
        {
          "name": "previousMonthLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names of the month-paging chevrons. Required — they are icons.",
          "announced": true
        },
        {
          "name": "startLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced on the first day of the span. Required — see the header.",
          "announced": true
        },
        {
          "name": "todayLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The hint on today's cell. Required.",
          "announced": true
        },
        {
          "name": "focusedMonth",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The month the grid opens on. Null starts at the selected date, or today."
        },
        {
          "name": "isDateMarked",
          "type": "bool Function(DateTime date)?",
          "required": false,
          "default": null,
          "description": "Marks days that carry something, with a dot. REQUIRES `markedLabel`."
        },
        {
          "name": "isDateUnavailable",
          "type": "bool Function(DateTime date)?",
          "required": false,
          "default": null,
          "description": "Returns whether a day cannot be chosen — a closed day, a taken slot."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "markedLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced on every marked day. Required as soon as `isDateMarked` is given.",
          "announced": true
        },
        {
          "name": "maxDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The latest date that can be chosen."
        },
        {
          "name": "minDate",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The earliest date that can be chosen."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<LumoDateRange>?",
          "required": false,
          "default": null,
          "description": "Fires with the span after every tap — with `to` absent while only the first end is picked."
        },
        {
          "name": "onMonthChanged",
          "type": "ValueChanged<DateTime>?",
          "required": false,
          "default": null,
          "description": "Fires with day 1 of the month the grid moved to."
        },
        {
          "name": "selectMonthLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Name of the caption button that opens the month/year list. `null` leaves the caption a plain heading.",
          "announced": true
        },
        {
          "name": "today",
          "type": "DateTime?",
          "required": false,
          "default": null,
          "description": "The day marked as today; defaults to the device clock. Injectable for determinism."
        },
        {
          "name": "value",
          "type": "LumoDateRange?",
          "required": false,
          "default": null,
          "description": "The selected span. Re-seeds the grid whenever it changes."
        }
      ]
    },
    "LumoRangeSlider": {
      "file": "packages/mobile/lib/src/slider.dart",
      "doc": "Two values from a range — Material's `RangeSlider` under the same contract. The web has no range slider yet (`formatRange` is not in core), so this is Lumo Mobile's own; its two thumbs are named by the REQUIRED `startLabel` / `endLabel` — Material's `RangeSlider` builds two UNNAMED slider nodes with no way in, so its semantics are excluded and two named `Semantics` nodes are laid over the halves (start half at the reading start: a `Row` under `Directionality`, exactly where Material puts its own), each with `isSlider`, value through `valueLabel`, and increase/decrease actions of one `step` (or a twentieth of the range, Material's own unit, when continuous).",
      "props": [
        {
          "name": "endLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the end thumb. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Displayed name of the whole control. REQUIRED.",
          "announced": true
        },
        {
          "name": "startLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the start thumb, e.g. «کمینهٔ بودجه». REQUIRED.",
          "announced": true
        },
        {
          "name": "valueLabel",
          "type": "LumoValueLabel",
          "required": true,
          "default": null,
          "description": "Turns a value into the string the reader hears and sees. Must format its digits.",
          "announced": true
        },
        {
          "name": "values",
          "type": "RangeValues",
          "required": true,
          "default": null,
          "description": "The current range."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error message for the field. Announced as the control's hint and marks it invalid.",
          "announced": true
        },
        {
          "name": "hideValue",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the value is painted beside the label. It is announced either way."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "max",
          "type": "double",
          "required": false,
          "default": "1",
          "description": "The highest value the control accepts."
        },
        {
          "name": "min",
          "type": "double",
          "required": false,
          "default": "0",
          "description": "The lowest value the control accepts."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<RangeValues>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "onChangeEnd",
          "type": "ValueChanged<RangeValues>?",
          "required": false,
          "default": null,
          "description": "Called once when the drag ends, rather than on every frame."
        },
        {
          "name": "step",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "The increment the control moves by."
        }
      ]
    },
    "LumoRating": {
      "file": "packages/mobile/lib/src/rating.dart",
      "doc": "A star rating, read-only or interactive — the web `Rating`. The value is a number and the stars are glyphs, so the announced strings cannot come from the content: `label` REQUIRED (the rating's name, e.g. «امتیاز شما»), `valueLabel` REQUIRED (the pre-formatted score the app built with `formatNumber`, e.g. «۴٫۷ از ۵» — announced ONCE as the value), and, when `onChanged` is set, `starLabel` REQUIRED (asserted): the name of one star from its position, e.g. `(n) => '${formatNumber(n, locale)} ستاره'`.\n\nRead-only: one `image` node — name + value; fractional values clip the fill (4.5 fills four and a half) from the reading START. Interactive: a `radioGroup` named by `label` carrying the value, one radio per star (checked = the chosen one, as on the web). The stars are a `Row`: the first star sits at the RIGHT under fa-IR and the fill runs in reading direction with no mirroring code. Controlled (`value`) or uncontrolled (`defaultValue`).\n\n**Touch, then fit.** An interactive star PAINTS the web's box (`size-4/5/6` inside `p-0.5`, so 20/24/28) and SITS IN a `LumoControl.lg` (44) cell — measured before, a md star was a 24x24 target and an lg one 28x28. A row of 44s is wider than a phone once there are many stars, so the rating gives ground in the order `segmented_control.dart`'s `_fit()` sets: the touch padding first (the cell shrinks toward the painted star), and only when even the bare stars will not fit does the row SCALE. A star has no words to truncate, so scaling is the honest last resort — before this, ten `lg` stars overflowed a 240 dp row by 40 px.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the rating. Required.",
          "announced": true
        },
        {
          "name": "valueLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The whole score as the app formatted it, e.g. «۴٫۷ از ۵». Announced once. Required.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "The initially chosen score (uncontrolled, interactive)."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "max",
          "type": "int",
          "required": false,
          "default": "5",
          "description": "How many stars."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<double>?",
          "required": false,
          "default": null,
          "description": "Called with the chosen score; makes the rating interactive."
        },
        {
          "name": "size",
          "type": "LumoRatingSize",
          "required": false,
          "default": "LumoRatingSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "starLabel",
          "type": "String Function(int position)?",
          "required": false,
          "default": null,
          "description": "Announced name of one star from its 1-based position. Required with `onChanged`.",
          "announced": true
        },
        {
          "name": "value",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "The score (controlled). Fractional values clip the fill when read-only."
        }
      ]
    },
    "LumoScope": {
      "file": "packages/mobile/lib/src/scope.dart",
      "doc": "The root of a Lumo tree — the counterpart of `LumoNativeProvider` / `LumoProvider`: `locale` is any BCP-47 tag; direction is DERIVED (there is no `dir` parameter anywhere in Lumo); the scheme follows `brightness`.\n\nA consumer's palette: `light` / `dark` replace the scheme's colours (the mobile counterpart of overriding `--lumo-sys-*` custom properties on the web — build them with `lightColours(brand).copyWith(accent: …)`); `brand` alone turns hue and chroma on the generated ramps.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "locale",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The BCP-47 tag everything below derives from: direction, digits, calendar."
        },
        {
          "name": "brand",
          "type": "LumoBrand",
          "required": false,
          "default": "LumoBrand.achromatic",
          "description": "Which brand's palette the scope resolves."
        },
        {
          "name": "brightness",
          "type": "Brightness?",
          "required": false,
          "default": null,
          "description": "Pin a scheme; default follows the platform."
        },
        {
          "name": "dark",
          "type": "LumoSchemeColours?",
          "required": false,
          "default": null,
          "description": "The dark scheme's colours, if not the generated defaults for `brand`."
        },
        {
          "name": "light",
          "type": "LumoSchemeColours?",
          "required": false,
          "default": null,
          "description": "The light scheme's colours, if not the generated defaults for `brand`."
        }
      ]
    },
    "LumoScrollArea": {
      "file": "packages/mobile/lib/src/scroll_area.dart",
      "doc": "A bounded scroller with Lumo's own scrollbar and fade edges — the web `ScrollArea`.\n\n**What the web does and why this differs.** `scroll-area.tsx` is a `<div>` with `overflow-*-auto` and two CSS scrollbar properties: it restyles the ENGINE's scrollbar, so wheel, touch, keyboard and RTL edge placement stay native. Flutter has no engine scrollbar to restyle — `Scrollbar` is a widget that paints from `ScrollbarThemeData`, i.e. from Material's palette. So the thumb here is drawn by [RawScrollbar] with the SCOPE's colours, which is the same decision the web file made (take the platform's mechanics, wear Lumo's paint) arrived at from the other side.\n\n**Fade edges** are the mobile addition the web has no need for: a desktop scrollbar says \"there is more\"; a phone's overlay thumb is gone two seconds after the finger lifts. The fade is a `ShaderMask` in `BlendMode.dstIn`, so it makes the content TRANSPARENT at the edge rather than painting a colour over it — a fade painted in `surface` is wrong the moment the area sits on `bgSubtle`, and that is the bug this avoids. The gradient runs from `AlignmentDirectional.centerStart` to `centerEnd` on the inline axis, so it mirrors with the locale and there is no `isRtl` branch anywhere in the file. Each edge fades only when there is content past it, so a list that fits has no fade at all.\n\n**`overflow-x-hidden`** on the web's `vertical` variant says a too-wide child must clip rather than push the page sideways. `ClipRect` is that line.\n\n**Semantics.** `label` is REQUIRED and names the region — as on the web, where the container is a tab stop and an unnamed one announces nothing. `SemanticsRole.region` is deliberately NOT set: Flutter grades it as a LANDMARK, and a landmark inside another landmark is a framework assertion — a scroll area inside a scroll area, or inside a future `main`, would crash the app rather than mis-announce. The node is a plain named container.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the scrollable region, e.g. «فهرست تراکنش‌ها». REQUIRED.",
          "announced": true
        },
        {
          "name": "controller",
          "type": "ScrollController?",
          "required": false,
          "default": null,
          "description": "A controller the caller owns. Given one, the widget never disposes it; without one it makes and disposes its own."
        },
        {
          "name": "fadeEdges",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Fade the edge that has content beyond it."
        },
        {
          "name": "fadeExtent",
          "type": "double",
          "required": false,
          "default": "24",
          "description": "How many logical pixels the fade covers."
        },
        {
          "name": "orientation",
          "type": "LumoScrollAreaOrientation",
          "required": false,
          "default": "LumoScrollAreaOrientation.vertical",
          "description": "Which axis the control runs along."
        },
        {
          "name": "padding",
          "type": "EdgeInsetsGeometry?",
          "required": false,
          "default": null,
          "description": "Inside the scroller, so the padding scrolls with the content."
        },
        {
          "name": "physics",
          "type": "ScrollPhysics?",
          "required": false,
          "default": null,
          "description": "Scroll physics, for a caller that needs something other than the platform default."
        },
        {
          "name": "showScrollbar",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Draw the thumb. Always visible while there is anything to scroll — an overlay thumb that has already faded cannot say \"there is more\"."
        }
      ]
    },
    "LumoSearchField": {
      "file": "packages/mobile/lib/src/search_field.dart",
      "doc": "A search field — the counterpart of the web `SearchField`: a text field with a search glyph at the inline START and a clear ✕ at the inline END, shown only while there is text. `label` REQUIRED (the field's name; `showLabel: false` keeps it for the reader and drops the visible row — the name never leaves), `clearLabel` REQUIRED (an ✕ is not a name). The keyboard's action key is `TextInputAction.search` and fires `onSubmitted`. As on the web the glyph and the ✕ are laid OVER the field (`PositionedDirectional`) so the border and focus ring stay on the element that takes focus; the field's padding reserves the two overlays logically. Semantics: ONE text-field node named by `label` (`MergeSemantics`), and the ✕ a separate button named by `clearLabel` — the glyph is decoration and excluded, and so is the painted label row (the name lives on the field's node; without the exclusion it was heard TWICE).\n\n**The ✕ draws the web's `IconButton size=\"sm\"` — `LumoControl.sm` square, 16-px glyph, 4 px in from the inline end — but it HITS `LumoControl.lg`.** A stated mobile deviation: 29 × 29 is under every touch guideline, and the extra px are transparent, so the input row reserves `LumoControl.lg` and the box draws centred inside it. The message therefore leaves `InputDecoration` for a row of its own (as in combobox.dart and time_field.dart): the box has to stay one fixed height for the two overlays to sit on its centre line, and `errorText` grew it.\n\n`isInvalid` and `isRequired` are the web's own props (`SearchFieldProps`), and they reach the reader as STATE — `SemanticsValidationResult.invalid` and `SemanticsFlag.isRequired` — not merely as words in a hint.",
      "props": [
        {
          "name": "clearLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The clear button's accessible name. REQUIRED — a default would be English.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced (and, unless `showLabel` is false, displayed) name. REQUIRED.",
          "announced": true
        },
        {
          "name": "autoFocus",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control takes focus when it first appears."
        },
        {
          "name": "controller",
          "type": "TextEditingController?",
          "required": false,
          "default": null,
          "description": "The app's own controller, when it wants one; exclusive with `value`."
        },
        {
          "name": "defaultValue",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Uncontrolled initial text."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error message for the field. Announced as the control's hint and marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isInvalid",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Marks the field wrong WITHOUT a sentence (the web's `isInvalid`). Null derives it from `errorMessage`."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "The web's `isRequired`. Draws the « *» marker and sets the reader's `required` state."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "onClear",
          "type": "VoidCallback?",
          "required": false,
          "default": null,
          "description": "Fires after the ✕ emptied the field (`onChanged('')` fires too)."
        },
        {
          "name": "onSubmitted",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Fires with the text when the keyboard's search key is pressed."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Placeholder text shown while the field is empty. Never a substitute for the label.",
          "announced": true
        },
        {
          "name": "showLabel",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Hide the label row; the name stays on the field."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Controlled text; the field follows it on every build."
        }
      ]
    },
    "LumoSegment": {
      "file": "packages/mobile/lib/src/segmented_control.dart",
      "doc": "One option of a segmented control: `id` REQUIRED (what the control reports), `label` REQUIRED (the announced name — an icon is not a name; the web's `aria-label` for an icon segment). `iconOnly` draws just the icon and keeps the label for the reader.",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading icon. Decorative — it is not announced, so it never carries meaning on its own."
        },
        {
          "name": "iconOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether only the icon is painted. The label is still announced."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoSegmentedControl": {
      "file": "packages/mobile/lib/src/segmented_control.dart",
      "doc": "Two to four mutually exclusive options, shown all at once — the web `SegmentedControl`: `label` REQUIRED (nothing else names the group; it is announced, not drawn). Controlled (`value`) and uncontrolled (`defaultValue`) like the web's `selectedKeys`/`defaultSelectedKeys` — a radio group holds exactly one key, so Dart takes the String. The selected segment is a `surface` pill on a `surfaceSunken` track that slides along the INLINE axis (`AlignmentDirectional`: the first segment sits at the reading start — right in Persian). Each segment is a button announced with its selected state; the rounding lives on the TRACK, never on first/last children, which would round the wrong corners under RTL. The pill's travel collapses to nothing under `disableAnimations` — it is at the new segment on the same frame.\n\n**A segment FILLS the track's height.** The segment row is `Positioned.fill` with `CrossAxisAlignment.stretch` for a measured reason: as a plain `Stack` child the row was sized by its content and aligned `topStart`, so at `md` each segment measured 175×20 inside a 32-tall control — the labels rode 6 px high with 12 px of dead track under them, and two thirds of the pill was not tappable. Both come from the same missing constraint.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "segments",
          "type": "List<LumoSegment>",
          "required": true,
          "default": null,
          "description": "The segments, in reading order."
        },
        {
          "name": "defaultValue",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The starting value for an uncontrolled control."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "size",
          "type": "LumoSegmentedControlSize",
          "required": false,
          "default": "LumoSegmentedControlSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Controlled selection; `null` leaves the state to the widget (`defaultValue`)."
        }
      ]
    },
    "LumoSelect": {
      "file": "packages/mobile/lib/src/select.dart",
      "doc": "A single choice from a short list — Lumo's own, as on React Native (a trigger named by the REQUIRED `label`, showing the REQUIRED `placeholder` or the chosen option; a bottom sheet of options; REQUIRED `closeLabel`). Material's own `DropdownMenu` was not used: its menu is a desktop-shaped popup and its announced strings default to English.\n\nThe trigger is `LumoControl.md` — the shared control scale every field in this library stands on (`text_field`, `combobox`, `date_field`, `multi_select`, `phone_input`), and the web's own default (`size: \"md\"` → `h-control-md`). It is full-bleed, so the target is short only in the block axis; raising it here alone would put one field out of line with the row above it, which is a worse defect than the one it fixes.\n\n**The empty list is a state, not a blank sheet.** The web carries `asyncState` with `emptyText` / loading / error rows; mobile carries the one case a static `options` list can actually be in, and `emptyLabel` is REQUIRED (asserted) as soon as `options` is empty — an empty sheet that says nothing is a dead end a reader cannot tell from a broken screen. Loading and error are not ported: `options` is a plain `List`, so there is no fetch here to be in either state.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the close affordance. An icon is not a name.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "options",
          "type": "List<LumoSelectOption>",
          "required": true,
          "default": null,
          "description": "The options to choose from, in reading order."
        },
        {
          "name": "placeholder",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Placeholder text shown while the field is empty. Never a substitute for the label.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "emptyLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What the sheet says when `options` is empty. REQUIRED then (asserted).",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error message for the field. Announced as the control's hint and marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The current value. Supply it with `onChanged` for a controlled widget; omit both and the widget owns its own."
        }
      ]
    },
    "LumoSelectOption": {
      "file": "packages/mobile/lib/src/select.dart",
      "doc": "",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoSeparator": {
      "file": "packages/mobile/lib/src/separator.dart",
      "doc": "A divider between two groups of content — the web `Separator`. Decorative (excluded from semantics) unless `label` is given, in which case it is announced as a named group boundary. `border`, not `borderControl`: a decorative rule has no 3:1 requirement. Horizontal fills the width; vertical fills the height its parent gives it (`IntrinsicHeight`/`stretch` a `Row`). Width and height are physical dimensions, not directions — nothing to mirror.",
      "props": [
        {
          "name": "label",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A name for the boundary, if the separator carries meaning. Optional: decorative otherwise.",
          "announced": true
        },
        {
          "name": "orientation",
          "type": "LumoSeparatorOrientation",
          "required": false,
          "default": "LumoSeparatorOrientation.horizontal",
          "description": "Which axis the control runs along."
        }
      ]
    },
    "LumoSheetTrigger": {
      "file": "packages/mobile/lib/src/sheet.dart",
      "doc": "The declarative shape: a trigger whose press opens the sheet. `onOpenChange` fires true on open and false when it closes (any way).",
      "props": [
        {
          "name": "body",
          "type": "WidgetBuilder",
          "required": true,
          "default": null,
          "description": "Builds the sheet's content."
        },
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the close affordance. An icon is not a name.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "trigger",
          "type": "Widget Function(VoidCallback? open)",
          "required": true,
          "default": null,
          "description": "Built with the press that opens the sheet."
        },
        {
          "name": "actions",
          "type": "List<Widget> Function(BuildContext)?",
          "required": false,
          "default": null,
          "description": "The actions, in reading order."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isDismissible",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Whether the sheet can be dismissed by the scrim or a drag."
        },
        {
          "name": "onOpenChange",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called when the surface opens or closes, with the new state."
        }
      ]
    },
    "LumoSkeleton": {
      "file": "packages/mobile/lib/src/skeleton.dart",
      "doc": "A placeholder block for content that has not arrived — the web `Skeleton`. A PULSE, not a shimmer: a gradient sweep is physical in its axis and its sign and runs against the reading direction in RTL; opacity has no direction. Static under `disableAnimations` (the block still says \"pending\"). Always excluded from semantics: loading is a STATE — put a `LumoSpinner(label:)` beside it, which announces.",
      "props": [
        {
          "name": "height",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "Height in logical pixels."
        },
        {
          "name": "shape",
          "type": "LumoSkeletonShape",
          "required": false,
          "default": "LumoSkeletonShape.rect",
          "description": "The shape variant."
        },
        {
          "name": "width",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "Width in logical pixels."
        }
      ]
    },
    "LumoSkeletonText": {
      "file": "packages/mobile/lib/src/skeleton.dart",
      "doc": "A stack of `lines` text-line skeletons; the last one is two-thirds wide so the block reads as a paragraph, not a wall. The short line sits at the reading START (a `Column` with `CrossAxisAlignment.start` mirrors).",
      "props": [
        {
          "name": "gap",
          "type": "double",
          "required": false,
          "default": "8",
          "description": "Space between the children."
        },
        {
          "name": "lines",
          "type": "int",
          "required": false,
          "default": "3",
          "description": "How many lines of placeholder text to draw."
        }
      ]
    },
    "LumoSlider": {
      "file": "packages/mobile/lib/src/slider.dart",
      "doc": "A single value chosen from a range — the counterpart of the web `Slider`, on Material's `Slider` wearing the tokens. `label` REQUIRED (an unnamed slider announces bare \"slider\"), `valueLabel` REQUIRED (a raw double is never rendered or announced — the app formats it, once, for both). `step` (web) is translated onto Material's `divisions`. The track fills from the reading START: Material lays the slider out under `Directionality`, so under `fa-*` the minimum sits at the RIGHT and the fill grows leftwards — no flag. Semantics: ONE node (`MergeSemantics`) named by `label`, `isSlider`, value / increasedValue / decreasedValue through `valueLabel`, increase/decrease actions; the header row is excluded so the name is announced once.\n\nThe web `Slider` has NO `isInvalid` and gives an invalid slider no visual state at all (`slider.tsx`: the engine forwards `aria-describedby` to its range input but not `aria-invalid`) — that is carried here, with the one thing Flutter can do that the DOM could not: the node itself takes `SemanticsValidationResult.invalid`, so the state is a state and not only a sentence. `description` and `errorMessage` are painted under the track and EXCLUDED there, because both are already this node's hint.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced (and, unless `hideValue`, displayed) name. REQUIRED.",
          "announced": true
        },
        {
          "name": "value",
          "type": "double",
          "required": true,
          "default": null,
          "description": "The current value. Supply it with `onChanged` for a controlled widget; omit both and the widget owns its own."
        },
        {
          "name": "valueLabel",
          "type": "LumoValueLabel",
          "required": true,
          "default": null,
          "description": "The value as the reader/eye gets it, e.g. `(v) => formatNumber(v.round(), locale)`. REQUIRED.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error message for the field. Announced as the control's hint and marks it invalid.",
          "announced": true
        },
        {
          "name": "hideValue",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Hide the label/value row and keep only the track. The name stays on the node."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "max",
          "type": "double",
          "required": false,
          "default": "1",
          "description": "The highest value the control accepts."
        },
        {
          "name": "min",
          "type": "double",
          "required": false,
          "default": "0",
          "description": "The lowest value the control accepts."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<double>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "onChangeEnd",
          "type": "ValueChanged<double>?",
          "required": false,
          "default": null,
          "description": "Fires when the drag ends (web `onChangeEnd`)."
        },
        {
          "name": "step",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "The amount one tick moves; null = continuous. Web `step`, Material `divisions`."
        }
      ]
    },
    "LumoSortable": {
      "file": "packages/mobile/lib/src/sortable.dart",
      "doc": "A list the reader can reorder — the web `Sortable`.\n\n**The keyboard route, translated.** `sortable.tsx` says its keyboard model IS the component and the pointer drag is the enhancement. A phone has no keyboard, so the same sentence lands differently: the ASSISTIVE route is the component. Every item carries two named semantic actions ([LumoSortableItem.moveUpLabel] / [LumoSortableItem.moveDownLabel]) and both routes — action and drag — go through the one [onReorder]. This is the part forui, shadcn_flutter and Flutter's own `ReorderableListView` all get wrong in the same way: `ReorderableListView` DOES emit move actions, and it names them `MaterialLocalizations.reorderItemUp` — English, from a table no parameter of ours reaches (see `reorderable_list.dart` line 420, `assert(debugCheckHasMaterialLocalizations(context))`). That is why this file does not build on it.\n\n**The drag** is a plain vertical drag on the grip, hit-testing the items' LIVE rects on every update rather than integrating a delta — rows move under the finger, so a cached geometry is wrong one frame after the first swap. It is the same decision `sortable.tsx` records for its pointer route, and for the same reason it needs no direction branch. The drag is SILENT: a `pointermove` is a sample of one gesture, and announcing each sample queues a sentence per pixel.\n\n**Not carried from the web:** `orientation: \"horizontal\"`. It exists there to reinterpret ArrowLeft/ArrowRight against the locale; there are no arrow keys here, and «بالا»/«پایین» would be the wrong words for a row of chips. A horizontal reorder wants its own strings and its own widget.",
      "props": [
        {
          "name": "items",
          "type": "List<LumoSortableItem>",
          "required": true,
          "default": null,
          "description": "The items, in their current order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the list, e.g. «ترتیب مراحل». REQUIRED.",
          "announced": true
        },
        {
          "name": "onReorder",
          "type": "void Function(int from, int to)",
          "required": true,
          "default": null,
          "description": "Called with the item's index BEFORE the move and the index it should end up at — the plain reading of both, unlike `ReorderableListView`, whose `newIndex` counts slots in a list that still contains the dragged row. [reorder] applies exactly this convention to a list."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoSortableItem": {
      "file": "packages/mobile/lib/src/sortable.dart",
      "doc": "One entry of a [LumoSortable].\n\nThree required strings, and the third and fourth are the point of the whole family: **a drag is not available to a screen-reader user**, so an item that can only be dragged cannot be reordered by them at all. [moveUpLabel] and [moveDownLabel] become named `CustomSemanticsAction`s on the item's node — the rotor entry / TalkBack action that IS the keyboard route `sortable.tsx` spells with Space-and-arrows. They are per ITEM, not per list, so the caller can write «انتقال «فاکتور مرداد» به بالا» instead of a bare «به بالا» that says nothing about which row moved.",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Stable identity across reorders. Not announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The item's announced name, and — unless [child] is given — its drawn first line. REQUIRED.",
          "announced": true
        },
        {
          "name": "moveDownLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the \"move one place later\" action, e.g. «انتقال به پایین». REQUIRED.",
          "announced": true
        },
        {
          "name": "moveUpLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Names the \"move one place earlier\" action, e.g. «انتقال به بالا». REQUIRED.",
          "announced": true
        },
        {
          "name": "child",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Replaces the drawn `label`/`description` block entirely. The name is still announced from the item's own node, so a `child` that repeats [label] in a `Text` is announced twice — the same rule `LumoCard` states for its content."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A second drawn line. Announced after the name, as `LumoItem` does.",
          "announced": true
        },
        {
          "name": "leading",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Decoration at the inline start, after the grip. Excluded from semantics."
        },
        {
          "name": "trailing",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Drawn at the inline end — a badge, a count."
        }
      ]
    },
    "LumoSparkline": {
      "file": "packages/mobile/lib/src/chart.dart",
      "doc": "A sparkline — one series, no axis, no labels, the height of a line of text.\n\nThe compact form of `LumoLineChart`, for a trend beside a figure in a stat tile (the shape the Khroos analytics header wanted and drew as a strip of bars because there was nothing to call). It is compact in PIXELS only: every point is still its own named node, because a chart small enough to ignore is not a chart small enough to leave out of the semantics tree.",
      "props": [
        {
          "name": "data",
          "type": "List<LumoChartPoint>",
          "required": true,
          "default": null,
          "description": "The points to plot, in x order."
        },
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "What a reader is told when there is nothing to plot. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The trend's announced name. REQUIRED.",
          "announced": true
        },
        {
          "name": "isArea",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Whether the area under the line is filled."
        },
        {
          "name": "plotHeight",
          "type": "double",
          "required": false,
          "default": "32",
          "description": "Height of the plot, in logical pixels."
        },
        {
          "name": "summary",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A sentence describing the shape for a reader who cannot see it. A chart without one is decoration."
        },
        {
          "name": "tone",
          "type": "LumoChartTone",
          "required": false,
          "default": "LumoChartTone.accent",
          "description": "The semantic tone the colour carries."
        }
      ]
    },
    "LumoSpinner": {
      "file": "packages/mobile/lib/src/progress.dart",
      "doc": "A busy indicator that says so out loud — the web `Spinner`. `label` REQUIRED (what is being waited for, e.g. «در حال بارگذاری…»), announced as a LIVE region on appearance (the web's `role=\"status\"`); shown beside the ring with `showLabel`. On Material's `CircularProgressIndicator`, sized to the spinner scale, its own semantics excluded so the name is heard ONCE.\n\n**Reduce motion.** Under `MediaQuery.disableAnimationsOf` the ROTATION stops and the ring pulses in place instead — the web's `motion-reduce:animate-pulse`, and its reason, verbatim: \"a static ring reads as a bug\". This is the one place the library does not simply go still (`skeleton.dart` does, because a frozen placeholder block still says \"pending\" — a frozen spinner says \"hung\"). The parked ring is the same 3/4 arc the spin draws (`value: 0.75` = the web's `border-bs-transparent` gap), so the drawing is unchanged; only the movement is. Opacity has no axis and no direction, which is why it is the fallback and a sweep is not.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "color",
          "type": "LumoSpinnerColor",
          "required": false,
          "default": "LumoSpinnerColor.current",
          "description": "Which colour role the spinner takes."
        },
        {
          "name": "showLabel",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the label is painted. It is announced either way — hiding a name is not dropping it."
        },
        {
          "name": "size",
          "type": "LumoSpinnerSize",
          "required": false,
          "default": "LumoSpinnerSize.md",
          "description": "The size step, from the shared control scale."
        }
      ]
    },
    "LumoStack": {
      "file": "packages/mobile/lib/src/layout.dart",
      "doc": "A one-axis layout: a logical row that mirrors under RTL, or a column, with a `gap` from the shared scale — the web `Stack` (`stack.tsx`).\n\nThis widget is thin on purpose, and it is not redundant: `gap`, `align` and `justify` are where the spacing scale and the LOGICAL alignment vocabulary get named ONCE. A hand-written `Row(children: [a, SizedBox(width: 12), b])` re-decides the step at every call site, and a hand-written `Alignment.centerLeft` is the silent RTL defect this library exists to prevent. Flutter's own `Row`/`Column`/`Wrap` already resolve `start`/`end` against the inherited `Directionality`, so nothing here needs a flag — the value is the shared names, not new geometry.\n\n`align: stretch` on a ROW is wrapped in an `IntrinsicHeight`, so `stretch` means the same thing on both axes (CSS `align-items: stretch` stretches children to the tallest item; a bare Flutter `Row` with `CrossAxisAlignment.stretch` asserts when its height is unbounded). That is one extra layout pass over the children — the price of the word meaning one thing, stated rather than hidden.\n\n`wrap: true` swaps the flex for a `Wrap` (which also mirrors), where `stretch` and `baseline` have no meaning — a wrapped child is sized by itself — and both fall back to `start`.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The children, in reading order."
        },
        {
          "name": "align",
          "type": "LumoAlign",
          "required": false,
          "default": "LumoAlign.stretch",
          "description": "How the children line up on the cross axis."
        },
        {
          "name": "direction",
          "type": "LumoStackDirection",
          "required": false,
          "default": "LumoStackDirection.column",
          "description": "Which way the stack runs."
        },
        {
          "name": "gap",
          "type": "LumoGap",
          "required": false,
          "default": "LumoGap.sm",
          "description": "Space between the children."
        },
        {
          "name": "justify",
          "type": "LumoJustify",
          "required": false,
          "default": "LumoJustify.start",
          "description": "How the free space on the main axis is spread. Only visible when the stack HAS free space: leave [mainAxisSize] at `min` and there is none."
        },
        {
          "name": "mainAxisSize",
          "type": "MainAxisSize",
          "required": false,
          "default": "MainAxisSize.min",
          "description": "`min` (the default) hugs the children — the shape a sheet, a card body or a list row wants, and the one that is safe inside a scroll view. `max` fills the parent, which is what [justify] needs to have anything to do."
        },
        {
          "name": "wrap",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Lets the line break instead of overflowing. See the docblock for what `align` can still mean once it does."
        }
      ]
    },
    "LumoStep": {
      "file": "packages/mobile/lib/src/steps.dart",
      "doc": "One step: `title` REQUIRED, an optional second line.",
      "props": [
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The visible title.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        }
      ]
    },
    "LumoSteps": {
      "file": "packages/mobile/lib/src/steps.dart",
      "doc": "A stepper — the web `Steps`: `label` REQUIRED (the sequence's announced name), `steps` in order, `current` the 0-BASED INDEX of the step in progress (`steps.length` once the sequence is complete — Dart indexes from 0 where the web's `current` was 1-based), and the three state words REQUIRED — `completedLabel`, `currentLabel`, `upcomingLabel` — announced after each title so colour is never the sole carrier (WCAG 1.4.1). Every disc number goes through `formatNumber(n, locale)` from the scope. A horizontal stepper is a `Row`: nodes and connectors follow the direction, so it reads right→left in Persian with no mirroring code.\n\nSemantics: a `list` named by `label`; each step a `listItem` whose node is title, state word, description — merged, so each is heard ONCE; the disc is decorative (the list already says \"item 2 of 4\").\n\n**A cramped horizontal stepper sheds decoration before it truncates words**, in the order `segmented_control.dart`'s `_fit()` sets: the DESCRIPTION goes first — the title names the step and the second line only elaborates — and only then is the title held to two lines and ellipsised. A shed description is still ANNOUNCED (a semantics-only node, as the state word is): it leaves the picture, never the reading. Three long Persian titles on a 320 dp row used to overflow it by 152 px; the vertical orientation has a column's full width and sheds nothing.",
      "props": [
        {
          "name": "completedLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced status of a finished step, e.g. «تکمیل‌شده». Required.",
          "announced": true
        },
        {
          "name": "current",
          "type": "int",
          "required": true,
          "default": null,
          "description": "0-based index of the step in progress; `steps.length` = all complete."
        },
        {
          "name": "currentLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced status of the step in progress, e.g. «مرحلهٔ فعلی». Required.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "steps",
          "type": "List<LumoStep>",
          "required": true,
          "default": null,
          "description": "The steps, in order."
        },
        {
          "name": "upcomingLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced status of a step not yet started, e.g. «انجام‌نشده». Required.",
          "announced": true
        },
        {
          "name": "orientation",
          "type": "LumoStepsOrientation",
          "required": false,
          "default": "LumoStepsOrientation.horizontal",
          "description": "Which axis the control runs along."
        }
      ]
    },
    "LumoSwitch": {
      "file": "packages/mobile/lib/src/switch.dart",
      "doc": "A switch, direction-sensitive: named by its visible `label` or by an explicit `accessibilityLabel` — the constructor asserts one is present (Dart has no union types; the assertion is the compile-adjacent guard). ON at the reading END, which `Directionality` mirrors — the thumb travels along the inline axis, and stands still under `disableAnimations`.\n\nTrack geometry is the web's BORDER box — `switchTrackVariants`' `h-4.5 w-8` (32×18) and `h-6 w-11` (44×24), thumb `size-3.5`/`size-5` (14/20). The first pass drew 30×16 / 42×22, which is the web's PADDING box: a `BoxDecoration` border in Flutter paints INSIDE the box and does not inset the child, so subtracting it a second time shipped a switch 2 logical px short on both axes in both sizes. The 2px thumb inset is the web's 1px border + 1px inset, which puts the ON thumb at `trackW − 2 − thumb`, the web's `start-3.75` / `start-5.25` measured from the border box.",
      "props": [
        {
          "name": "accessibilityLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The announced name, for when it must differ from the visible text.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isSelected",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether this one is selected."
        },
        {
          "name": "label",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "size",
          "type": "LumoSwitchSize",
          "required": false,
          "default": "LumoSwitchSize.md",
          "description": "The size step, from the shared control scale."
        }
      ]
    },
    "LumoTab": {
      "file": "packages/mobile/lib/src/tabs.dart",
      "doc": "One tab: `id` is the key its panel is matched to; `label` is REQUIRED — the tab's visible text and its announced name (an icon is not a name). `badge` is a pre-formatted String (the app ran `formatNumber`), announced as the tab's value.",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "badge",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A short count or marker shown on the tab."
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading icon. Decorative — it is not announced, so it never carries meaning on its own."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoTable": {
      "file": "packages/mobile/lib/src/table.dart",
      "doc": "A data table that works on a phone — the web `Table` family (`TableHeader`/`Column`/`TableBody`/`Row`/`Cell`) folded into ONE widget, because a phone table is configured once, not composed per call site.\n\nThe API shape is the web's; the mobile PATTERN is the point:\n\n- **The table scrolls, the page does not.** A wide table lives in its own horizontal scroller, clipped (`ClipRect`) and with clamping physics, so the inline drag is consumed here. A page holding this widget never moves sideways, which is the failure every naive Flutter table has: a `Row` wider than the screen either overflows with a yellow stripe or drags the whole page. - **The first column can be pinned** ([pinFirstColumn]). It is pinned by DRAWING it twice: once in the scrolled table, where it holds the layout and owns the semantics, and once as a silent overlay at the inline START (`PositionedDirectional(start: 0)` — so it is on the RIGHT in Persian, with no direction flag). The overlay is `ExcludeSemantics` + `IgnorePointer`, so the reader hears each cell once and a drag that begins over the frozen column still scrolls the table. This is why rows have a fixed [rowHeight]: two copies of a column can only line up if the row's height does not depend on which columns are in it. - **Numeric columns are LTR islands** — see [LumoTableColumn.isNumeric]. - **A narrow viewport can collapse the grid** ([collapseBelow]). forui and shadcn_flutter both leave this to the app; it is opt-in here, off by default, because collapsing silently would change what a caller's screenshots show. Below the given width each row becomes a card of header/value lines. The `table` / `row` / `cell` roles SURVIVE the collapse — it is the same data — but there is no header row, so each cell takes its column's name as its NAME and the datum as its VALUE, which is the pairing `LumoDescriptionList` makes and invents no punctuation to make it.\n\n**Semantics.** `label` is REQUIRED and names the table; `caption` is drawn under it and read as its own node, the web's `<caption>`. The roles are Flutter's own (`SemanticsRole.table` / `row` / `columnHeader` / `cell`), and they come with assertions: a `row` must be a direct child of a `table` and a `cell` a direct child of a `row`. That is why the table role sits INSIDE the scroller rather than around it — the `Scrollable`'s node would otherwise be a child of the table and the framework would throw.\n\n**Not carried from the web.** Sorting, resizing, pinning by column id, row selection, grouping, pagination and the whole TanStack `useLumoTable` layer: they are a desktop data grid, and `data-grid.tsx` is where they live. This widget is `table.tsx`'s presentational half.",
      "props": [
        {
          "name": "columns",
          "type": "List<LumoTableColumn>",
          "required": true,
          "default": null,
          "description": "The columns, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the table, e.g. «تراکنش‌های اخیر». REQUIRED.",
          "announced": true
        },
        {
          "name": "rows",
          "type": "List<LumoTableRowData>",
          "required": true,
          "default": null,
          "description": "The rows, top to bottom."
        },
        {
          "name": "caption",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Drawn under the table and read as its own node — the web's `<caption>`."
        },
        {
          "name": "collapseBelow",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "Below this viewport width, stack each row into a card of header/value lines instead of a grid. Opt-in; `null` never collapses."
        },
        {
          "name": "controller",
          "type": "ScrollController?",
          "required": false,
          "default": null,
          "description": "The horizontal scroller's controller, when the caller wants to drive it."
        },
        {
          "name": "emptyLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown and announced instead of an empty body. Optional; with no string an empty table draws only its header, which is the honest default when the caller has an empty state of their own above it.",
          "announced": true
        },
        {
          "name": "headerHeight",
          "type": "double",
          "required": false,
          "default": "40",
          "description": "Height of the header row, in logical pixels."
        },
        {
          "name": "pinFirstColumn",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Freeze the first column against the inline start while the rest scrolls."
        },
        {
          "name": "rowHeight",
          "type": "double",
          "required": false,
          "default": "44",
          "description": "Every data row is exactly this tall. See the docblock: pinning depends on it, and a phone table with reflowing row heights cannot be scanned."
        }
      ]
    },
    "LumoTableCell": {
      "file": "packages/mobile/lib/src/table.dart",
      "doc": "One cell's content — the same two-arm shape as `LumoDescription`.",
      "props": []
    },
    "LumoTableColumn": {
      "file": "packages/mobile/lib/src/table.dart",
      "doc": "One column — the web `Column`.",
      "props": [
        {
          "name": "header",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The column's announced and drawn name, e.g. «مبلغ». REQUIRED — a table whose columns have no names is a grid of unattributed strings."
        },
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Stable identity. Not announced."
        },
        {
          "name": "align",
          "type": "LumoTableAlign?",
          "required": false,
          "default": null,
          "description": "Defaults to [LumoTableAlign.end] for a numeric column (a money column reads down its last digit) and [LumoTableAlign.start] otherwise."
        },
        {
          "name": "isNumeric",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Marks the column an **LTR island**: its cells are wrapped in their own `Directionality.ltr` so «۱٬۲۰۰٬۰۰۰−» cannot be re-ordered by the paragraph direction around it. The digits stay the reader's (they arrive already formatted); only their RUN is laid out left-to-right, which is what the Unicode bidi algorithm wants for a number and what the Khroos app wrote by hand at every money site."
        },
        {
          "name": "width",
          "type": "double",
          "required": false,
          "default": "120",
          "description": "The column's width in logical pixels. Widths are absolute, not flexible: a phone table scrolls, and a flexible column would collapse to nothing rather than scroll. When the sum FITS the viewport the columns are grown proportionally to fill it, so a two-column table has no dead gap."
        }
      ]
    },
    "LumoTableRowData": {
      "file": "packages/mobile/lib/src/table.dart",
      "doc": "One row — the web `Row`.",
      "props": [
        {
          "name": "cells",
          "type": "List<LumoTableCell>",
          "required": true,
          "default": null,
          "description": "One entry per column, in column order."
        },
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Stable identity. Not announced."
        }
      ]
    },
    "LumoTabs": {
      "file": "packages/mobile/lib/src/tabs.dart",
      "doc": "Tabs — the web `Tabs`/`TabList`/`Tab`/`TabPanel` as one self-contained widget: `label` REQUIRED (an unnamed tab list announces as bare \"tab list\"), the tabs in reading order (a `Row`, so the FIRST tab sits at the RIGHT under fa-IR), the active indicator under the selected tab, moving along the inline axis because each tab paints its own — nothing here is positioned by a physical offset. Controlled (`value` + `onChanged`) or uncontrolled (`defaultValue`, else the first tab — the web derives the same default). Panels come from `views` (by id) or `builder` (the selected id); only the selected panel is built, in an `AnimatedSwitcher` — no `DefaultTabController`, no `TabBarView`, no swipe (a swipe is physical).\n\nSemantics: the list is a `tabBar` group named by `label`; every tab is a `tab` with `selected`, `enabled`, its name announced ONCE (the visible text is excluded); the panel is a `tabPanel`.\n\nMotion: the panel cross-fade, the pill fill and the underline all collapse to `Duration.zero` under `MediaQuery.disableAnimationsOf` — a tab switch under «Reduce motion» is one frame, not a quick one.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the tab list. Required.",
          "announced": true
        },
        {
          "name": "tabs",
          "type": "List<LumoTab>",
          "required": true,
          "default": null,
          "description": "The tabs, in reading order."
        },
        {
          "name": "builder",
          "type": "Widget Function(BuildContext context, String selectedId)?",
          "required": false,
          "default": null,
          "description": "The panel for the selected id, when the panels are not a fixed map."
        },
        {
          "name": "defaultValue",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The selected tab's id (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the whole tab set is disabled."
        },
        {
          "name": "isScrollable",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Scroll the tab list along the inline axis instead of squeezing the tabs."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The selected tab's id (controlled)."
        },
        {
          "name": "variant",
          "type": "LumoTabsVariant",
          "required": false,
          "default": "LumoTabsVariant.underline",
          "description": "The visual variant."
        },
        {
          "name": "views",
          "type": "Map<String, WidgetBuilder>?",
          "required": false,
          "default": null,
          "description": "The panel per tab id."
        }
      ]
    },
    "LumoTagGroup": {
      "file": "packages/mobile/lib/src/chip.dart",
      "doc": "A group of tags — the web `TagGroup`: `label` REQUIRED (names the collection). Removable when told how — `onRemove` and `removeLabel` are one decision (asserted, as the web's union), `removeLabel` a FUNCTION of the tag's own text because Persian word order is not English with the words swapped: `(tag) => 'حذف $tag'`. Static: a plain named list; the reader gets the label once and each tag as text.",
      "props": [
        {
          "name": "items",
          "type": "List<LumoTagItem>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onRemove",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the id of the tag to drop."
        },
        {
          "name": "removeLabel",
          "type": "String Function(String textValue)?",
          "required": false,
          "default": null,
          "description": "Builds the announced name of each tag's remove control from that tag's `textValue`.",
          "announced": true
        },
        {
          "name": "size",
          "type": "LumoChipSize",
          "required": false,
          "default": "LumoChipSize.md",
          "description": "The size step, from the shared control scale."
        }
      ]
    },
    "LumoTagItem": {
      "file": "packages/mobile/lib/src/chip.dart",
      "doc": "One tag of a `LumoTagGroup`: `id` handed back to `onRemove`, `textValue` REQUIRED (the plain text — nothing derives it, so it cannot be empty by accident; also the argument handed to `removeLabel`).",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "textValue",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The text this item is matched and announced by."
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading icon. Decorative — it is not announced, so it never carries meaning on its own."
        }
      ]
    },
    "LumoTagsInput": {
      "file": "packages/mobile/lib/src/tags_input.dart",
      "doc": "A field whose value is a LIST of short strings the reader writes: skills, neighbourhoods, keywords — the web `TagsInput` (`tags-input.tsx`).\n\nThe tags are `LumoChip`s, imported from `chip.dart` rather than redrawn: a tag here and a tag in a `LumoTagGroup` are the same object to a reader, and two implementations of a chip is how they stop being.\n\n**Announced strings, all REQUIRED**: `label` (the field's name), `addLabel` (the ✚ — a glyph is not a name) and `removeLabel`, a FUNCTION of the tag's own text, because «حذف تهران» is not English with the words swapped — the same shape as `LumoTagGroup.removeLabel` and `LumoMultiSelect.removeLabel`.\n\n**Committing a tag** has two doors on a phone, and it needs both: the keyboard's action key (`TextInputAction.done`, which is where a thumb already is) and a real ✚ button (which is where a reader who cannot see the keyboard's action key looks, and the only door a switch user has). The web commits on Enter alone because a physical keyboard is a given there. Pasted or typed text containing one of `splitCharacters` becomes several tags at once, as on the web.\n\n**Duplicates and the cap** are silent on the web — the addition simply does not happen, which on a phone reads as a broken button. So a rejection can say why: `duplicateMessage` and `maxTagsMessage` are optional (nothing is announced if the app has nothing to say) and, when given, are announced as a live region the moment the rejection happens.\n\nSemantics: ONE text-field node named by `label`, carrying `description` and `errorMessage` in its own `hint` — Flutter has no `aria-describedby`, so the field's hint IS the binding (`text_field.dart` states the rule). The chips are their own nodes in a container with no name of its own; the ✚ is a named button; the drawn label, description and error are `ExcludeSemantics`, so each is heard exactly once.\n\nWeb props not carried: `suggestions`/`suggestionsLabel` (an `aria-activedescendant` listbox under an input is a pointer/arrow-key pattern with no phone counterpart — a phone picks from a list in a sheet, which is `LumoMultiSelect`, and offering a half version here would be a second, worse picker) and `name` (nothing is posted by the platform).",
      "props": [
        {
          "name": "addLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the ✚ that commits the draft. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced (and, unless `showLabel` is false, displayed) name. REQUIRED.",
          "announced": true
        },
        {
          "name": "removeLabel",
          "type": "String Function(String tag)",
          "required": true,
          "default": null,
          "description": "Builds each chip's remove-button name from that chip's own text. REQUIRED.",
          "announced": true
        },
        {
          "name": "values",
          "type": "List<String>",
          "required": true,
          "default": null,
          "description": "The tags, controlled — the web's `value`. The widget holds only the draft."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Help text — drawn under the field and carried in its `hint`.",
          "announced": true
        },
        {
          "name": "duplicateMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced when an addition is refused because the tag is already there."
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The error — drawn under the field, carried in its `hint`, and it marks the box invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isDuplicate",
          "type": "bool Function(String candidate, List<String> current)?",
          "required": false,
          "default": null,
          "description": "Decides whether a candidate is already present. Default: exact match. A Persian app usually wants `lumoFoldForSearch` on both sides."
        },
        {
          "name": "maxTags",
          "type": "int?",
          "required": false,
          "default": null,
          "description": "Upper bound on how many tags may exist; further additions are refused."
        },
        {
          "name": "maxTagsMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Announced when an addition is refused because [maxTags] is reached."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<List<String>>?",
          "required": false,
          "default": null,
          "description": "Called with the WHOLE list after every addition or removal."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the empty draft box.",
          "announced": true
        },
        {
          "name": "showLabel",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "Hides the label row; the name stays on the field."
        },
        {
          "name": "splitCharacters",
          "type": "List<String>",
          "required": false,
          "default": "const [',']",
          "description": "Characters that split typed or pasted text into separate tags."
        }
      ]
    },
    "LumoTextArea": {
      "file": "packages/mobile/lib/src/text_area.dart",
      "doc": "The multi-line box — the counterpart of the web `TextArea`, in `LumoTextField`'s visual language (same decoration theme, same label / description / error rows): `label` REQUIRED (visible, and the field's name), a MINIMUM of `minLines` that grows to `maxLines` (null = unbounded), and an optional `maxLength` whose counter is rendered through `formatNumber` — Material's own counter prints raw «12/200», so `buildCounter` is owned here (Persian digits under `fa-*`). Text aligns to the reading start by `Directionality`. Semantics: ONE text-field node named by `label` (`MergeSemantics`) carrying `currentValueLength`/`maxValueLength`, plus the `invalid` and `required` STATES the web sets on the control (`isInvalid` is the web's own prop, separate from `errorMessage`); the painted counter, the painted label and the painted description are decoration and excluded — each of the three already reaches the reader through the field's own node, and without the exclusion every one of them was heard TWICE.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "autoFocus",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control takes focus when it first appears."
        },
        {
          "name": "controller",
          "type": "TextEditingController?",
          "required": false,
          "default": null,
          "description": "An optional controller: supply one to read or drive the value from outside, or omit it and the widget owns its own."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error message for the field. Announced as the control's hint and marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isInvalid",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Marks the box wrong WITHOUT a sentence (the web's `isInvalid`). Null derives it from `errorMessage`."
        },
        {
          "name": "isReadOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether user input is required before the form is submitted."
        },
        {
          "name": "maxLength",
          "type": "int?",
          "required": false,
          "default": null,
          "description": "Enforced limit; when set, a counter «۱۲/۲۰۰» in the reader's digits sits under the box."
        },
        {
          "name": "maxLines",
          "type": "int?",
          "required": false,
          "default": null,
          "description": "The box stops growing here and scrolls; null grows without bound."
        },
        {
          "name": "minLines",
          "type": "int",
          "required": false,
          "default": "4",
          "description": "Visible lines before the box grows. The web `TextArea`'s `rows`, which defaults to FOUR (`text-area.tsx`); this used to say three and default to three, which was drift against the reference, not a mobile decision."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Placeholder text shown while the field is empty. Never a substitute for the label.",
          "announced": true
        }
      ]
    },
    "LumoTextField": {
      "file": "packages/mobile/lib/src/text_field.dart",
      "doc": "A labelled text field: `label` REQUIRED (visible, and the field's name), `description` under it, `errorMessage` announced, text aligned to the reading start by `Directionality`. On Material's `TextField`.\n\n`isNumeric`: the value is digits (a phone number, a code, an amount) — it is laid out left-to-right inside an RTL form and gets a numeric keyboard. A data-type fact, not a direction flag (there is no `dir` anywhere in Lumo).\n\n**State reaches the reader as STATE, not only as words.** `isInvalid` is the web's own prop (`TextFieldProps.isInvalid`, separate from `errorMessage`, so a form can mark a field wrong before it has a sentence for it) and lands on the node as `SemanticsValidationResult.invalid`; `isRequired` lands as `SemanticsFlag.isRequired`. The web carries required on the native `required` attribute and paints no marker — the « *» drawn here is a mobile addition, kept because a phone form has no other affordance for it.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "autofillHints",
          "type": "Iterable<String>?",
          "required": false,
          "default": null,
          "description": "Autofill hints for the platform's keyboard and password manager."
        },
        {
          "name": "autofocus",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control takes focus when it first appears."
        },
        {
          "name": "controller",
          "type": "TextEditingController?",
          "required": false,
          "default": null,
          "description": "An optional controller: supply one to read or drive the value from outside, or omit it and the widget owns its own."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "An error message for the field. Announced as the control's hint and marks it invalid.",
          "announced": true
        },
        {
          "name": "focusNode",
          "type": "FocusNode?",
          "required": false,
          "default": null,
          "description": "An optional focus node, for callers that manage focus themselves."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isInvalid",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Marks the field wrong WITHOUT a sentence (the web's `isInvalid`). Null derives it from `errorMessage`, which is what a message alone already means."
        },
        {
          "name": "isNumeric",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the field holds a number, so its digits stay LTR inside RTL text."
        },
        {
          "name": "isReadOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether user input is required before the form is submitted."
        },
        {
          "name": "keyboardType",
          "type": "TextInputType?",
          "required": false,
          "default": null,
          "description": "Which keyboard the platform should offer."
        },
        {
          "name": "maxLines",
          "type": "int?",
          "required": false,
          "default": "1",
          "description": "How many lines the text may occupy before it wraps no further."
        },
        {
          "name": "minLines",
          "type": "int?",
          "required": false,
          "default": null,
          "description": "The fewest lines the field occupies before it grows."
        },
        {
          "name": "obscureText",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the text is masked, as for a password."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called with the new value when the user changes it. Omitting it makes the control read-only."
        },
        {
          "name": "onSubmitted",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "Called when the reader submits from the keyboard."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Placeholder text shown while the field is empty. Never a substitute for the label.",
          "announced": true
        },
        {
          "name": "prefix",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A glyph at the inline START of the box (decorative — the label names the field)."
        },
        {
          "name": "showLabel",
          "type": "bool",
          "required": false,
          "default": "true",
          "description": "The label is always the field's NAME; `showLabel: false` hides it visually (a search box with a placeholder) — the name is still announced."
        },
        {
          "name": "suffix",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A widget at the inline END of the box (a named button, a unit…)."
        },
        {
          "name": "textInputAction",
          "type": "TextInputAction?",
          "required": false,
          "default": null,
          "description": "What the keyboard's action key does."
        }
      ]
    },
    "LumoTimeField": {
      "file": "packages/mobile/lib/src/time_field.dart",
      "doc": "A time behind a button — the mobile counterpart of the web `TimeField`.\n\nThe web types a time segment by segment (`DateInput` over `useTimeFieldState`); a phone keyboard has no segment navigation, so the field here is READ-ONLY and the value is picked from columns, the shape the Khroos app reached by hand (`_TimePicker`). Everything else is the web's: the hour cycle is the LOCALE's decision, the digits are the reader's.\n\n**Not Material's `showTimePicker`.** Its every string comes from `MaterialLocalizations` — English on any app that has not added the Persian delegate, and unreachable by a parameter either way — and its dial is Material's design, not Lumo's. The picker is a `showLumoSheet` route.\n\nAnnounced strings, all REQUIRED: `label` (the field's name), `openLabel` (the picker button is an icon), `closeLabel` (the sheet's ✕ and scrim), `hourLabel` and `minuteLabel` (the columns' headers). The 12-hour day period is NOT a parameter: its two words are read out of `intl`'s `DateSymbols.AMPMS` for the locale — derived, like the web reads them from `Intl.formatToParts`, so «ق.ظ.»/«ب.ظ.» are never English by default.\n\n`use24Hour` defaults to what the LOCALE does — `lumoLocaleUses24Hour`, which asks `intl` for the locale's own `jm` pattern (`fa` → `H:mm`, so Persian is 24-hour; `en-US` → `h:mm a`). It is never defaulted to the English convention, and a caller may still override it: an hour cycle is a user-visible convention, exactly as on the web.\n\nThe formatted time is an LTR island: a clock time reads left-to-right in every script (the same data-type rule as `LumoTextField(isNumeric:)`), while the box itself stays at the reading start.\n\n`isInvalid` and `isRequired` are the web `TimeField`'s own props. The web `TimeField` is the ONE component of the family that emits `aria-required` (`time-field.tsx`, because its segment group has no native control to carry `required`); here both land on the field's node as real state — `SemanticsFlag.isRequired` and `SemanticsValidationResult.invalid` — so a reader is told the field is wrong, not only read the sentence saying so.\n\nThe picker's cells keep their 40-px DRAWING and take 44 of HIT AREA: the 4-px gap between rows moved inside the tap surface, so the column's extent per row is the same 44 its scroll arithmetic already assumed.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the sheet's ✕ and of its scrim. REQUIRED.",
          "announced": true
        },
        {
          "name": "hourLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Header of the hour column. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name. REQUIRED — an unnamed field is a defect.",
          "announced": true
        },
        {
          "name": "minuteLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Header of the minute column. REQUIRED.",
          "announced": true
        },
        {
          "name": "openLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the button that opens the picker. REQUIRED — it is an icon.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the field and announced. Supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isInvalid",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Marks the field wrong WITHOUT a sentence (the web's `isInvalid`). Null derives it from `errorMessage`."
        },
        {
          "name": "isRequired",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "The web's `isRequired`: draws the « *» marker and sets the reader's `required` state."
        },
        {
          "name": "minuteStep",
          "type": "int",
          "required": false,
          "default": "15",
          "description": "How far apart the offered minutes are. Must divide the hour."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<TimeOfDay>?",
          "required": false,
          "default": null,
          "description": "Called with the time after every pick. A pick on one column completes the other with zero, so a half-made time is never reported."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the field while there is no value.",
          "announced": true
        },
        {
          "name": "use24Hour",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Overrides the locale's own clock. Null derives it (`lumoLocaleUses24Hour`)."
        },
        {
          "name": "value",
          "type": "TimeOfDay?",
          "required": false,
          "default": null,
          "description": "The chosen time. Controlled; `null` is an empty field."
        }
      ]
    },
    "LumoTimeline": {
      "file": "packages/mobile/lib/src/timeline.dart",
      "doc": "A sequence of EVENTS down a rail — the web `Timeline`: an order's history, an audit trail, a job's stages.\n\n**Timeline is not Steps.** `LumoSteps` is where you are in a sequence you have to FINISH — a wizard's progress, numbered discs, one `current` index, and every step before it complete by definition. A timeline is a RECORD of what happened: each item carries its own `state`, the order is the information (the web renders an `<ol>` for exactly that reason), items are not numbered, and there may be no current item at all. Same distinction as the web's two registry entries — «Where you are in a sequence you have to finish» against «A sequence of events down a rail». Pick Steps for a form, Timeline for a history.\n\n`label` is REQUIRED (the list's announced name; the web `<ol>` takes its name from the surrounding page, mobile has no such page), and so are the three STATE WORDS — `doneLabel`, `currentLabel`, `upcomingLabel` — announced after each title, so colour is never the sole carrier (WCAG 1.4.1). This is the rule `steps.dart` states and the same implementation: a semantics-only node after the title, never a label that would REPLACE it.\n\nDirection: the rail, the dots and the text all sit on the INLINE axis — vertically the dot column is at the reading start (right under fa-IR), horizontally the first event is at the reading start, because a `Row` mirrors itself. Nothing is placed by a physical offset.\n\nSemantics: a `list` named by `label`; each item a `listItem` whose node is title, state word, description, meta — MERGED, so each string is heard exactly once; the dot, its icon and the connectors are decoration.\n\nWeb props not carried: the per-item `tone` (`neutral | accent | positive | critical`) — on mobile the dot and the rail take their colour from `state`, which is the axis that has a required WORD behind it; a second, wordless colour axis would be exactly the \"colour is the only carrier\" defect the state labels exist to prevent. `marker` is `icon`, `TimelineTime`'s `dateTime` (the machine-readable ISO stamp) has no mobile counterpart — there is no `<time>` element for a reader to parse.",
      "props": [
        {
          "name": "currentLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced state of the event in progress, e.g. «در جریان». Required.",
          "announced": true
        },
        {
          "name": "doneLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced state of an event that happened, e.g. «انجام‌شده». Required.",
          "announced": true
        },
        {
          "name": "items",
          "type": "List<LumoTimelineItem>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The announced name of the sequence, e.g. «تاریخچهٔ سفارش». Required.",
          "announced": true
        },
        {
          "name": "upcomingLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced state of an event not yet reached, e.g. «در انتظار». Required.",
          "announced": true
        },
        {
          "name": "orientation",
          "type": "LumoTimelineOrientation",
          "required": false,
          "default": "LumoTimelineOrientation.vertical",
          "description": "Which axis the control runs along."
        }
      ]
    },
    "LumoTimelineItem": {
      "file": "packages/mobile/lib/src/timeline.dart",
      "doc": "One event: `title` REQUIRED, `description` the sentence under it, `meta` the already-formatted stamp — «۱۹ مرداد ۱۴۰۵», a STRING the caller built with `formatLumoDate`/`formatNumber`, because the caller holds the locale (the web `TimelineTime` takes the same already-formatted string, and for the same reason). `icon` is what sits IN the dot: decorative.",
      "props": [
        {
          "name": "title",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The visible title.",
          "announced": true
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "The dot's glyph. DECORATIVE — the title says what happened."
        },
        {
          "name": "meta",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The pre-formatted time or place stamp. Never a `DateTime`, never a number."
        },
        {
          "name": "state",
          "type": "LumoTimelineState",
          "required": false,
          "default": "LumoTimelineState.done",
          "description": "Where this item sits in the sequence: done, current, or still to come."
        }
      ]
    },
    "LumoToggle": {
      "file": "packages/mobile/lib/src/toggle.dart",
      "doc": "ONE two-state button — the web `Toggle`/`IconToggle`. Bold on / bold off, muted / unmuted, pinned / unpinned.\n\n**This is a button, not a switch.** The state is announced as `toggled` (Flutter's counterpart of `aria-pressed`), which is what a pressed BUTTON carries; a `LumoSwitch` announces a switch's on/off, and the two are not interchangeable to a reader — a switch takes effect the moment it moves and describes a SETTING, a toggle button describes the state of the thing it acts on and belongs beside other buttons. Choosing the wrong one is silent on screen and wrong in the ear.\n\n`label` is REQUIRED — the web splits `IconToggle` out of `Toggle` for exactly this, since a nameless toggle announces \"button, pressed\", which sounds like information and is not. **The name does not change with the state** («بی‌صدا», never «بی‌صدا کردن»): `toggled` carries the state, and a voice-control user must be able to say the same name twice. `iconOnly` draws only the icon and keeps the name for the reader.\n\nControlled (`isSelected`) and uncontrolled (`defaultSelected`), as on the web. No press feedback of its own: a toggle's press CHANGES ITS STATE, so the tap answers itself — that is the web's rule too. The fill cross-fade collapses to nothing under `disableAnimations`, so under «Reduce motion» the state change is instant, not merely faster.",
      "props": [
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The announced name — and the visible text unless `iconOnly`. Required.",
          "announced": true
        },
        {
          "name": "defaultSelected",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the toggle is on to begin with (uncontrolled)."
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A glyph beside the words, or the whole content with `iconOnly`."
        },
        {
          "name": "iconOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Draw only the icon; the label stays for the reader."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "isSelected",
          "type": "bool?",
          "required": false,
          "default": null,
          "description": "Whether the toggle is on (controlled); `null` leaves the state here."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<bool>?",
          "required": false,
          "default": null,
          "description": "Called with the NEXT on-state."
        },
        {
          "name": "size",
          "type": "LumoToggleSize",
          "required": false,
          "default": "LumoToggleSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "variant",
          "type": "LumoToggleVariant",
          "required": false,
          "default": "LumoToggleVariant.ghost",
          "description": "The visual variant."
        }
      ]
    },
    "LumoToggleGroup": {
      "file": "packages/mobile/lib/src/toggle.dart",
      "doc": "A strip of two-state buttons — the web `ToggleButtonGroup`. `label` is REQUIRED because the engine renders `role=\"group\"`, which names nothing.\n\nA member's ON state is the SOLID accent (`accent` / `accentFg`), not the standalone toggle's 10% tint — that difference is the web's, and it is what makes a strip read as a strip. Each member is still announced with `toggled`, never `selected`: Base UI emits `aria-pressed` in single AND multiple mode, which is precisely why the web's `SegmentedControl` moved off this family onto a radio group. Reach for `LumoSegmentedControl` when the options are mutually exclusive VIEWS of the same thing and for this when they are independent switches that happen to sit together.\n\nThe rounding lives on the GROUP, never on the first and last children, which would round the wrong corners under RTL; the dividers are inline-start borders, so they land between the right pair in either direction.\n\nSelection is one `Set<String>` in both modes, as on the web (`selectedKeys`); `value`/`defaultValue` are the single-mode conveniences and the constructor refuses them alongside `values`/`defaultValues` or in multiple mode. `onChanged` is handed the whole next set.\n\nA cramped strip **sheds decoration before it truncates words** — the house `_fit` pattern: padding first (down to 4), then the icons, and only a bare label that still will not fit ellipsizes.\n\nThe member fill cross-fade collapses to nothing under `disableAnimations`.",
      "props": [
        {
          "name": "items",
          "type": "List<LumoToggleItem>",
          "required": true,
          "default": null,
          "description": "The items to show, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the group, e.g. «چیدمان». Required.",
          "announced": true
        },
        {
          "name": "defaultValue",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The pressed key in single mode (uncontrolled)."
        },
        {
          "name": "defaultValues",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The pressed keys (uncontrolled) — the web's `defaultSelectedKeys`."
        },
        {
          "name": "disallowEmptySelection",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Refuse to empty the group — the web prop of the same name."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<Set<String>>?",
          "required": false,
          "default": null,
          "description": "Called with the whole next selection."
        },
        {
          "name": "selectionMode",
          "type": "LumoToggleSelectionMode",
          "required": false,
          "default": "LumoToggleSelectionMode.single",
          "description": "Whether one item may be selected, or many."
        },
        {
          "name": "size",
          "type": "LumoToggleSize",
          "required": false,
          "default": "LumoToggleSize.md",
          "description": "The size step, from the shared control scale."
        },
        {
          "name": "value",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "The pressed key in single mode (controlled)."
        },
        {
          "name": "values",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The pressed keys (controlled) — the web's `selectedKeys`."
        }
      ]
    },
    "LumoToggleItem": {
      "file": "packages/mobile/lib/src/toggle.dart",
      "doc": "One member of a `LumoToggleGroup`: `id` REQUIRED (the key the group reports), `label` REQUIRED (the announced name — an icon is not a name).",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "A stable identity for this item — used for selection and equality, never shown or announced."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The name this control is announced by, and painted where the family shows one.",
          "announced": true
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "A leading icon. Decorative — it is not announced, so it never carries meaning on its own."
        },
        {
          "name": "iconOnly",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether only the icon is painted. The label is still announced."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoToolbar": {
      "file": "packages/mobile/lib/src/toolbar.dart",
      "doc": "A group of related controls — `packages/ui/src/toolbar.tsx`.\n\nLumoToolbar(label: 'قالب‌بندی متن', children: [ LumoToggle(label: 'پررنگ', …), LumoToolbarSeparator(), LumoIconButton(label: 'پیوند', …), ])\n\n`label` is REQUIRED for the same reason as on the web: a toolbar is ONE named group and an unnamed one announces as bare \"toolbar\". (Flutter's `SemanticsRole` has no `toolbar` member in 3.35, so the group is a named semantics container with explicit child nodes — the controls keep their own names and roles under it.)\n\nThe web component's other job — a roving tabindex, with `ToolbarItem` as the registry that Base UI's composite needs — has no counterpart here and is DELIBERATELY not carried: Flutter's focus traversal already walks the children in tree order, which is reading order, and mirrors with the `Directionality` the `Row` reads. There is nothing for a child to enrol in, so there is no `LumoToolbarItem`.\n\nA cramped toolbar sheds nothing and truncates nothing: with more controls than room it SCROLLS (`LumoToolbarOverflow.scroll`, the default) or flows onto another line (`wrap`). Both keep every control reachable, which is the house rule (`segmented_control.dart` `_fit()`) applied to a container whose children are opaque widgets it may not restyle. The pattern is forui's and Material's: a control strip that runs off the edge is a scroll view, not an ellipsis.",
      "props": [
        {
          "name": "children",
          "type": "List<Widget>",
          "required": true,
          "default": null,
          "description": "The children, in reading order."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the group. Required.",
          "announced": true
        },
        {
          "name": "orientation",
          "type": "LumoToolbarOrientation",
          "required": false,
          "default": "LumoToolbarOrientation.horizontal",
          "description": "Which axis the control runs along."
        },
        {
          "name": "overflow",
          "type": "LumoToolbarOverflow",
          "required": false,
          "default": "LumoToolbarOverflow.scroll",
          "description": "What happens to items that do not fit."
        },
        {
          "name": "spacing",
          "type": "double",
          "required": false,
          "default": "4",
          "description": "The gap between controls, on the toolbar's own axis."
        }
      ]
    },
    "LumoToolbarSeparator": {
      "file": "packages/mobile/lib/src/toolbar.dart",
      "doc": "The hairline between two groups of controls — the web's `ToolbarSeparator`. Decoration: excluded from semantics. Its own axis is the toolbar's PERPENDICULAR, derived from the enclosing `LumoToolbar` exactly as the web derives `aria-orientation` from the engine's `data-orientation`; pass `orientation` only when the separator stands outside a toolbar.",
      "props": [
        {
          "name": "orientation",
          "type": "LumoToolbarOrientation?",
          "required": false,
          "default": null,
          "description": "The enclosing toolbar's axis, when there is no toolbar to read it from."
        }
      ]
    },
    "LumoTooltip": {
      "file": "packages/mobile/lib/src/tooltip.dart",
      "doc": "A description shown on long-press (touch) or hover (pointer), on Material's `Tooltip` wearing the tokens: the INVERTED surface (`bg` text on `fg`), as the web tooltip in both schemes. `message` REQUIRED. A tooltip is a DESCRIPTION, never a name: Flutter's semantics has a dedicated `tooltip` field, read after the child's own name (the web's `aria-describedby`) — so an icon-only child still needs its own `label` (`LumoIconButton`), and this tooltip is NOT excluded from semantics (`LumoIconButton` excludes its own, where the message would only repeat the name).\n\n**Motion is the one thing this file cannot honour.** Material's `Tooltip` fades the tip in and out over durations it holds privately (`waitDuration`, `showDuration` and `exitDuration` are the only `Duration`s it accepts, and all three are DELAYS, not the fade), and it never reads `MediaQuery.disableAnimationsOf` itself — so under «reduce motion» the tip still fades over ~150ms. No parameter of ours reaches it; the alternative is re-implementing an overlay tooltip, which would lose Material's dismissal and positioning. Recorded here rather than papered over. Everything else in this library collapses its duration to zero (`disclosure.dart`, `carousel.dart`, `dialog.dart`, `sheet.dart`, `popover.dart`).\n\nThe long-press is Lumo's own recognizer (Material's `triggerMode` is `manual`): `LumoIconButton` already carries a `Tooltip` that shows its label on long-press, and nested tooltips both race for the same gesture — the inner one wins a tie. This recognizer fires 100ms before Material's `kLongPressTimeout`, so the more specific description wins; the tip then stays 1.5s after the finger lifts (Material's own `showDuration`) or until the next touch anywhere. `delay` is the web's `delay` (hover `waitDuration`); `isDisabled` renders the child alone. No `closeDelay` / `shouldCloseOnPress`: Material has no knob with the same meaning, so none is spelled as if it were.",
      "props": [
        {
          "name": "child",
          "type": "Widget",
          "required": true,
          "default": null,
          "description": "The widget this one wraps."
        },
        {
          "name": "message",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The tip's text.",
          "announced": true
        },
        {
          "name": "delay",
          "type": "Duration?",
          "required": false,
          "default": null,
          "description": "How long a press is held before the tip appears."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "placement",
          "type": "LumoTooltipPlacement",
          "required": false,
          "default": "LumoTooltipPlacement.top",
          "description": "Which side the surface prefers to open on."
        }
      ]
    },
    "LumoTree": {
      "file": "packages/mobile/lib/src/tree.dart",
      "doc": "A nested outline — the web `Tree` (`packages/ui/src/tree.tsx`).\n\nLumoTree( label: 'محدودهٔ جغرافیایی', nodes: provinces, defaultExpandedIds: const {'thr'}, itemLabel: (p) => '${p.label}، سطح ${p.level}، ${p.position} از ${p.setSize}', expandLabel: (name) => 'باز کردن $name', collapseLabel: (name) => 'بستن $name', )\n\n**Shape.** The web tree is a `role=\"treegrid\"` with a roving tab stop, arrow keys and typeahead — a keyboard model. A phone has no arrow keys and no typeahead buffer, so what carries over is the STRUCTURE, not the keyboard: rows in a flat reading order, each announcing its own depth and place, a branch that opens and closes, and a marker that points the right way. The interaction is the mobile one (Material's `ExpansionTile`, forui's accordion): the whole row is the target, with the marker as a second, smaller one for a tree that also selects. Lost from the web on purpose: arrow-key navigation, typeahead, `disallowEmptySelection`, async status — each of them a keyboard or a desktop-loading concern with no phone form.\n\n**The children of a collapsed branch are not built** — out of the widget tree and out of the semantics tree, as Base UI's tree does by default.\n\n**Semantics.** Flutter 3.35 has no `tree`/`treeItem` role (and the roles it does declare for tabular structures are `_unimplemented` and throw from their own debug validator), so the tree is a named `SemanticsRole.list` of `listItem` rows — the structure a reader is actually offered. Each row is ONE node: a button, `hasExpandedState` + `isExpanded` when it is a branch (Flutter's `expanded:`, the counterpart of `aria-expanded`), its selection or checked state, enabled, and named by `itemLabel` — which is where LEVEL and POSITION-IN-SET are announced, because Flutter's semantics has no `aria-level`/`aria-posinset` field of its own. The visible label is inside `ExcludeSemantics` so the name is heard ONCE; the marker is a child node with its own name (`expandLabel`/`collapseLabel`), which is what lets a reader open a branch without selecting it.\n\nExpansion is controlled with `expandedIds` + `onExpandedChanged` and uncontrolled with `defaultExpandedIds`; selection the same way — both pairs exactly as the web has both.",
      "props": [
        {
          "name": "collapseLabel",
          "type": "String Function(String nodeLabel)",
          "required": true,
          "default": null,
          "description": "Name of the marker that CLOSES a branch. REQUIRED.",
          "announced": true
        },
        {
          "name": "expandLabel",
          "type": "String Function(String nodeLabel)",
          "required": true,
          "default": null,
          "description": "Name of the marker that OPENS a branch, built from that branch's own text («باز کردن تهران»). REQUIRED — a chevron is not a name.",
          "announced": true
        },
        {
          "name": "itemLabel",
          "type": "LumoTreeItemLabel",
          "required": true,
          "default": null,
          "description": "What each row announces, built from its place in the tree. REQUIRED.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The tree's announced name. REQUIRED — a list names nothing by itself.",
          "announced": true
        },
        {
          "name": "nodes",
          "type": "List<LumoTreeNode>",
          "required": true,
          "default": null,
          "description": "The tree's roots, in reading order."
        },
        {
          "name": "defaultExpandedIds",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The open branch ids at first build (uncontrolled)."
        },
        {
          "name": "defaultSelectedIds",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The selected row ids at first build (uncontrolled)."
        },
        {
          "name": "emptyLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What a reader is told when there is nothing in the tree. Announced as the tree node's value and drawn in its place; omit it and an empty tree draws nothing at all.",
          "announced": true
        },
        {
          "name": "expandedIds",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The open branch ids (controlled)."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "onActivate",
          "type": "ValueChanged<String>?",
          "required": false,
          "default": null,
          "description": "A row was activated — the web's `onAction`. Only reached when the tree does not select and the row is a leaf, so an activation is never also a selection or an expansion."
        },
        {
          "name": "onExpandedChanged",
          "type": "ValueChanged<Set<String>>?",
          "required": false,
          "default": null,
          "description": "Called with the full set of expanded ids."
        },
        {
          "name": "onSelectionChanged",
          "type": "ValueChanged<Set<String>>?",
          "required": false,
          "default": null,
          "description": "Called with the full set of selected ids."
        },
        {
          "name": "selectedIds",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The selected row ids (controlled)."
        },
        {
          "name": "selectionMode",
          "type": "LumoTreeSelectionMode",
          "required": false,
          "default": "LumoTreeSelectionMode.none",
          "description": "Whether one item may be selected, or many."
        }
      ]
    },
    "LumoTreeNode": {
      "file": "packages/mobile/lib/src/tree.dart",
      "doc": "One node of a `LumoTree` — the web `TreeItem`'s `id` + `textValue`/`title`, as data rather than a child element, because on a phone a hierarchy is fetched and configured, not written out by hand a hundred rows deep.",
      "props": [
        {
          "name": "id",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The stable key reported through `onExpandedChanged` / `onSelectionChanged`. A key, never a rendered string."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "The row's visible text AND its announced name, in one string, so seen and announced cannot drift (the `LumoDisclosureItem.title` rule). REQUIRED.",
          "announced": true
        },
        {
          "name": "children",
          "type": "List<LumoTreeNode>",
          "required": false,
          "default": "const <LumoTreeNode>[]",
          "description": "The children, in reading order."
        },
        {
          "name": "icon",
          "type": "Widget?",
          "required": false,
          "default": null,
          "description": "Decoration beside the name. Excluded from semantics — an icon is not a name."
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        }
      ]
    },
    "LumoTreeSelect": {
      "file": "packages/mobile/lib/src/tree.dart",
      "doc": "See [LumoTreeSelectMode] for the field's documentation.",
      "props": [
        {
          "name": "closeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the sheet's ✕ and of its scrim. REQUIRED.",
          "announced": true
        },
        {
          "name": "collapseLabel",
          "type": "String Function(String nodeLabel)",
          "required": true,
          "default": null,
          "description": "Name of the marker that closes a branch. REQUIRED.",
          "announced": true
        },
        {
          "name": "confirmLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Name of the footer's primary button, which closes the sheet. REQUIRED.",
          "announced": true
        },
        {
          "name": "expandLabel",
          "type": "String Function(String nodeLabel)",
          "required": true,
          "default": null,
          "description": "Name of the marker that opens a branch. REQUIRED.",
          "announced": true
        },
        {
          "name": "itemLabel",
          "type": "LumoTreeItemLabel",
          "required": true,
          "default": null,
          "description": "What each row of the tree announces. REQUIRED — see `LumoTree`.",
          "announced": true
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced and displayed name of the field, and the sheet's title. REQUIRED.",
          "announced": true
        },
        {
          "name": "nodes",
          "type": "List<LumoTreeNode>",
          "required": true,
          "default": null,
          "description": "The tree's roots, in reading order."
        },
        {
          "name": "treeLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the TREE inside the sheet — the web's `treeLabel`. REQUIRED and separate from `label`: `label` names the field a reader is standing on, this names the outline they have just been handed, and one string doing both jobs puts the same name on two different things.",
          "announced": true
        },
        {
          "name": "values",
          "type": "List<String>",
          "required": true,
          "default": null,
          "description": "The chosen ids. Controlled, as the web's `value`."
        },
        {
          "name": "defaultExpandedIds",
          "type": "Set<String>?",
          "required": false,
          "default": null,
          "description": "The branches open when the sheet is first shown."
        },
        {
          "name": "description",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "A description for the field, shown under the control and announced as its hint.",
          "announced": true
        },
        {
          "name": "emptyLabel",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "What a reader is told when the tree has nothing in it.",
          "announced": true
        },
        {
          "name": "errorMessage",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown under the field and announced. Supplying one marks it invalid.",
          "announced": true
        },
        {
          "name": "isDisabled",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the control is disabled."
        },
        {
          "name": "mode",
          "type": "LumoTreeSelectMode",
          "required": false,
          "default": "LumoTreeSelectMode.single",
          "description": "Whether one node may be selected, or many."
        },
        {
          "name": "onChanged",
          "type": "ValueChanged<List<String>>?",
          "required": false,
          "default": null,
          "description": "Called with the full id list after every change."
        },
        {
          "name": "placeholder",
          "type": "String?",
          "required": false,
          "default": null,
          "description": "Shown in the trigger while nothing is chosen.",
          "announced": true
        }
      ]
    },
    "LumoVirtualList": {
      "file": "packages/mobile/lib/src/virtual_list.dart",
      "doc": "A long list that builds only what is on screen — the web `VirtualList`.\n\n**What is and is not being re-invented.** `virtual-list.tsx` owns arithmetic (`virtualizer.ts`) because the DOM has no windowing: a `<div>` with ten thousand children IS ten thousand elements. Flutter's sliver protocol is already lazy, so there is nothing to compute here and this file makes no attempt to. What it carries across is the CONTRACT the web file states in its own docblock — a named list, a total, a per-item position, one required empty message — plus the three `ListView` settings that people get wrong:\n\n- **Keys, and the half of them everyone forgets.** Without [itemKey] a row's `Element` is identified by its SLOT, so inserting at the top makes every row inherit the state of the row below it (a half-typed field, an open expander, a switch mid-animation). [itemKey] alone fixes the wrong half of that: a `KeyedSubtree` whose key changed cannot be updated in place, so the row is rebuilt and its state is DISCARDED — correct, but lost. A sliver does not move elements by key the way a `Row` or `Column` does; it needs [findItemIndex], which is what lets it relocate the existing element to its new slot and carry the state with it. Two props, three behaviours, and `virtual_list_test.dart` pins all three. - **Extent.** [estimatedItemExtent] mirrors the web's `estimateSize`, but say plainly what it becomes: Flutter's `itemExtent`, which is a CONTRACT, not a hint — every row is laid out at exactly that height. Pass it when the rows really are uniform (the Khroos bench's 2,000 rows are 64 px) and scroll-offset arithmetic becomes O(1); omit it for variable rows, because a wrong number clips content rather than estimating it. - **Keep-alives.** `addAutomaticKeepAlives` is FALSE by default here where `ListView` defaults it true: a keep-alive holds every row that ever asked for one for the life of the list, which on a feed is the whole corpus and defeats the point. Turn it on ([keepAlive]) only for rows that own real state — a playing video, a map.\n\n**Position in a set.** The web emits `aria-setsize`/`aria-posinset` on every row. Flutter's `Semantics` widget has no such properties; its counterpart is the SLIVER's own semantic index — `SliverChildBuilderDelegate` stamps each child with an index and reports [itemCount] as the scrollable's `scrollChildCount`, which is what the platforms map to Android's `CollectionInfo`/`CollectionItemInfo` and to iOS's ordinal announcements. So the total is carried on the SCROLLABLE's node, not on each row, and it is a raw integer — a Persian digit in a set size is not a value, it is a crash waiting for a parser. `virtual_list_test.dart` pins both.\n\n**The list role.** `SemanticsRole.list` names the container. Rows do NOT get `SemanticsRole.listItem`: Flutter asserts that a `listItem`'s DIRECT parent carries `list`, and the `Scrollable`'s own node always sits between them, so the role would turn every scrolling list in the app into a crash. The role is on the container, the position comes from the sliver, and nothing lies.",
      "props": [
        {
          "name": "emptyLabel",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Shown and announced when [itemCount] is zero. REQUIRED — \"nothing here\" is a sentence in the reader's language, never an empty box.",
          "announced": true
        },
        {
          "name": "itemBuilder",
          "type": "Widget Function(BuildContext context, int index)",
          "required": true,
          "default": null,
          "description": "Builds one row. Called with the row's TRUE index."
        },
        {
          "name": "itemCount",
          "type": "int",
          "required": true,
          "default": null,
          "description": "How many rows exist in the corpus, not how many are built."
        },
        {
          "name": "label",
          "type": "String",
          "required": true,
          "default": null,
          "description": "Announced name of the list, e.g. «فهرست سفارش‌ها». REQUIRED.",
          "announced": true
        },
        {
          "name": "controller",
          "type": "ScrollController?",
          "required": false,
          "default": null,
          "description": "An optional controller: supply one to read or drive the value from outside, or omit it and the widget owns its own."
        },
        {
          "name": "estimatedItemExtent",
          "type": "double?",
          "required": false,
          "default": null,
          "description": "The exact extent of every row along the scroll axis. See the docblock: on Flutter this is not an estimate."
        },
        {
          "name": "findItemIndex",
          "type": "int? Function(Object key)?",
          "required": false,
          "default": null,
          "description": "The inverse of [itemKey], when the caller can answer it in O(1). Wired to `findChildIndexCallback`, which is the ONLY thing that lets a sliver re-use an existing element for a row that MOVED rather than rebuilding it — see the docblock. Not derived from [itemKey]: deriving it would mean walking the corpus, which is the one thing a lazy list must not do."
        },
        {
          "name": "itemKey",
          "type": "Object Function(int index)?",
          "required": false,
          "default": null,
          "description": "A stable identity per row, so a row's state follows its datum."
        },
        {
          "name": "keepAlive",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Keep rows alive when scrolled off. Off by default; see the docblock."
        },
        {
          "name": "padding",
          "type": "EdgeInsetsGeometry?",
          "required": false,
          "default": null,
          "description": "Padding inside the surface. Directional: start and end, never left and right."
        },
        {
          "name": "physics",
          "type": "ScrollPhysics?",
          "required": false,
          "default": null,
          "description": "Scroll physics, for a caller that needs something other than the platform default."
        },
        {
          "name": "scrollDirection",
          "type": "Axis",
          "required": false,
          "default": "Axis.vertical",
          "description": "Which axis the list scrolls along."
        },
        {
          "name": "separatorBuilder",
          "type": "Widget Function(BuildContext context, int index)?",
          "required": false,
          "default": null,
          "description": "Drawn between rows. Separators are NOT counted in [itemCount] and carry no semantic index — a hairline is not an item of the set."
        },
        {
          "name": "shrinkWrap",
          "type": "bool",
          "required": false,
          "default": "false",
          "description": "Whether the list sizes to its content instead of filling its parent."
        }
      ]
    }
  },
  "enums": {
    "LumoAlertTone": [
      "neutral",
      "accent",
      "positive",
      "critical",
      "caution"
    ],
    "LumoAlertVariant": [
      "subtle",
      "outline"
    ],
    "LumoAlign": [
      "start",
      "center",
      "end",
      "stretch",
      "baseline"
    ],
    "LumoAttachmentStatus": [
      "pending",
      "uploading",
      "done",
      "failed"
    ],
    "LumoAvatarShape": [
      "circle",
      "rounded"
    ],
    "LumoAvatarSize": [
      "sm",
      "md",
      "lg",
      "xl"
    ],
    "LumoAvatarStatus": [
      "online",
      "offline",
      "busy"
    ],
    "LumoBadgeSize": [
      "sm",
      "md"
    ],
    "LumoBadgeTone": [
      "neutral",
      "accent",
      "positive",
      "critical",
      "caution"
    ],
    "LumoBadgeVariant": [
      "solid",
      "subtle",
      "outline"
    ],
    "LumoBarChartOrientation": [
      "vertical",
      "horizontal"
    ],
    "LumoButtonGroupOrientation": [
      "horizontal",
      "vertical"
    ],
    "LumoButtonSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoButtonVariant": [
      "solid",
      "outline",
      "ghost",
      "critical"
    ],
    "LumoCalendarSystem": [
      "jalali",
      "gregorian"
    ],
    "LumoCardVariant": [
      "outline",
      "elevated",
      "sunken"
    ],
    "LumoChartTone": [
      "accent",
      "positive",
      "caution",
      "critical",
      "neutral"
    ],
    "LumoChipSize": [
      "sm",
      "md"
    ],
    "LumoDateStyle": [
      "short",
      "medium",
      "long"
    ],
    "LumoDescriptionListOrientation": [
      "inline",
      "stacked"
    ],
    "LumoDescriptionListSize": [
      "sm",
      "md"
    ],
    "LumoEmptyStateSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoGap": [
      "none",
      "xs",
      "sm",
      "md",
      "lg",
      "xl"
    ],
    "LumoIconStackSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoIconTileShape": [
      "rounded",
      "circle"
    ],
    "LumoIconTileSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoIconTileTone": [
      "neutral",
      "accent",
      "positive",
      "critical",
      "caution"
    ],
    "LumoIconTileVariant": [
      "subtle",
      "solid"
    ],
    "LumoInputGroupSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoItemSize": [
      "sm",
      "md"
    ],
    "LumoItemVariant": [
      "plain",
      "outlined",
      "muted"
    ],
    "LumoJustify": [
      "start",
      "center",
      "end",
      "between",
      "around",
      "evenly"
    ],
    "LumoLinkVariant": [
      "inline",
      "standalone"
    ],
    "LumoListBoxSelectionMode": [
      "single",
      "multiple"
    ],
    "LumoMessageSide": [
      "incoming",
      "outgoing"
    ],
    "LumoMessageStatus": [
      "sending",
      "sent",
      "read"
    ],
    "LumoNavigationBarIndicator": [
      "pill",
      "mark"
    ],
    "LumoPlacement": [
      "bottomStart",
      "bottom",
      "bottomEnd",
      "topStart",
      "top",
      "topEnd"
    ],
    "LumoPressFeedback": [
      "none",
      "tint",
      "ripple"
    ],
    "LumoProgressSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoProgressTone": [
      "accent",
      "positive",
      "caution",
      "critical"
    ],
    "LumoRadioOrientation": [
      "vertical",
      "horizontal"
    ],
    "LumoRatingSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoScrollAreaOrientation": [
      "vertical",
      "horizontal"
    ],
    "LumoSegmentedControlSize": [
      "sm",
      "md"
    ],
    "LumoSeparatorOrientation": [
      "horizontal",
      "vertical"
    ],
    "LumoSkeletonShape": [
      "rect",
      "circle",
      "text"
    ],
    "LumoSpinnerColor": [
      "current",
      "accent",
      "muted"
    ],
    "LumoSpinnerSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoStackDirection": [
      "row",
      "column"
    ],
    "LumoStepsOrientation": [
      "horizontal",
      "vertical"
    ],
    "LumoStepStatus": [
      "completed",
      "current",
      "upcoming"
    ],
    "LumoSwitchSize": [
      "md",
      "lg"
    ],
    "LumoTableAlign": [
      "start",
      "end",
      "center"
    ],
    "LumoTabsVariant": [
      "underline",
      "pill"
    ],
    "LumoTimelineOrientation": [
      "vertical",
      "horizontal"
    ],
    "LumoTimelineState": [
      "done",
      "current",
      "upcoming"
    ],
    "LumoToastTone": [
      "neutral",
      "positive",
      "critical",
      "caution"
    ],
    "LumoToggleSelectionMode": [
      "single",
      "multiple"
    ],
    "LumoToggleSize": [
      "sm",
      "md",
      "lg"
    ],
    "LumoToggleVariant": [
      "ghost",
      "outline"
    ],
    "LumoToolbarOrientation": [
      "horizontal",
      "vertical"
    ],
    "LumoToolbarOverflow": [
      "scroll",
      "wrap"
    ],
    "LumoTooltipPlacement": [
      "top",
      "bottom"
    ],
    "LumoTreeCheckState": [
      "unchecked",
      "mixed",
      "checked"
    ],
    "LumoTreeSelectionMode": [
      "none",
      "single",
      "multiple",
      "checkbox"
    ],
    "LumoTreeSelectMode": [
      "single",
      "multiple",
      "checkbox"
    ]
  }
}
